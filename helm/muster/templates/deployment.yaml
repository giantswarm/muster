apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "muster.fullname" . }}
  labels:
    {{- include "muster.labels" . | nindent 4 }}
spec:
  revisionHistoryLimit: {{ .Values.revisionHistoryLimit }}
  {{- if not .Values.autoscaling.enabled }}
  replicas: {{ .Values.replicaCount }}
  {{- end }}
  selector:
    matchLabels:
      {{- include "muster.selectorLabels" . | nindent 6 }}
  template:
    metadata:
      annotations:
        checksum/config: {{ include (print $.Template.BasePath "/configmap.yaml") . | sha256sum }}
        {{- with .Values.podAnnotations }}
        {{- toYaml . | nindent 8 }}
        {{- end }}
      labels:
        {{- include "muster.labels" . | nindent 8 }}
        {{- with .Values.podLabels }}
        {{- toYaml . | nindent 8 }}
        {{- end }}
    spec:
      {{- with .Values.imagePullSecrets }}
      imagePullSecrets:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      serviceAccountName: {{ include "muster.serviceAccountName" . }}
      automountServiceAccountToken: false
      securityContext:
        {{- toYaml .Values.podSecurityContext | nindent 8 }}
      containers:
        - name: {{ .Chart.Name }}
          securityContext:
            {{- toYaml .Values.securityContext | nindent 12 }}
          image: "{{ .Values.image.registry }}/{{ .Values.image.repository }}:{{ .Values.image.tag | default .Chart.AppVersion }}"
          imagePullPolicy: {{ .Values.image.pullPolicy }}
          command:
            - /muster
          args:
            - serve
            - --config-path=/etc/muster
            {{- if .Values.muster.debug }}
            - --debug
            {{- end }}
            {{- if .Values.muster.oauth.mcpClient.enabled }}
            - --oauth-mcp-client
            - --oauth-mcp-client-public-url={{ .Values.muster.oauth.mcpClient.publicUrl }}
            {{- if .Values.muster.oauth.mcpClient.clientId }}
            - --oauth-mcp-client-id={{ .Values.muster.oauth.mcpClient.clientId }}
            {{- end }}
            {{- end }}
            {{- if .Values.muster.oauth.server.enabled }}
            - --oauth-server
            - --oauth-server-base-url={{ .Values.muster.oauth.server.baseUrl }}
            {{- end }}
            {{- if .Values.muster.extraCaFile.secret.name }}
            - --extra-ca-file={{ .Values.muster.extraCaFile.path }}
            {{- end }}
          ports:
            - name: http
              containerPort: {{ .Values.muster.aggregator.port }}
              protocol: TCP
            {{- if eq (include "muster.prometheusExporterEnabled" .) "true" }}
            - name: metrics
              containerPort: {{ .Values.muster.observability.metrics.prometheus.port }}
              protocol: TCP
            {{- end }}
          livenessProbe:
            httpGet:
              path: /health
              port: http
            initialDelaySeconds: 10
            periodSeconds: 10
            timeoutSeconds: 5
            failureThreshold: 3
          readinessProbe:
            httpGet:
              path: /health
              port: http
            initialDelaySeconds: 5
            periodSeconds: 5
            timeoutSeconds: 3
            failureThreshold: 3
          env:
            - name: K8S_NAMESPACE
              valueFrom:
                fieldRef:
                  fieldPath: metadata.namespace
            - name: K8S_POD_NAME
              valueFrom:
                fieldRef:
                  fieldPath: metadata.name
            - name: K8S_NODE_NAME
              valueFrom:
                fieldRef:
                  fieldPath: spec.nodeName
            {{- with .Values.muster.observability.otel.endpoint }}
            - name: OTEL_EXPORTER_OTLP_ENDPOINT
              value: {{ . | quote }}
            - name: OTEL_EXPORTER_OTLP_PROTOCOL
              value: {{ $.Values.muster.observability.otel.protocol | default "grpc" | quote }}
            {{- with $.Values.muster.observability.otel.headers }}
            - name: OTEL_EXPORTER_OTLP_HEADERS
              value: {{ . | quote }}
            {{- end }}
            {{- end }}
            {{- with .Values.muster.observability.metrics.exporter }}
            - name: OTEL_METRICS_EXPORTER
              value: {{ . | quote }}
            {{- end }}
            {{- if or .Values.muster.observability.otel.endpoint .Values.muster.observability.metrics.exporter }}
            - name: OTEL_RESOURCE_ATTRIBUTES
              value: "k8s.namespace.name=$(K8S_NAMESPACE),k8s.pod.name=$(K8S_POD_NAME),k8s.node.name=$(K8S_NODE_NAME){{- with .Values.muster.observability.otel.resourceAttributes }},{{ . }}{{- end }}"
            {{- end }}
            {{- if eq (include "muster.prometheusExporterEnabled" .) "true" }}
            - name: OTEL_EXPORTER_PROMETHEUS_HOST
              value: "0.0.0.0"
            - name: OTEL_EXPORTER_PROMETHEUS_PORT
              value: {{ .Values.muster.observability.metrics.prometheus.port | quote }}
            {{- end }}
            # Note: OAuth secrets are loaded from mounted files (per MCP OAuth security recommendations)
            # See /etc/muster/secrets/ volume mount
          resources:
            {{- toYaml .Values.resources | nindent 12 }}
          volumeMounts:
            - name: tmp
              mountPath: /tmp
            - name: sa-token
              mountPath: /var/run/secrets/kubernetes.io/serviceaccount
              readOnly: true
            - name: config
              mountPath: /etc/muster
              readOnly: true
            {{- if .Values.muster.oauth.server.enabled }}
            - name: oauth-secrets
              mountPath: /etc/muster/secrets
              readOnly: true
            {{- end }}
            {{- if .Values.muster.extraCaFile.secret.name }}
            - name: extra-ca
              mountPath: {{ dir .Values.muster.extraCaFile.path }}
              readOnly: true
            {{- end }}
          {{- with .Values.volumeMounts }}
            {{- toYaml . | nindent 12 }}
          {{- end }}
      volumes:
        - name: tmp
          emptyDir: {}
        - name: config
          configMap:
            name: {{ include "muster.fullname" . }}-config
        {{- if .Values.muster.oauth.server.enabled }}
        - name: oauth-secrets
          projected:
            sources:
              # OAuth credentials (dex-client-secret, oauth-encryption-key, registration-token)
              - secret:
                  name: {{ .Values.muster.oauth.server.existingSecret | default (printf "%s-oauth" (include "muster.fullname" .)) }}
              {{- if and (eq .Values.muster.oauth.server.storage.type "valkey") (or .Values.muster.oauth.server.storage.valkey.existingSecret .Values.muster.oauth.server.existingSecret .Values.muster.oauth.server.storage.valkey.password) }}
              # Valkey password (mounted as valkey-password)
              - secret:
                  name: {{ .Values.muster.oauth.server.storage.valkey.existingSecret | default .Values.muster.oauth.server.existingSecret | default (printf "%s-oauth" (include "muster.fullname" .)) }}
                  items:
                    - key: {{ .Values.muster.oauth.server.storage.valkey.secretKeyPassword | default "valkey-password" }}
                      path: valkey-password
              {{- end }}
        {{- end }}
        - name: sa-token
          projected:
            sources:
              - serviceAccountToken:
                  expirationSeconds: 3600
                  path: token
              - configMap:
                  name: kube-root-ca.crt
                  items:
                    - key: ca.crt
                      path: ca.crt
              - downwardAPI:
                  items:
                    - path: namespace
                      fieldRef:
                        fieldPath: metadata.namespace
        {{- if .Values.muster.extraCaFile.secret.name }}
        - name: extra-ca
          secret:
            secretName: {{ .Values.muster.extraCaFile.secret.name }}
            items:
              - key: {{ .Values.muster.extraCaFile.secret.key }}
                path: {{ base .Values.muster.extraCaFile.path }}
        {{- end }}
      {{- with .Values.volumes }}
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.nodeSelector }}
      nodeSelector:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.affinity }}
      affinity:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.tolerations }}
      tolerations:
        {{- toYaml . | nindent 8 }}
      {{- end }}
