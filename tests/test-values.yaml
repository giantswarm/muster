# Test values for ATS smoke tests — not used by ct install directly.
# For future use: create helm/muster/ci/test-values.yaml if values overrides are needed.

# networkPolicy is disabled by default in values.yaml, listed here for reference.
networkPolicy:
  enabled: false
