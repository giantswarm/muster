name: "capability-validate"
category: "behavioral"
concept: "capability"
tags: ["capability", "validate", "core-api"]
timeout: "1m"

steps:
- id: "validate-capabilities"
  tool: "core_capability_validate"
  args:
    name: "validate-test-capability"
    type: "test"
    operations:
      test_operation:
        description: "Test operation for validation"
        requires: []
  expected:
    success: true 