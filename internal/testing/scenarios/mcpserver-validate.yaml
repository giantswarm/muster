name: "mcpserver-validate"
description: "Test MCP server validation without creating"
category: "behavioral"
concept: "mcpserver"
tags: ["mcpserver", "validate", "core-api"]
timeout: "30s"

steps:
- id: "validate-stdio-mcpserver"
  description: "Validate a stdio MCP server configuration"
  tool: "core_mcpserver_validate"
  args:
    name: "test-validate-server"
    type: "stdio"
    command: "echo"
    args: ["validation-test"]
    autoStart: false
  expected:
    success: true 
