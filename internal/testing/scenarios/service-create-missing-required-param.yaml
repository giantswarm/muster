name: "service-create-missing-required-param"
category: "behavioral"
concept: "service"
tags: ["service", "create", "parameters", "error-handling", "core-api"]
timeout: "1m"
# NOTE: Parameter validation is not yet fully implemented
# TODO: This test should fail when parameter validation is added

pre_configuration:
  service_classes:
  - name: "sc-requires-param"
    config:
      name: "sc-requires-param"
      version: "1.0.0"
      serviceConfig:
        createParameters:
          message:
            required: true
        lifecycleTools:
          start: { tool: "core_config_get" }
          stop: { tool: "core_config_get" }
steps:
- id: "create-missing-param"
  tool: "core_service_create"
  args:
    serviceClassName: "sc-requires-param"
    name: "my-failed-service"
  expected:
    success: true  # Currently succeeds because validation not implemented
  cleanup:
  - tool: "core_service_delete"
    args: { name: "my-failed-service" }
 