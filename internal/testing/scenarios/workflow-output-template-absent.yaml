name: "workflow-output-template-absent"
description: "When no output template is declared the workflow returns the default response unchanged — execution_id, workflow, status, input, steps[] and template_vars are all present and the output-flag-driven steps[].result behavior is preserved (issue #874 back-compat)"
category: "behavioral"
concept: "workflow"
tags: ["workflow", "output", "output-template", "back-compat", "core-api"]
timeout: "2m"

pre_configuration:
  mcp_servers:
  - name: "echo-provider"
    config:
      tools:
      - name: "echo"
        responses: [ { response: { said: "{{ .text }}" } } ]

steps:
- id: "create-no-output-template"
  tool: "core_workflow_create"
  args:
    name: "no-output-template"
    args:
      who: { type: "string", required: true }
    steps:
      - id: "greet"
        tool: "x_echo-provider_echo"
        args: { text: "hello {{ .input.who }}" }
        output: true
  expected:
    success: true
  cleanup:
    - tool: "core_workflow_delete"
      args: { name: "no-output-template" }

# No spec.output: the full default response is returned, including the echoed
# input and the output step's result inside steps[].
- id: "run-no-output-template"
  tool: "workflow_no-output-template"
  args:
    who: "world"
  expected:
    success: true
    json_path:
      workflow: "no-output-template"
      status: "completed"
      input.who: "world"
      steps.0.id: "greet"
      steps.0.status: "completed"
      steps.0.result.said: "hello world"
    contains: ["execution_id", "template_vars"]

cleanup:
- id: "delete-no-output-template"
  tool: "core_workflow_delete"
  args: { name: "no-output-template" }
  expected:
    success: true
