name: "workflow-output-projection-error-recoverable"
description: "A projection render error no longer discards successful step results. When the projection references a value that does not resolve (missingkey=error), the workflow execution fails loud BUT the response still carries the recorded step results plus an 'output_error' message, so the underlying data stays recoverable for debugging the projection. Issue #877."
category: "behavioral"
concept: "workflow"
tags: ["workflow", "output", "projection", "error", "core-api"]
timeout: "2m"

pre_configuration:
  mcp_servers:
  - name: "k8s-mock"
    config:
      tools:
      - name: "list_pods"
        responses: [ { response: { items: [ { name: "pod-a" } ] } } ]

steps:
- id: "create-broken-projection"
  tool: "core_workflow_create"
  args:
    name: "broken-projection"
    steps:
      - id: "pods"
        tool: "x_k8s-mock_list_pods"
    output:
      # References a step that never ran: the render fails under missingkey=error.
      value: "{{ .results.missing.field }}"
  expected:
    success: true
  cleanup:
    - tool: "core_workflow_delete"
      args: { name: "broken-projection" }

# The run fails (the projection cannot render) but the successful step's result
# (pod-a) and the projection error are still recoverable from the response.
- id: "run-broken-projection"
  tool: "workflow_broken-projection"
  args: {}
  expected:
    success: false
    error_contains: ["output projection"]
    contains:
      - "output_error"
      - "pod-a"

cleanup:
- id: "delete-broken-projection"
  tool: "core_workflow_delete"
  args: { name: "broken-projection" }
  expected:
    success: true
