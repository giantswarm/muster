name: "capability-delete"
category: "behavioral"
concept: "capability"
tags: ["capability", "delete", "core-api"]
timeout: "1m"

pre_configuration:
  capabilities:
    - name: "delete-test-capability"
      config:
        name: "delete-test-capability"
        type: "test"
        operations:
          test_operation:
            description: "Test operation for delete test"
            requires: []

steps:
- id: "delete-the-capability"
  tool: "core_capability_delete"
  args:
    name: "delete-test-capability"
  expected:
    success: true

- id: "verify-capability-is-gone"
  tool: "core_capability_get"
  args:
    name: "delete-test-capability"
  expected:
    success: false
    error_contains: ["not found"] 