# Generated by `go generate ./internal/testing/fixtures/scale/` from the shape in shape.go. DO NOT EDIT.

# The installation-shaped scale fixture: 87 MCPServers (84 session-authenticated, 5 families on 28 installations, 3 in-house), 21 capability documents, 282 workflows. Every name is invented.
documents:
  # The distinct capability documents; a server's tools alias one of them. The harness ignores this key.
  clusters-v1: &clusters-v1
    - name: list_clusters
      description: List the workload clusters of the installation with their release, node count and readiness.
      annotations:
        read_only_hint: true
      input_schema:
        type: object
        properties:
          organization:
            type: string
            description: Restrict the list to the clusters of one organization.
          phase:
            type: string
            description: Restrict the list to clusters in one lifecycle phase.
      responses:
        - response: {ok: true, tool: list_clusters}
    - name: get_cluster
      description: 'Describe one workload cluster: release, node pools, conditions and the last transition of each.'
      annotations:
        read_only_hint: true
      input_schema:
        type: object
        properties:
          cluster:
            type: string
            description: Name of the cluster.
        required: [cluster]
      responses:
        - response: {ok: true, tool: get_cluster}
    - name: list_node_pools
      description: List the node pools of a cluster with their instance type, size and autoscaling bounds.
      annotations:
        read_only_hint: true
      input_schema:
        type: object
        properties:
          cluster:
            type: string
            description: Name of the cluster.
        required: [cluster]
      responses:
        - response: {ok: true, tool: list_node_pools}
    - name: get_release
      description: 'Describe a release: the component versions it ships and the clusters running it.'
      annotations:
        read_only_hint: true
      input_schema:
        type: object
        properties:
          release:
            type: string
            description: Release version, for example 30.1.0.
        required: [release]
      responses:
        - response: {ok: true, tool: get_release}
    - name: list_upgrades
      description: List the upgrades scheduled or in progress on the installation.
      annotations:
        read_only_hint: true
      input_schema:
        type: object
        properties:
          cluster:
            type: string
            description: Restrict to one cluster.
          window:
            type: string
            description: Only upgrades scheduled within this duration, for example 72h.
      responses:
        - response: {ok: true, tool: list_upgrades}
    - name: get_quota
      description: 'Report the installation''s quota usage: clusters, nodes and volumes against the contracted limits.'
      annotations:
        read_only_hint: true
      input_schema:
        type: object
        properties:
          organization:
            type: string
            description: Restrict to one organization.
      responses:
        - response: {ok: true, tool: get_quota}
  clusters-v2: &clusters-v2
    - name: list_clusters
      description: List the workload clusters of the installation with their release, node count and readiness.
      annotations:
        read_only_hint: true
      input_schema:
        type: object
        properties:
          organization:
            type: string
            description: Restrict the list to the clusters of one organization.
          phase:
            type: string
            description: Restrict the list to clusters in one lifecycle phase.
      responses:
        - response: {ok: true, tool: list_clusters}
    - name: get_cluster
      description: 'Describe one workload cluster: release, node pools, conditions and the last transition of each.'
      annotations:
        read_only_hint: true
      input_schema:
        type: object
        properties:
          cluster:
            type: string
            description: Name of the cluster.
        required: [cluster]
      responses:
        - response: {ok: true, tool: get_cluster}
    - name: list_node_pools
      description: List the node pools of a cluster with their instance type, size and autoscaling bounds.
      annotations:
        read_only_hint: true
      input_schema:
        type: object
        properties:
          cluster:
            type: string
            description: Name of the cluster.
        required: [cluster]
      responses:
        - response: {ok: true, tool: list_node_pools}
    - name: get_release
      description: 'Describe a release: the component versions it ships and the clusters running it.'
      annotations:
        read_only_hint: true
      input_schema:
        type: object
        properties:
          release:
            type: string
            description: Release version, for example 30.1.0.
        required: [release]
      responses:
        - response: {ok: true, tool: get_release}
    - name: list_upgrades
      description: List the upgrades scheduled or in progress on the installation.
      annotations:
        read_only_hint: true
      input_schema:
        type: object
        properties:
          cluster:
            type: string
            description: Restrict to one cluster.
          window:
            type: string
            description: Only upgrades scheduled within this duration, for example 72h.
      responses:
        - response: {ok: true, tool: list_upgrades}
    - name: get_quota
      description: 'Report the installation''s quota usage: clusters, nodes and volumes against the contracted limits.'
      annotations:
        read_only_hint: true
      input_schema:
        type: object
        properties:
          organization:
            type: string
            description: Restrict to one organization.
      responses:
        - response: {ok: true, tool: get_quota}
    - name: scale_node_pool
      description: Change the size of a node pool.
      annotations:
        destructive_hint: true
      input_schema:
        type: object
        properties:
          cluster:
            type: string
            description: Name of the cluster.
          node_pool:
            type: string
            description: Name of the node pool.
          replicas:
            type: string
            description: Desired number of nodes.
        required: [cluster, node_pool, replicas]
      responses:
        - response: {ok: true, tool: scale_node_pool}
  clusters-v3: &clusters-v3
    - name: list_clusters
      description: List the workload clusters of the installation with their release, node count and readiness.
      annotations:
        read_only_hint: true
      input_schema:
        type: object
        properties:
          organization:
            type: string
            description: Restrict the list to the clusters of one organization.
          phase:
            type: string
            description: Restrict the list to clusters in one lifecycle phase.
      responses:
        - response: {ok: true, tool: list_clusters}
    - name: get_cluster
      description: 'Describe one workload cluster: release, node pools, conditions and the last transition of each.'
      annotations:
        read_only_hint: true
      input_schema:
        type: object
        properties:
          cluster:
            type: string
            description: Name of the cluster.
        required: [cluster]
      responses:
        - response: {ok: true, tool: get_cluster}
    - name: list_node_pools
      description: List the node pools of a cluster with their instance type, size and autoscaling bounds.
      annotations:
        read_only_hint: true
      input_schema:
        type: object
        properties:
          cluster:
            type: string
            description: Name of the cluster.
        required: [cluster]
      responses:
        - response: {ok: true, tool: list_node_pools}
    - name: get_release
      description: 'Describe a release: the component versions it ships and the clusters running it.'
      annotations:
        read_only_hint: true
      input_schema:
        type: object
        properties:
          release:
            type: string
            description: Release version, for example 30.1.0.
        required: [release]
      responses:
        - response: {ok: true, tool: get_release}
    - name: list_upgrades
      description: List the upgrades scheduled or in progress on the installation.
      annotations:
        read_only_hint: true
      input_schema:
        type: object
        properties:
          cluster:
            type: string
            description: Restrict to one cluster.
          window:
            type: string
            description: Only upgrades scheduled within this duration, for example 72h.
      responses:
        - response: {ok: true, tool: list_upgrades}
    - name: get_quota
      description: 'Report the installation''s quota usage: clusters, nodes and volumes against the contracted limits.'
      annotations:
        read_only_hint: true
      input_schema:
        type: object
        properties:
          organization:
            type: string
            description: Restrict to one organization.
      responses:
        - response: {ok: true, tool: get_quota}
    - name: scale_node_pool
      description: Change the size of a node pool.
      annotations:
        destructive_hint: true
      input_schema:
        type: object
        properties:
          cluster:
            type: string
            description: Name of the cluster.
          node_pool:
            type: string
            description: Name of the node pool.
          replicas:
            type: string
            description: Desired number of nodes.
        required: [cluster, node_pool, replicas]
      responses:
        - response: {ok: true, tool: scale_node_pool}
    - name: schedule_upgrade
      description: Schedule a cluster upgrade to a release at a time.
      annotations:
        destructive_hint: true
      input_schema:
        type: object
        properties:
          cluster:
            type: string
            description: Name of the cluster.
          release:
            type: string
            description: Target release version.
          at:
            type: string
            description: RFC 3339 time to start the upgrade.
        required: [cluster, release]
      responses:
        - response: {ok: true, tool: schedule_upgrade}
  clusters-v4: &clusters-v4
    - name: list_clusters
      description: List the workload clusters of the installation with their release, node count and readiness.
      annotations:
        read_only_hint: true
      input_schema:
        type: object
        properties:
          organization:
            type: string
            description: Restrict the list to the clusters of one organization.
          phase:
            type: string
            description: Restrict the list to clusters in one lifecycle phase.
      responses:
        - response: {ok: true, tool: list_clusters}
    - name: get_cluster
      description: 'Describe one workload cluster: release, node pools, conditions and the last transition of each.'
      annotations:
        read_only_hint: true
      input_schema:
        type: object
        properties:
          cluster:
            type: string
            description: Name of the cluster.
        required: [cluster]
      responses:
        - response: {ok: true, tool: get_cluster}
    - name: list_node_pools
      description: List the node pools of a cluster with their instance type, size and autoscaling bounds.
      annotations:
        read_only_hint: true
      input_schema:
        type: object
        properties:
          cluster:
            type: string
            description: Name of the cluster.
        required: [cluster]
      responses:
        - response: {ok: true, tool: list_node_pools}
    - name: get_release
      description: 'Describe a release: the component versions it ships and the clusters running it.'
      annotations:
        read_only_hint: true
      input_schema:
        type: object
        properties:
          release:
            type: string
            description: Release version, for example 30.1.0.
        required: [release]
      responses:
        - response: {ok: true, tool: get_release}
    - name: list_upgrades
      description: List the upgrades scheduled or in progress on the installation.
      annotations:
        read_only_hint: true
      input_schema:
        type: object
        properties:
          cluster:
            type: string
            description: Restrict to one cluster.
          window:
            type: string
            description: Only upgrades scheduled within this duration, for example 72h.
      responses:
        - response: {ok: true, tool: list_upgrades}
    - name: get_quota
      description: 'Report the installation''s quota usage: clusters, nodes and volumes against the contracted limits.'
      annotations:
        read_only_hint: true
      input_schema:
        type: object
        properties:
          organization:
            type: string
            description: Restrict to one organization.
      responses:
        - response: {ok: true, tool: get_quota}
    - name: scale_node_pool
      description: Change the size of a node pool.
      annotations:
        destructive_hint: true
      input_schema:
        type: object
        properties:
          cluster:
            type: string
            description: Name of the cluster.
          node_pool:
            type: string
            description: Name of the node pool.
          replicas:
            type: string
            description: Desired number of nodes.
        required: [cluster, node_pool, replicas]
      responses:
        - response: {ok: true, tool: scale_node_pool}
    - name: schedule_upgrade
      description: Schedule a cluster upgrade to a release at a time.
      annotations:
        destructive_hint: true
      input_schema:
        type: object
        properties:
          cluster:
            type: string
            description: Name of the cluster.
          release:
            type: string
            description: Target release version.
          at:
            type: string
            description: RFC 3339 time to start the upgrade.
        required: [cluster, release]
      responses:
        - response: {ok: true, tool: schedule_upgrade}
    - name: list_events
      description: List the platform events of a cluster in the given window.
      annotations:
        read_only_hint: true
      input_schema:
        type: object
        properties:
          cluster:
            type: string
            description: Name of the cluster.
          window:
            type: string
            description: Window to look back, for example 24h.
        required: [cluster]
      responses:
        - response: {ok: true, tool: list_events}
  metrics-v1: &metrics-v1
    - name: query_instant
      description: Evaluate a query at one instant and return the resulting samples.
      annotations:
        read_only_hint: true
      input_schema:
        type: object
        properties:
          query:
            type: string
            description: The query expression.
          at:
            type: string
            description: RFC 3339 evaluation time; now when absent.
        required: [query]
      responses:
        - response: {ok: true, tool: query_instant}
    - name: query_range
      description: Evaluate a query over a window with a step and return the series.
      annotations:
        read_only_hint: true
      input_schema:
        type: object
        properties:
          query:
            type: string
            description: The query expression.
          window:
            type: string
            description: Window to look back, for example 6h.
          step:
            type: string
            description: Resolution step, for example 1m.
        required: [query]
      responses:
        - response: {ok: true, tool: query_range}
    - name: list_series
      description: List the series matching a selector, with their labels.
      annotations:
        read_only_hint: true
      input_schema:
        type: object
        properties:
          selector:
            type: string
            description: A series selector.
          limit:
            type: string
            description: Maximum number of series to return.
        required: [selector]
      responses:
        - response: {ok: true, tool: list_series}
    - name: list_label_values
      description: List the values a label takes across the matching series.
      annotations:
        read_only_hint: true
      input_schema:
        type: object
        properties:
          label:
            type: string
            description: Label name.
          selector:
            type: string
            description: Restrict to series matching this selector.
        required: [label]
      responses:
        - response: {ok: true, tool: list_label_values}
  metrics-v2: &metrics-v2
    - name: query_instant
      description: Evaluate a query at one instant and return the resulting samples.
      annotations:
        read_only_hint: true
      input_schema:
        type: object
        properties:
          query:
            type: string
            description: The query expression.
          at:
            type: string
            description: RFC 3339 evaluation time; now when absent.
        required: [query]
      responses:
        - response: {ok: true, tool: query_instant}
    - name: query_range
      description: Evaluate a query over a window with a step and return the series.
      annotations:
        read_only_hint: true
      input_schema:
        type: object
        properties:
          query:
            type: string
            description: The query expression.
          window:
            type: string
            description: Window to look back, for example 6h.
          step:
            type: string
            description: Resolution step, for example 1m.
        required: [query]
      responses:
        - response: {ok: true, tool: query_range}
    - name: list_series
      description: List the series matching a selector, with their labels.
      annotations:
        read_only_hint: true
      input_schema:
        type: object
        properties:
          selector:
            type: string
            description: A series selector.
          limit:
            type: string
            description: Maximum number of series to return.
        required: [selector]
      responses:
        - response: {ok: true, tool: list_series}
    - name: list_label_values
      description: List the values a label takes across the matching series.
      annotations:
        read_only_hint: true
      input_schema:
        type: object
        properties:
          label:
            type: string
            description: Label name.
          selector:
            type: string
            description: Restrict to series matching this selector.
        required: [label]
      responses:
        - response: {ok: true, tool: list_label_values}
    - name: list_targets
      description: List the scrape targets with their health and last scrape.
      annotations:
        read_only_hint: true
      input_schema:
        type: object
        properties:
          job:
            type: string
            description: Restrict to one scrape job.
      responses:
        - response: {ok: true, tool: list_targets}
  metrics-v3: &metrics-v3
    - name: query_instant
      description: Evaluate a query at one instant and return the resulting samples.
      annotations:
        read_only_hint: true
      input_schema:
        type: object
        properties:
          query:
            type: string
            description: The query expression.
          at:
            type: string
            description: RFC 3339 evaluation time; now when absent.
        required: [query]
      responses:
        - response: {ok: true, tool: query_instant}
    - name: query_range
      description: Evaluate a query over a window with a step and return the series.
      annotations:
        read_only_hint: true
      input_schema:
        type: object
        properties:
          query:
            type: string
            description: The query expression.
          window:
            type: string
            description: Window to look back, for example 6h.
          step:
            type: string
            description: Resolution step, for example 1m.
        required: [query]
      responses:
        - response: {ok: true, tool: query_range}
    - name: list_series
      description: List the series matching a selector, with their labels.
      annotations:
        read_only_hint: true
      input_schema:
        type: object
        properties:
          selector:
            type: string
            description: A series selector.
          limit:
            type: string
            description: Maximum number of series to return.
        required: [selector]
      responses:
        - response: {ok: true, tool: list_series}
    - name: list_label_values
      description: List the values a label takes across the matching series.
      annotations:
        read_only_hint: true
      input_schema:
        type: object
        properties:
          label:
            type: string
            description: Label name.
          selector:
            type: string
            description: Restrict to series matching this selector.
        required: [label]
      responses:
        - response: {ok: true, tool: list_label_values}
    - name: list_targets
      description: List the scrape targets with their health and last scrape.
      annotations:
        read_only_hint: true
      input_schema:
        type: object
        properties:
          job:
            type: string
            description: Restrict to one scrape job.
      responses:
        - response: {ok: true, tool: list_targets}
    - name: list_rules
      description: List the recording and alerting rules with their health and last evaluation.
      annotations:
        read_only_hint: true
      input_schema:
        type: object
        properties:
          group:
            type: string
            description: Restrict to one rule group.
      responses:
        - response: {ok: true, tool: list_rules}
  metrics-v4: &metrics-v4
    - name: query_instant
      description: Evaluate a query at one instant and return the resulting samples.
      annotations:
        read_only_hint: true
      input_schema:
        type: object
        properties:
          query:
            type: string
            description: The query expression.
          at:
            type: string
            description: RFC 3339 evaluation time; now when absent.
        required: [query]
      responses:
        - response: {ok: true, tool: query_instant}
    - name: query_range
      description: Evaluate a query over a window with a step and return the series.
      annotations:
        read_only_hint: true
      input_schema:
        type: object
        properties:
          query:
            type: string
            description: The query expression.
          window:
            type: string
            description: Window to look back, for example 6h.
          step:
            type: string
            description: Resolution step, for example 1m.
        required: [query]
      responses:
        - response: {ok: true, tool: query_range}
    - name: list_series
      description: List the series matching a selector, with their labels.
      annotations:
        read_only_hint: true
      input_schema:
        type: object
        properties:
          selector:
            type: string
            description: A series selector.
          limit:
            type: string
            description: Maximum number of series to return.
        required: [selector]
      responses:
        - response: {ok: true, tool: list_series}
    - name: list_label_values
      description: List the values a label takes across the matching series.
      annotations:
        read_only_hint: true
      input_schema:
        type: object
        properties:
          label:
            type: string
            description: Label name.
          selector:
            type: string
            description: Restrict to series matching this selector.
        required: [label]
      responses:
        - response: {ok: true, tool: list_label_values}
    - name: list_targets
      description: List the scrape targets with their health and last scrape.
      annotations:
        read_only_hint: true
      input_schema:
        type: object
        properties:
          job:
            type: string
            description: Restrict to one scrape job.
      responses:
        - response: {ok: true, tool: list_targets}
    - name: list_rules
      description: List the recording and alerting rules with their health and last evaluation.
      annotations:
        read_only_hint: true
      input_schema:
        type: object
        properties:
          group:
            type: string
            description: Restrict to one rule group.
      responses:
        - response: {ok: true, tool: list_rules}
    - name: explain_series
      description: 'Describe where a series comes from: the job, the exporter and the rule that records it.'
      annotations:
        read_only_hint: true
      input_schema:
        type: object
        properties:
          series:
            type: string
            description: The series name.
        required: [series]
      responses:
        - response: {ok: true, tool: explain_series}
  alerts-v1: &alerts-v1
    - name: list_alerts
      description: List the alerts currently firing or pending with their labels and duration.
      annotations:
        read_only_hint: true
      input_schema:
        type: object
        properties:
          state:
            type: string
            description: Restrict to firing or pending alerts.
          severity:
            type: string
            description: Restrict to one severity.
      responses:
        - response: {ok: true, tool: list_alerts}
    - name: get_alert
      description: 'Describe one alert: its labels, annotations, the rule behind it and the silences that match it.'
      annotations:
        read_only_hint: true
      input_schema:
        type: object
        properties:
          fingerprint:
            type: string
            description: The alert's fingerprint.
        required: [fingerprint]
      responses:
        - response: {ok: true, tool: get_alert}
    - name: list_silences
      description: List the silences with their matchers, creator and expiry.
      annotations:
        read_only_hint: true
      input_schema:
        type: object
        properties:
          state:
            type: string
            description: Restrict to active, pending or expired silences.
      responses:
        - response: {ok: true, tool: list_silences}
  alerts-v2: &alerts-v2
    - name: list_alerts
      description: List the alerts currently firing or pending with their labels and duration.
      annotations:
        read_only_hint: true
      input_schema:
        type: object
        properties:
          state:
            type: string
            description: Restrict to firing or pending alerts.
          severity:
            type: string
            description: Restrict to one severity.
      responses:
        - response: {ok: true, tool: list_alerts}
    - name: get_alert
      description: 'Describe one alert: its labels, annotations, the rule behind it and the silences that match it.'
      annotations:
        read_only_hint: true
      input_schema:
        type: object
        properties:
          fingerprint:
            type: string
            description: The alert's fingerprint.
        required: [fingerprint]
      responses:
        - response: {ok: true, tool: get_alert}
    - name: list_silences
      description: List the silences with their matchers, creator and expiry.
      annotations:
        read_only_hint: true
      input_schema:
        type: object
        properties:
          state:
            type: string
            description: Restrict to active, pending or expired silences.
      responses:
        - response: {ok: true, tool: list_silences}
    - name: create_silence
      description: Silence the alerts matching the given matchers until a time.
      annotations:
        destructive_hint: true
      input_schema:
        type: object
        properties:
          matchers:
            type: string
            description: Label matchers, comma separated.
          until:
            type: string
            description: RFC 3339 expiry time.
          comment:
            type: string
            description: Why the alerts are silenced.
        required: [matchers, until, comment]
      responses:
        - response: {ok: true, tool: create_silence}
  alerts-v3: &alerts-v3
    - name: list_alerts
      description: List the alerts currently firing or pending with their labels and duration.
      annotations:
        read_only_hint: true
      input_schema:
        type: object
        properties:
          state:
            type: string
            description: Restrict to firing or pending alerts.
          severity:
            type: string
            description: Restrict to one severity.
      responses:
        - response: {ok: true, tool: list_alerts}
    - name: get_alert
      description: 'Describe one alert: its labels, annotations, the rule behind it and the silences that match it.'
      annotations:
        read_only_hint: true
      input_schema:
        type: object
        properties:
          fingerprint:
            type: string
            description: The alert's fingerprint.
        required: [fingerprint]
      responses:
        - response: {ok: true, tool: get_alert}
    - name: list_silences
      description: List the silences with their matchers, creator and expiry.
      annotations:
        read_only_hint: true
      input_schema:
        type: object
        properties:
          state:
            type: string
            description: Restrict to active, pending or expired silences.
      responses:
        - response: {ok: true, tool: list_silences}
    - name: create_silence
      description: Silence the alerts matching the given matchers until a time.
      annotations:
        destructive_hint: true
      input_schema:
        type: object
        properties:
          matchers:
            type: string
            description: Label matchers, comma separated.
          until:
            type: string
            description: RFC 3339 expiry time.
          comment:
            type: string
            description: Why the alerts are silenced.
        required: [matchers, until, comment]
      responses:
        - response: {ok: true, tool: create_silence}
    - name: expire_silence
      description: Expire a silence now.
      annotations:
        destructive_hint: true
      input_schema:
        type: object
        properties:
          silence:
            type: string
            description: The silence's identifier.
        required: [silence]
      responses:
        - response: {ok: true, tool: expire_silence}
  alerts-v4: &alerts-v4
    - name: list_alerts
      description: List the alerts currently firing or pending with their labels and duration.
      annotations:
        read_only_hint: true
      input_schema:
        type: object
        properties:
          state:
            type: string
            description: Restrict to firing or pending alerts.
          severity:
            type: string
            description: Restrict to one severity.
      responses:
        - response: {ok: true, tool: list_alerts}
    - name: get_alert
      description: 'Describe one alert: its labels, annotations, the rule behind it and the silences that match it.'
      annotations:
        read_only_hint: true
      input_schema:
        type: object
        properties:
          fingerprint:
            type: string
            description: The alert's fingerprint.
        required: [fingerprint]
      responses:
        - response: {ok: true, tool: get_alert}
    - name: list_silences
      description: List the silences with their matchers, creator and expiry.
      annotations:
        read_only_hint: true
      input_schema:
        type: object
        properties:
          state:
            type: string
            description: Restrict to active, pending or expired silences.
      responses:
        - response: {ok: true, tool: list_silences}
    - name: create_silence
      description: Silence the alerts matching the given matchers until a time.
      annotations:
        destructive_hint: true
      input_schema:
        type: object
        properties:
          matchers:
            type: string
            description: Label matchers, comma separated.
          until:
            type: string
            description: RFC 3339 expiry time.
          comment:
            type: string
            description: Why the alerts are silenced.
        required: [matchers, until, comment]
      responses:
        - response: {ok: true, tool: create_silence}
    - name: expire_silence
      description: Expire a silence now.
      annotations:
        destructive_hint: true
      input_schema:
        type: object
        properties:
          silence:
            type: string
            description: The silence's identifier.
        required: [silence]
      responses:
        - response: {ok: true, tool: expire_silence}
    - name: list_routes
      description: List the notification routes and the receivers they deliver to.
      annotations:
        read_only_hint: true
      input_schema:
        type: object
        properties: {}
      responses:
        - response: {ok: true, tool: list_routes}
  logs-v1: &logs-v1
    - name: query_logs
      description: Return the log lines matching a selector in a window.
      annotations:
        read_only_hint: true
      input_schema:
        type: object
        properties:
          selector:
            type: string
            description: Stream selector.
          window:
            type: string
            description: Window to look back, for example 1h.
          limit:
            type: string
            description: Maximum number of lines.
        required: [selector]
      responses:
        - response: {ok: true, tool: query_logs}
    - name: list_streams
      description: List the log streams matching a selector, with their labels and volume.
      annotations:
        read_only_hint: true
      input_schema:
        type: object
        properties:
          selector:
            type: string
            description: Stream selector.
      responses:
        - response: {ok: true, tool: list_streams}
    - name: count_lines
      description: Count the matching log lines per interval over a window.
      annotations:
        read_only_hint: true
      input_schema:
        type: object
        properties:
          selector:
            type: string
            description: Stream selector.
          window:
            type: string
            description: Window to look back.
          interval:
            type: string
            description: Bucket size, for example 5m.
        required: [selector]
      responses:
        - response: {ok: true, tool: count_lines}
    - name: detect_patterns
      description: Cluster the matching log lines into patterns and return each pattern with its count.
      annotations:
        read_only_hint: true
      input_schema:
        type: object
        properties:
          selector:
            type: string
            description: Stream selector.
          window:
            type: string
            description: Window to look back.
        required: [selector]
      responses:
        - response: {ok: true, tool: detect_patterns}
  logs-v2: &logs-v2
    - name: query_logs
      description: Return the log lines matching a selector in a window.
      annotations:
        read_only_hint: true
      input_schema:
        type: object
        properties:
          selector:
            type: string
            description: Stream selector.
          window:
            type: string
            description: Window to look back, for example 1h.
          limit:
            type: string
            description: Maximum number of lines.
        required: [selector]
      responses:
        - response: {ok: true, tool: query_logs}
    - name: list_streams
      description: List the log streams matching a selector, with their labels and volume.
      annotations:
        read_only_hint: true
      input_schema:
        type: object
        properties:
          selector:
            type: string
            description: Stream selector.
      responses:
        - response: {ok: true, tool: list_streams}
    - name: count_lines
      description: Count the matching log lines per interval over a window.
      annotations:
        read_only_hint: true
      input_schema:
        type: object
        properties:
          selector:
            type: string
            description: Stream selector.
          window:
            type: string
            description: Window to look back.
          interval:
            type: string
            description: Bucket size, for example 5m.
        required: [selector]
      responses:
        - response: {ok: true, tool: count_lines}
    - name: detect_patterns
      description: Cluster the matching log lines into patterns and return each pattern with its count.
      annotations:
        read_only_hint: true
      input_schema:
        type: object
        properties:
          selector:
            type: string
            description: Stream selector.
          window:
            type: string
            description: Window to look back.
        required: [selector]
      responses:
        - response: {ok: true, tool: detect_patterns}
    - name: get_retention
      description: Report the retention applied to the matching streams.
      annotations:
        read_only_hint: true
      input_schema:
        type: object
        properties:
          selector:
            type: string
            description: Stream selector.
      responses:
        - response: {ok: true, tool: get_retention}
  logs-v3: &logs-v3
    - name: query_logs
      description: Return the log lines matching a selector in a window.
      annotations:
        read_only_hint: true
      input_schema:
        type: object
        properties:
          selector:
            type: string
            description: Stream selector.
          window:
            type: string
            description: Window to look back, for example 1h.
          limit:
            type: string
            description: Maximum number of lines.
        required: [selector]
      responses:
        - response: {ok: true, tool: query_logs}
    - name: list_streams
      description: List the log streams matching a selector, with their labels and volume.
      annotations:
        read_only_hint: true
      input_schema:
        type: object
        properties:
          selector:
            type: string
            description: Stream selector.
      responses:
        - response: {ok: true, tool: list_streams}
    - name: count_lines
      description: Count the matching log lines per interval over a window.
      annotations:
        read_only_hint: true
      input_schema:
        type: object
        properties:
          selector:
            type: string
            description: Stream selector.
          window:
            type: string
            description: Window to look back.
          interval:
            type: string
            description: Bucket size, for example 5m.
        required: [selector]
      responses:
        - response: {ok: true, tool: count_lines}
    - name: detect_patterns
      description: Cluster the matching log lines into patterns and return each pattern with its count.
      annotations:
        read_only_hint: true
      input_schema:
        type: object
        properties:
          selector:
            type: string
            description: Stream selector.
          window:
            type: string
            description: Window to look back.
        required: [selector]
      responses:
        - response: {ok: true, tool: detect_patterns}
    - name: get_retention
      description: Report the retention applied to the matching streams.
      annotations:
        read_only_hint: true
      input_schema:
        type: object
        properties:
          selector:
            type: string
            description: Stream selector.
      responses:
        - response: {ok: true, tool: get_retention}
    - name: tail_stream
      description: Return the newest lines of a stream.
      annotations:
        read_only_hint: true
      input_schema:
        type: object
        properties:
          selector:
            type: string
            description: Stream selector.
          lines:
            type: string
            description: Number of lines.
        required: [selector]
      responses:
        - response: {ok: true, tool: tail_stream}
  ledger-v1: &ledger-v1
    - name: list_usage
      description: List the metered usage of the installation per cluster and day in a window.
      annotations:
        read_only_hint: true
      input_schema:
        type: object
        properties:
          window:
            type: string
            description: Window to look back, for example 720h.
          cluster:
            type: string
            description: Restrict to one cluster.
      responses:
        - response: {ok: true, tool: list_usage}
    - name: get_statement
      description: Return the statement of a billing period.
      annotations:
        read_only_hint: true
      input_schema:
        type: object
        properties:
          period:
            type: string
            description: Billing period, for example 2026-08.
        required: [period]
      responses:
        - response: {ok: true, tool: get_statement}
    - name: list_adjustments
      description: List the manual adjustments booked against the installation.
      annotations:
        read_only_hint: true
      input_schema:
        type: object
        properties:
          period:
            type: string
            description: Restrict to one billing period.
      responses:
        - response: {ok: true, tool: list_adjustments}
  ledger-v2: &ledger-v2
    - name: list_usage
      description: List the metered usage of the installation per cluster and day in a window.
      annotations:
        read_only_hint: true
      input_schema:
        type: object
        properties:
          window:
            type: string
            description: Window to look back, for example 720h.
          cluster:
            type: string
            description: Restrict to one cluster.
      responses:
        - response: {ok: true, tool: list_usage}
    - name: get_statement
      description: Return the statement of a billing period.
      annotations:
        read_only_hint: true
      input_schema:
        type: object
        properties:
          period:
            type: string
            description: Billing period, for example 2026-08.
        required: [period]
      responses:
        - response: {ok: true, tool: get_statement}
    - name: list_adjustments
      description: List the manual adjustments booked against the installation.
      annotations:
        read_only_hint: true
      input_schema:
        type: object
        properties:
          period:
            type: string
            description: Restrict to one billing period.
      responses:
        - response: {ok: true, tool: list_adjustments}
    - name: book_adjustment
      description: Book a manual adjustment against a billing period.
      annotations:
        destructive_hint: true
      input_schema:
        type: object
        properties:
          period:
            type: string
            description: Billing period.
          amount:
            type: string
            description: Signed amount in the contract's currency.
          reason:
            type: string
            description: Why the adjustment is booked.
        required: [period, amount, reason]
      responses:
        - response: {ok: true, tool: book_adjustment}
  ledger-v3: &ledger-v3
    - name: list_usage
      description: List the metered usage of the installation per cluster and day in a window.
      annotations:
        read_only_hint: true
      input_schema:
        type: object
        properties:
          window:
            type: string
            description: Window to look back, for example 720h.
          cluster:
            type: string
            description: Restrict to one cluster.
      responses:
        - response: {ok: true, tool: list_usage}
    - name: get_statement
      description: Return the statement of a billing period.
      annotations:
        read_only_hint: true
      input_schema:
        type: object
        properties:
          period:
            type: string
            description: Billing period, for example 2026-08.
        required: [period]
      responses:
        - response: {ok: true, tool: get_statement}
    - name: list_adjustments
      description: List the manual adjustments booked against the installation.
      annotations:
        read_only_hint: true
      input_schema:
        type: object
        properties:
          period:
            type: string
            description: Restrict to one billing period.
      responses:
        - response: {ok: true, tool: list_adjustments}
    - name: book_adjustment
      description: Book a manual adjustment against a billing period.
      annotations:
        destructive_hint: true
      input_schema:
        type: object
        properties:
          period:
            type: string
            description: Billing period.
          amount:
            type: string
            description: Signed amount in the contract's currency.
          reason:
            type: string
            description: Why the adjustment is booked.
        required: [period, amount, reason]
      responses:
        - response: {ok: true, tool: book_adjustment}
    - name: export_records
      description: Export the raw usage records of a period.
      annotations:
        read_only_hint: true
      input_schema:
        type: object
        properties:
          period:
            type: string
            description: Billing period.
          format:
            type: string
            description: csv or json.
        required: [period]
      responses:
        - response: {ok: true, tool: export_records}
  handbook: &handbook
    - name: search_pages
      description: Search the handbook pages and return the matching sections with their paths.
      annotations:
        read_only_hint: true
      input_schema:
        type: object
        properties:
          query:
            type: string
            description: Free-text query.
          limit:
            type: string
            description: Maximum number of sections.
        required: [query]
      responses:
        - response: {ok: true, tool: search_pages}
    - name: read_page
      description: Return one handbook page as text.
      annotations:
        read_only_hint: true
      input_schema:
        type: object
        properties:
          path:
            type: string
            description: Path of the page.
        required: [path]
      responses:
        - response: {ok: true, tool: read_page}
    - name: list_sections
      description: List the sections of a handbook page.
      annotations:
        read_only_hint: true
      input_schema:
        type: object
        properties:
          path:
            type: string
            description: Path of the page.
        required: [path]
      responses:
        - response: {ok: true, tool: list_sections}
    - name: list_recent_changes
      description: List the pages changed within a window.
      annotations:
        read_only_hint: true
      input_schema:
        type: object
        properties:
          window:
            type: string
            description: Window to look back, for example 168h.
      responses:
        - response: {ok: true, tool: list_recent_changes}
    - name: propose_change
      description: Open a change proposal for a page with the new text.
      annotations:
        destructive_hint: true
      input_schema:
        type: object
        properties:
          path:
            type: string
            description: Path of the page.
          text:
            type: string
            description: The proposed text.
          summary:
            type: string
            description: One-line summary of the change.
        required: [path, text]
      responses:
        - response: {ok: true, tool: propose_change}
  tickets: &tickets
    - name: list_tickets
      description: List the tickets of a queue with their state and age.
      annotations:
        read_only_hint: true
      input_schema:
        type: object
        properties:
          queue:
            type: string
            description: Queue name.
          state:
            type: string
            description: Restrict to open, waiting or closed tickets.
      responses:
        - response: {ok: true, tool: list_tickets}
    - name: read_ticket
      description: Return one ticket with its comments.
      annotations:
        read_only_hint: true
      input_schema:
        type: object
        properties:
          ticket:
            type: string
            description: Ticket identifier.
        required: [ticket]
      responses:
        - response: {ok: true, tool: read_ticket}
    - name: search_tickets
      description: Search tickets by free text.
      annotations:
        read_only_hint: true
      input_schema:
        type: object
        properties:
          query:
            type: string
            description: Free-text query.
          limit:
            type: string
            description: Maximum number of tickets.
        required: [query]
      responses:
        - response: {ok: true, tool: search_tickets}
    - name: comment_ticket
      description: Add a comment to a ticket.
      annotations:
        destructive_hint: true
      input_schema:
        type: object
        properties:
          ticket:
            type: string
            description: Ticket identifier.
          text:
            type: string
            description: Comment text.
        required: [ticket, text]
      responses:
        - response: {ok: true, tool: comment_ticket}
    - name: assign_ticket
      description: Assign a ticket to a person.
      annotations:
        destructive_hint: true
      input_schema:
        type: object
        properties:
          ticket:
            type: string
            description: Ticket identifier.
          assignee:
            type: string
            description: Person to assign.
        required: [ticket, assignee]
      responses:
        - response: {ok: true, tool: assign_ticket}
    - name: close_ticket
      description: Close a ticket with a resolution.
      annotations:
        destructive_hint: true
      input_schema:
        type: object
        properties:
          ticket:
            type: string
            description: Ticket identifier.
          resolution:
            type: string
            description: Resolution text.
        required: [ticket, resolution]
      responses:
        - response: {ok: true, tool: close_ticket}
  runbook-library: &runbook-library
    - name: list_runbooks
      description: List the runbooks with their title and the alerts they cover.
      annotations:
        read_only_hint: true
      input_schema:
        type: object
        properties:
          alert:
            type: string
            description: Restrict to runbooks covering this alert.
      responses:
        - response: {ok: true, tool: list_runbooks}
    - name: read_runbook
      description: Return one runbook as text.
      annotations:
        read_only_hint: true
      input_schema:
        type: object
        properties:
          runbook:
            type: string
            description: Runbook identifier.
        required: [runbook]
      responses:
        - response: {ok: true, tool: read_runbook}
    - name: search_runbooks
      description: Search runbooks by free text.
      annotations:
        read_only_hint: true
      input_schema:
        type: object
        properties:
          query:
            type: string
            description: Free-text query.
        required: [query]
      responses:
        - response: {ok: true, tool: search_runbooks}
    - name: list_recent_runs
      description: List the recent executions of a runbook.
      annotations:
        read_only_hint: true
      input_schema:
        type: object
        properties:
          runbook:
            type: string
            description: Runbook identifier.
          window:
            type: string
            description: Window to look back.
        required: [runbook]
      responses:
        - response: {ok: true, tool: list_recent_runs}
mock_oauth_servers:
  - name: fixture-idp
    scopes:
      - openid
      - profile
      - email
      - groups
    auto_approve: true
    pkce_required: false
    token_lifetime: 1h
    client_id: fixture-client
    client_secret: fixture-secret
    use_as_muster_oauth_server: true
  - name: fleet-idp
muster_broker:
  trusted_issuers:
    - oauth_server_ref: fleet-idp
      accepted_typ_headers:
        - ""
mcp_servers:
  - name: amber-clusters
    config:
      type: streamable-http
      oauth: {required: true, trust_issuer_ref: fleet-idp, forward_token: true}
      family: {name: clusters, instanceArg: installation}
      tools: *clusters-v1
  - name: basalt-clusters
    config:
      type: streamable-http
      oauth: {required: true, trust_issuer_ref: fleet-idp, forward_token: true}
      family: {name: clusters, instanceArg: installation}
      tools: *clusters-v2
  - name: cobalt-clusters
    config:
      type: streamable-http
      oauth: {required: true, trust_issuer_ref: fleet-idp, forward_token: true}
      family: {name: clusters, instanceArg: installation}
      tools: *clusters-v3
  - name: dunite-clusters
    config:
      type: streamable-http
      oauth: {required: true, trust_issuer_ref: fleet-idp, forward_token: true}
      family: {name: clusters, instanceArg: installation}
      tools: *clusters-v4
  - name: epidote-clusters
    config:
      type: streamable-http
      oauth: {required: true, trust_issuer_ref: fleet-idp, forward_token: true}
      family: {name: clusters, instanceArg: installation}
      tools: *clusters-v1
  - name: feldspar-clusters
    config:
      type: streamable-http
      oauth: {required: true, trust_issuer_ref: fleet-idp, forward_token: true}
      family: {name: clusters, instanceArg: installation}
      tools: *clusters-v2
  - name: garnet-clusters
    config:
      type: streamable-http
      oauth: {required: true, trust_issuer_ref: fleet-idp, forward_token: true}
      family: {name: clusters, instanceArg: installation}
      tools: *clusters-v3
  - name: halite-clusters
    config:
      type: streamable-http
      oauth: {required: true, trust_issuer_ref: fleet-idp, forward_token: true}
      family: {name: clusters, instanceArg: installation}
      tools: *clusters-v4
  - name: iolite-clusters
    config:
      type: streamable-http
      oauth: {required: true, trust_issuer_ref: fleet-idp, forward_token: true}
      family: {name: clusters, instanceArg: installation}
      tools: *clusters-v1
  - name: jasper-clusters
    config:
      type: streamable-http
      oauth: {required: true, trust_issuer_ref: fleet-idp, forward_token: true}
      family: {name: clusters, instanceArg: installation}
      tools: *clusters-v2
  - name: kyanite-clusters
    config:
      type: streamable-http
      oauth: {required: true, trust_issuer_ref: fleet-idp, forward_token: true}
      family: {name: clusters, instanceArg: installation}
      tools: *clusters-v3
  - name: lazurite-clusters
    config:
      type: streamable-http
      oauth: {required: true, trust_issuer_ref: fleet-idp, forward_token: true}
      family: {name: clusters, instanceArg: installation}
      tools: *clusters-v4
  - name: marble-clusters
    config:
      type: streamable-http
      oauth: {required: true, trust_issuer_ref: fleet-idp, forward_token: true}
      family: {name: clusters, instanceArg: installation}
      tools: *clusters-v1
  - name: nephrite-clusters
    config:
      type: streamable-http
      oauth: {required: true, trust_issuer_ref: fleet-idp, forward_token: true}
      family: {name: clusters, instanceArg: installation}
      tools: *clusters-v2
  - name: onyx-clusters
    config:
      type: streamable-http
      oauth: {required: true, trust_issuer_ref: fleet-idp, forward_token: true}
      family: {name: clusters, instanceArg: installation}
      tools: *clusters-v3
  - name: pumice-clusters
    config:
      type: streamable-http
      oauth: {required: true, trust_issuer_ref: fleet-idp, forward_token: true}
      family: {name: clusters, instanceArg: installation}
      tools: *clusters-v4
  - name: quartz-clusters
    config:
      type: streamable-http
      oauth: {required: true, trust_issuer_ref: fleet-idp, forward_token: true}
      family: {name: clusters, instanceArg: installation}
      tools: *clusters-v1
  - name: rutile-clusters
    config:
      type: streamable-http
      oauth: {required: true, trust_issuer_ref: fleet-idp, forward_token: true}
      family: {name: clusters, instanceArg: installation}
      tools: *clusters-v2
  - name: sodalite-clusters
    config:
      type: streamable-http
      oauth: {required: true, trust_issuer_ref: fleet-idp, forward_token: true}
      family: {name: clusters, instanceArg: installation}
      tools: *clusters-v3
  - name: topaz-clusters
    config:
      type: streamable-http
      oauth: {required: true, trust_issuer_ref: fleet-idp, forward_token: true}
      family: {name: clusters, instanceArg: installation}
      tools: *clusters-v4
  - name: ulexite-clusters
    config:
      type: streamable-http
      oauth: {required: true, trust_issuer_ref: fleet-idp, forward_token: true}
      family: {name: clusters, instanceArg: installation}
      tools: *clusters-v1
  - name: verdite-clusters
    config:
      type: streamable-http
      oauth: {required: true, trust_issuer_ref: fleet-idp, forward_token: true}
      family: {name: clusters, instanceArg: installation}
      tools: *clusters-v2
  - name: wulfenite-clusters
    config:
      type: streamable-http
      oauth: {required: true, trust_issuer_ref: fleet-idp, forward_token: true}
      family: {name: clusters, instanceArg: installation}
      tools: *clusters-v3
  - name: xenotime-clusters
    config:
      type: streamable-http
      oauth: {required: true, trust_issuer_ref: fleet-idp, forward_token: true}
      family: {name: clusters, instanceArg: installation}
      tools: *clusters-v4
  - name: yttria-clusters
    config:
      type: streamable-http
      oauth: {required: true, trust_issuer_ref: fleet-idp, forward_token: true}
      family: {name: clusters, instanceArg: installation}
      tools: *clusters-v1
  - name: zircon-clusters
    config:
      type: streamable-http
      oauth: {required: true, trust_issuer_ref: fleet-idp, forward_token: true}
      family: {name: clusters, instanceArg: installation}
      tools: *clusters-v2
  - name: agate-clusters
    config:
      type: streamable-http
      oauth: {required: true, trust_issuer_ref: fleet-idp, forward_token: true}
      family: {name: clusters, instanceArg: installation}
      tools: *clusters-v3
  - name: beryl-clusters
    config:
      type: streamable-http
      oauth: {required: true, trust_issuer_ref: fleet-idp, forward_token: true}
      family: {name: clusters, instanceArg: installation}
      tools: *clusters-v4
  - name: amber-metrics
    config:
      type: streamable-http
      oauth: {required: true, trust_issuer_ref: fleet-idp, forward_token: true}
      family: {name: metrics, instanceArg: installation}
      tools: *metrics-v1
  - name: basalt-metrics
    config:
      type: streamable-http
      oauth: {required: true, trust_issuer_ref: fleet-idp, forward_token: true}
      family: {name: metrics, instanceArg: installation}
      tools: *metrics-v2
  - name: cobalt-metrics
    config:
      type: streamable-http
      oauth: {required: true, trust_issuer_ref: fleet-idp, forward_token: true}
      family: {name: metrics, instanceArg: installation}
      tools: *metrics-v3
  - name: dunite-metrics
    config:
      type: streamable-http
      oauth: {required: true, trust_issuer_ref: fleet-idp, forward_token: true}
      family: {name: metrics, instanceArg: installation}
      tools: *metrics-v4
  - name: epidote-metrics
    config:
      type: streamable-http
      oauth: {required: true, trust_issuer_ref: fleet-idp, forward_token: true}
      family: {name: metrics, instanceArg: installation}
      tools: *metrics-v1
  - name: feldspar-metrics
    config:
      type: streamable-http
      oauth: {required: true, trust_issuer_ref: fleet-idp, forward_token: true}
      family: {name: metrics, instanceArg: installation}
      tools: *metrics-v2
  - name: garnet-metrics
    config:
      type: streamable-http
      oauth: {required: true, trust_issuer_ref: fleet-idp, forward_token: true}
      family: {name: metrics, instanceArg: installation}
      tools: *metrics-v3
  - name: halite-metrics
    config:
      type: streamable-http
      oauth: {required: true, trust_issuer_ref: fleet-idp, forward_token: true}
      family: {name: metrics, instanceArg: installation}
      tools: *metrics-v4
  - name: iolite-metrics
    config:
      type: streamable-http
      oauth: {required: true, trust_issuer_ref: fleet-idp, forward_token: true}
      family: {name: metrics, instanceArg: installation}
      tools: *metrics-v1
  - name: jasper-metrics
    config:
      type: streamable-http
      oauth: {required: true, trust_issuer_ref: fleet-idp, forward_token: true}
      family: {name: metrics, instanceArg: installation}
      tools: *metrics-v2
  - name: kyanite-metrics
    config:
      type: streamable-http
      oauth: {required: true, trust_issuer_ref: fleet-idp, forward_token: true}
      family: {name: metrics, instanceArg: installation}
      tools: *metrics-v3
  - name: lazurite-metrics
    config:
      type: streamable-http
      oauth: {required: true, trust_issuer_ref: fleet-idp, forward_token: true}
      family: {name: metrics, instanceArg: installation}
      tools: *metrics-v4
  - name: marble-metrics
    config:
      type: streamable-http
      oauth: {required: true, trust_issuer_ref: fleet-idp, forward_token: true}
      family: {name: metrics, instanceArg: installation}
      tools: *metrics-v1
  - name: nephrite-metrics
    config:
      type: streamable-http
      oauth: {required: true, trust_issuer_ref: fleet-idp, forward_token: true}
      family: {name: metrics, instanceArg: installation}
      tools: *metrics-v2
  - name: onyx-metrics
    config:
      type: streamable-http
      oauth: {required: true, trust_issuer_ref: fleet-idp, forward_token: true}
      family: {name: metrics, instanceArg: installation}
      tools: *metrics-v3
  - name: pumice-metrics
    config:
      type: streamable-http
      oauth: {required: true, trust_issuer_ref: fleet-idp, forward_token: true}
      family: {name: metrics, instanceArg: installation}
      tools: *metrics-v4
  - name: quartz-metrics
    config:
      type: streamable-http
      oauth: {required: true, trust_issuer_ref: fleet-idp, forward_token: true}
      family: {name: metrics, instanceArg: installation}
      tools: *metrics-v1
  - name: rutile-metrics
    config:
      type: streamable-http
      oauth: {required: true, trust_issuer_ref: fleet-idp, forward_token: true}
      family: {name: metrics, instanceArg: installation}
      tools: *metrics-v2
  - name: sodalite-metrics
    config:
      type: streamable-http
      oauth: {required: true, trust_issuer_ref: fleet-idp, forward_token: true}
      family: {name: metrics, instanceArg: installation}
      tools: *metrics-v3
  - name: topaz-metrics
    config:
      type: streamable-http
      oauth: {required: true, trust_issuer_ref: fleet-idp, forward_token: true}
      family: {name: metrics, instanceArg: installation}
      tools: *metrics-v4
  - name: amber-alerts
    config:
      type: streamable-http
      oauth: {required: true, trust_issuer_ref: fleet-idp, forward_token: true}
      family: {name: alerts, instanceArg: installation}
      tools: *alerts-v1
  - name: basalt-alerts
    config:
      type: streamable-http
      oauth: {required: true, trust_issuer_ref: fleet-idp, forward_token: true}
      family: {name: alerts, instanceArg: installation}
      tools: *alerts-v2
  - name: cobalt-alerts
    config:
      type: streamable-http
      oauth: {required: true, trust_issuer_ref: fleet-idp, forward_token: true}
      family: {name: alerts, instanceArg: installation}
      tools: *alerts-v3
  - name: dunite-alerts
    config:
      type: streamable-http
      oauth: {required: true, trust_issuer_ref: fleet-idp, forward_token: true}
      family: {name: alerts, instanceArg: installation}
      tools: *alerts-v4
  - name: epidote-alerts
    config:
      type: streamable-http
      oauth: {required: true, trust_issuer_ref: fleet-idp, forward_token: true}
      family: {name: alerts, instanceArg: installation}
      tools: *alerts-v1
  - name: feldspar-alerts
    config:
      type: streamable-http
      oauth: {required: true, trust_issuer_ref: fleet-idp, forward_token: true}
      family: {name: alerts, instanceArg: installation}
      tools: *alerts-v2
  - name: garnet-alerts
    config:
      type: streamable-http
      oauth: {required: true, trust_issuer_ref: fleet-idp, forward_token: true}
      family: {name: alerts, instanceArg: installation}
      tools: *alerts-v3
  - name: halite-alerts
    config:
      type: streamable-http
      oauth: {required: true, trust_issuer_ref: fleet-idp, forward_token: true}
      family: {name: alerts, instanceArg: installation}
      tools: *alerts-v4
  - name: iolite-alerts
    config:
      type: streamable-http
      oauth: {required: true, trust_issuer_ref: fleet-idp, forward_token: true}
      family: {name: alerts, instanceArg: installation}
      tools: *alerts-v1
  - name: jasper-alerts
    config:
      type: streamable-http
      oauth: {required: true, trust_issuer_ref: fleet-idp, forward_token: true}
      family: {name: alerts, instanceArg: installation}
      tools: *alerts-v2
  - name: kyanite-alerts
    config:
      type: streamable-http
      oauth: {required: true, trust_issuer_ref: fleet-idp, forward_token: true}
      family: {name: alerts, instanceArg: installation}
      tools: *alerts-v3
  - name: lazurite-alerts
    config:
      type: streamable-http
      oauth: {required: true, trust_issuer_ref: fleet-idp, forward_token: true}
      family: {name: alerts, instanceArg: installation}
      tools: *alerts-v4
  - name: marble-alerts
    config:
      type: streamable-http
      oauth: {required: true, trust_issuer_ref: fleet-idp, forward_token: true}
      family: {name: alerts, instanceArg: installation}
      tools: *alerts-v1
  - name: nephrite-alerts
    config:
      type: streamable-http
      oauth: {required: true, trust_issuer_ref: fleet-idp, forward_token: true}
      family: {name: alerts, instanceArg: installation}
      tools: *alerts-v2
  - name: onyx-alerts
    config:
      type: streamable-http
      oauth: {required: true, trust_issuer_ref: fleet-idp, forward_token: true}
      family: {name: alerts, instanceArg: installation}
      tools: *alerts-v3
  - name: pumice-alerts
    config:
      type: streamable-http
      oauth: {required: true, trust_issuer_ref: fleet-idp, forward_token: true}
      family: {name: alerts, instanceArg: installation}
      tools: *alerts-v4
  - name: amber-logs
    config:
      type: streamable-http
      oauth: {required: true, trust_issuer_ref: fleet-idp, forward_token: true}
      family: {name: logs, instanceArg: installation}
      tools: *logs-v1
  - name: basalt-logs
    config:
      type: streamable-http
      oauth: {required: true, trust_issuer_ref: fleet-idp, forward_token: true}
      family: {name: logs, instanceArg: installation}
      tools: *logs-v2
  - name: cobalt-logs
    config:
      type: streamable-http
      oauth: {required: true, trust_issuer_ref: fleet-idp, forward_token: true}
      family: {name: logs, instanceArg: installation}
      tools: *logs-v3
  - name: dunite-logs
    config:
      type: streamable-http
      oauth: {required: true, trust_issuer_ref: fleet-idp, forward_token: true}
      family: {name: logs, instanceArg: installation}
      tools: *logs-v1
  - name: epidote-logs
    config:
      type: streamable-http
      oauth: {required: true, trust_issuer_ref: fleet-idp, forward_token: true}
      family: {name: logs, instanceArg: installation}
      tools: *logs-v2
  - name: feldspar-logs
    config:
      type: streamable-http
      oauth: {required: true, trust_issuer_ref: fleet-idp, forward_token: true}
      family: {name: logs, instanceArg: installation}
      tools: *logs-v3
  - name: garnet-logs
    config:
      type: streamable-http
      oauth: {required: true, trust_issuer_ref: fleet-idp, forward_token: true}
      family: {name: logs, instanceArg: installation}
      tools: *logs-v1
  - name: halite-logs
    config:
      type: streamable-http
      oauth: {required: true, trust_issuer_ref: fleet-idp, forward_token: true}
      family: {name: logs, instanceArg: installation}
      tools: *logs-v2
  - name: iolite-logs
    config:
      type: streamable-http
      oauth: {required: true, trust_issuer_ref: fleet-idp, forward_token: true}
      family: {name: logs, instanceArg: installation}
      tools: *logs-v3
  - name: jasper-logs
    config:
      type: streamable-http
      oauth: {required: true, trust_issuer_ref: fleet-idp, forward_token: true}
      family: {name: logs, instanceArg: installation}
      tools: *logs-v1
  - name: kyanite-logs
    config:
      type: streamable-http
      oauth: {required: true, trust_issuer_ref: fleet-idp, forward_token: true}
      family: {name: logs, instanceArg: installation}
      tools: *logs-v2
  - name: lazurite-logs
    config:
      type: streamable-http
      oauth: {required: true, trust_issuer_ref: fleet-idp, forward_token: true}
      family: {name: logs, instanceArg: installation}
      tools: *logs-v3
  - name: amber-ledger
    config:
      type: streamable-http
      oauth: {required: true, trust_issuer_ref: fleet-idp, forward_token: true}
      family: {name: ledger, instanceArg: installation}
      tools: *ledger-v1
  - name: basalt-ledger
    config:
      type: streamable-http
      oauth: {required: true, trust_issuer_ref: fleet-idp, forward_token: true}
      family: {name: ledger, instanceArg: installation}
      tools: *ledger-v2
  - name: cobalt-ledger
    config:
      type: streamable-http
      oauth: {required: true, trust_issuer_ref: fleet-idp, forward_token: true}
      family: {name: ledger, instanceArg: installation}
      tools: *ledger-v3
  - name: dunite-ledger
    config:
      type: streamable-http
      oauth: {required: true, trust_issuer_ref: fleet-idp, forward_token: true}
      family: {name: ledger, instanceArg: installation}
      tools: *ledger-v1
  - name: epidote-ledger
    config:
      type: streamable-http
      oauth: {required: true, trust_issuer_ref: fleet-idp, forward_token: true}
      family: {name: ledger, instanceArg: installation}
      tools: *ledger-v2
  - name: feldspar-ledger
    config:
      type: streamable-http
      oauth: {required: true, trust_issuer_ref: fleet-idp, forward_token: true}
      family: {name: ledger, instanceArg: installation}
      tools: *ledger-v3
  - name: garnet-ledger
    config:
      type: streamable-http
      oauth: {required: true, trust_issuer_ref: fleet-idp, forward_token: true}
      family: {name: ledger, instanceArg: installation}
      tools: *ledger-v1
  - name: halite-ledger
    config:
      type: streamable-http
      oauth: {required: true, trust_issuer_ref: fleet-idp, forward_token: true}
      family: {name: ledger, instanceArg: installation}
      tools: *ledger-v2
  - name: handbook
    config:
      type: streamable-http
      tools: *handbook
  - name: tickets
    config:
      type: streamable-http
      tools: *tickets
  - name: runbook-library
    config:
      type: streamable-http
      tools: *runbook-library
workflows:
  - name: audit-alert-noise-across-organizations
    labels: {fixture.invalid/tier: gold, fixture.invalid/verb: audit}
    config:
      name: audit-alert-noise-across-organizations
      description: 'Runbook: Audit alert noise across organizations.'
      args:
        installation: {type: string, required: true, description: The installation to run against.}
        window: {type: string, required: false, description: 'How far to look back, for example 24h.'}
      steps:
        - id: step-1
          tool: x_logs_detect_patterns
          args: {installation: '{{ .input.installation }}', window: '{{ .input.window }}'}
          store: true
        - id: step-2
          tool: x_tickets_list_tickets
          args: {queue: audit alert noise}
          store: true
  - name: audit-alert-noise-for-the-week
    labels: {fixture.invalid/verb: audit}
    config:
      name: audit-alert-noise-for-the-week
      description: 'Runbook: Audit alert noise for the week.'
      args:
        installation: {type: string, required: true, description: The installation to run against.}
        window: {type: string, required: false, description: 'How far to look back, for example 24h.'}
      steps:
        - id: step-1
          tool: x_logs_query_logs
          args: {installation: '{{ .input.installation }}', window: '{{ .input.window }}'}
          store: true
        - id: step-2
          tool: x_metrics_query_instant
          args: {installation: '{{ .input.installation }}'}
          store: true
        - id: step-3
          tool: x_ledger_list_usage
          args: {installation: '{{ .input.installation }}', window: '{{ .input.window }}'}
          store: true
  - name: audit-alert-noise-per-installation
    labels: {fixture.invalid/verb: audit}
    config:
      name: audit-alert-noise-per-installation
      description: 'Runbook: Audit alert noise per installation.'
      args:
        installation: {type: string, required: true, description: The installation to run against.}
        window: {type: string, required: false, description: 'How far to look back, for example 24h.'}
      steps:
        - id: step-1
          tool: x_clusters_get_release
          args: {installation: '{{ .input.installation }}'}
          store: true
        - id: step-2
          tool: x_clusters_get_quota
          args: {installation: '{{ .input.installation }}'}
          store: true
        - id: step-3
          tool: x_logs_count_lines
          args: {installation: '{{ .input.installation }}', window: '{{ .input.window }}'}
          store: true
        - id: step-4
          tool: x_metrics_explain_series
          args: {installation: '{{ .input.installation }}'}
          store: true
  - name: audit-certificate-expiry-before-upgrade
    labels: {fixture.invalid/tier: gold, fixture.invalid/verb: audit}
    config:
      name: audit-certificate-expiry-before-upgrade
      description: 'Runbook: Audit certificate expiry before upgrade.'
      args:
        installation: {type: string, required: true, description: The installation to run against.}
        window: {type: string, required: false, description: 'How far to look back, for example 24h.'}
      steps:
        - id: step-1
          tool: x_logs_list_streams
          args: {installation: '{{ .input.installation }}'}
          store: true
  - name: audit-certificate-expiry-on-request
    labels: {fixture.invalid/verb: audit}
    config:
      name: audit-certificate-expiry-on-request
      description: 'Runbook: Audit certificate expiry on request.'
      args:
        installation: {type: string, required: true, description: The installation to run against.}
        window: {type: string, required: false, description: 'How far to look back, for example 24h.'}
      steps:
        - id: step-1
          tool: x_metrics_list_label_values
          args: {installation: '{{ .input.installation }}'}
          store: true
  - name: audit-ingress-errors-after-incident
    labels: {fixture.invalid/verb: audit}
    config:
      name: audit-ingress-errors-after-incident
      description: 'Runbook: Audit ingress errors after incident.'
      args:
        installation: {type: string, required: true, description: The installation to run against.}
        window: {type: string, required: false, description: 'How far to look back, for example 24h.'}
      steps:
        - id: step-1
          tool: x_ledger_get_statement
          args: {installation: '{{ .input.installation }}'}
          store: true
        - id: step-2
          tool: x_alerts_list_alerts
          args: {installation: '{{ .input.installation }}'}
          store: true
        - id: step-3
          tool: x_ledger_get_statement
          args: {installation: '{{ .input.installation }}'}
          store: true
  - name: audit-log-volume-after-incident
    labels: {fixture.invalid/tier: gold, fixture.invalid/verb: audit}
    config:
      name: audit-log-volume-after-incident
      description: 'Runbook: Audit log volume after incident.'
      args:
        installation: {type: string, required: true, description: The installation to run against.}
        window: {type: string, required: false, description: 'How far to look back, for example 24h.'}
      steps:
        - id: step-1
          tool: x_clusters_get_quota
          args: {installation: '{{ .input.installation }}'}
          store: true
        - id: step-2
          tool: x_ledger_list_usage
          args: {installation: '{{ .input.installation }}', window: '{{ .input.window }}'}
          store: true
        - id: step-3
          tool: x_clusters_list_events
          args: {installation: '{{ .input.installation }}', window: '{{ .input.window }}'}
          store: true
  - name: audit-log-volume-before-upgrade
    labels: {fixture.invalid/verb: audit}
    config:
      name: audit-log-volume-before-upgrade
      description: 'Runbook: Audit log volume before upgrade.'
      args:
        installation: {type: string, required: true, description: The installation to run against.}
        window: {type: string, required: false, description: 'How far to look back, for example 24h.'}
      steps:
        - id: step-1
          tool: x_alerts_list_silences
          args: {installation: '{{ .input.installation }}'}
          store: true
        - id: step-2
          tool: x_ledger_get_statement
          args: {installation: '{{ .input.installation }}'}
          store: true
        - id: step-3
          tool: x_metrics_query_range
          args: {installation: '{{ .input.installation }}', window: '{{ .input.window }}'}
          store: true
        - id: step-4
          tool: x_alerts_get_alert
          args: {installation: '{{ .input.installation }}'}
          store: true
        - id: step-5
          tool: x_metrics_query_instant
          args: {installation: '{{ .input.installation }}'}
          store: true
  - name: audit-log-volume-per-installation
    labels: {fixture.invalid/verb: audit}
    config:
      name: audit-log-volume-per-installation
      description: 'Runbook: Audit log volume per installation.'
      args:
        installation: {type: string, required: true, description: The installation to run against.}
        window: {type: string, required: false, description: 'How far to look back, for example 24h.'}
      steps:
        - id: step-1
          tool: x_logs_detect_patterns
          args: {installation: '{{ .input.installation }}', window: '{{ .input.window }}'}
          store: true
  - name: audit-node-pool-capacity-per-installation
    labels: {fixture.invalid/tier: gold, fixture.invalid/verb: audit}
    config:
      name: audit-node-pool-capacity-per-installation
      description: 'Runbook: Audit node pool capacity per installation.'
      args:
        installation: {type: string, required: true, description: The installation to run against.}
        window: {type: string, required: false, description: 'How far to look back, for example 24h.'}
      steps:
        - id: step-1
          tool: x_clusters_list_upgrades
          args: {installation: '{{ .input.installation }}', window: '{{ .input.window }}'}
          store: true
        - id: step-2
          tool: x_clusters_list_clusters
          args: {installation: '{{ .input.installation }}'}
          store: true
        - id: step-3
          tool: x_ledger_list_adjustments
          args: {installation: '{{ .input.installation }}'}
          store: true
  - name: audit-release-drift-after-incident
    labels: {fixture.invalid/verb: audit}
    config:
      name: audit-release-drift-after-incident
      description: 'Runbook: Audit release drift after incident.'
      args:
        installation: {type: string, required: true, description: The installation to run against.}
        window: {type: string, required: false, description: 'How far to look back, for example 24h.'}
      steps:
        - id: step-1
          tool: x_metrics_list_rules
          args: {installation: '{{ .input.installation }}'}
          store: true
        - id: step-2
          tool: x_metrics_query_range
          args: {installation: '{{ .input.installation }}', window: '{{ .input.window }}'}
          store: true
        - id: step-3
          tool: x_logs_count_lines
          args: {installation: '{{ .input.installation }}', window: '{{ .input.window }}'}
          store: true
        - id: step-4
          tool: x_clusters_list_node_pools
          args: {installation: '{{ .input.installation }}'}
          store: true
  - name: audit-retention-gaps-across-organizations
    labels: {fixture.invalid/verb: audit}
    config:
      name: audit-retention-gaps-across-organizations
      description: 'Runbook: Audit retention gaps across organizations.'
      args:
        installation: {type: string, required: true, description: The installation to run against.}
        window: {type: string, required: false, description: 'How far to look back, for example 24h.'}
      steps:
        - id: step-1
          tool: x_alerts_get_alert
          args: {installation: '{{ .input.installation }}'}
          store: true
        - id: step-2
          tool: x_ledger_list_usage
          args: {installation: '{{ .input.installation }}', window: '{{ .input.window }}'}
          store: true
        - id: step-3
          tool: x_ledger_export_records
          args: {installation: '{{ .input.installation }}'}
          store: true
        - id: step-4
          tool: x_clusters_list_upgrades
          args: {installation: '{{ .input.installation }}', window: '{{ .input.window }}'}
          store: true
        - id: step-5
          tool: x_metrics_query_instant
          args: {installation: '{{ .input.installation }}'}
          store: true
  - name: audit-retention-gaps-per-installation
    labels: {fixture.invalid/tier: gold, fixture.invalid/verb: audit}
    config:
      name: audit-retention-gaps-per-installation
      description: 'Runbook: Audit retention gaps per installation.'
      args:
        installation: {type: string, required: true, description: The installation to run against.}
        window: {type: string, required: false, description: 'How far to look back, for example 24h.'}
      steps:
        - id: step-1
          tool: x_clusters_get_cluster
          args: {installation: '{{ .input.installation }}'}
          store: true
        - id: step-2
          tool: x_alerts_get_alert
          args: {installation: '{{ .input.installation }}'}
          store: true
        - id: step-3
          tool: x_runbook-library_list_runbooks
          args: {alert: audit retention gaps}
          store: true
  - name: audit-scrape-health-after-incident
    labels: {fixture.invalid/verb: audit}
    config:
      name: audit-scrape-health-after-incident
      description: 'Runbook: Audit scrape health after incident.'
      args:
        installation: {type: string, required: true, description: The installation to run against.}
        window: {type: string, required: false, description: 'How far to look back, for example 24h.'}
      steps:
        - id: step-1
          tool: x_metrics_list_targets
          args: {installation: '{{ .input.installation }}'}
          store: true
  - name: audit-scrape-health-before-upgrade
    labels: {fixture.invalid/verb: audit}
    config:
      name: audit-scrape-health-before-upgrade
      description: 'Runbook: Audit scrape health before upgrade.'
      args:
        installation: {type: string, required: true, description: The installation to run against.}
        window: {type: string, required: false, description: 'How far to look back, for example 24h.'}
      steps:
        - id: step-1
          tool: x_logs_count_lines
          args: {installation: '{{ .input.installation }}', window: '{{ .input.window }}'}
          store: true
        - id: step-2
          tool: x_metrics_list_targets
          args: {installation: '{{ .input.installation }}'}
          store: true
        - id: step-3
          tool: x_metrics_explain_series
          args: {installation: '{{ .input.installation }}'}
          store: true
  - name: audit-scrape-health-per-installation
    labels: {fixture.invalid/tier: gold, fixture.invalid/verb: audit}
    config:
      name: audit-scrape-health-per-installation
      description: 'Runbook: Audit scrape health per installation.'
      args:
        installation: {type: string, required: true, description: The installation to run against.}
        window: {type: string, required: false, description: 'How far to look back, for example 24h.'}
      steps:
        - id: step-1
          tool: x_clusters_get_cluster
          args: {installation: '{{ .input.installation }}'}
          store: true
        - id: step-2
          tool: x_handbook_read_page
          args: {path: audit scrape health}
          store: true
        - id: step-3
          tool: x_logs_list_streams
          args: {installation: '{{ .input.installation }}'}
          store: true
        - id: step-4
          tool: x_alerts_list_alerts
          args: {installation: '{{ .input.installation }}'}
          store: true
        - id: step-5
          tool: x_tickets_read_ticket
          args: {ticket: audit scrape health}
          store: true
  - name: audit-silence-hygiene-across-organizations
    labels: {fixture.invalid/verb: audit}
    config:
      name: audit-silence-hygiene-across-organizations
      description: 'Runbook: Audit silence hygiene across organizations.'
      args:
        installation: {type: string, required: true, description: The installation to run against.}
        window: {type: string, required: false, description: 'How far to look back, for example 24h.'}
      steps:
        - id: step-1
          tool: x_logs_count_lines
          args: {installation: '{{ .input.installation }}', window: '{{ .input.window }}'}
          store: true
        - id: step-2
          tool: x_alerts_list_routes
          args: {installation: '{{ .input.installation }}'}
          store: true
        - id: step-3
          tool: x_alerts_list_routes
          args: {installation: '{{ .input.installation }}'}
          store: true
        - id: step-4
          tool: x_metrics_list_rules
          args: {installation: '{{ .input.installation }}'}
          store: true
  - name: audit-silence-hygiene-after-incident
    labels: {fixture.invalid/verb: audit}
    config:
      name: audit-silence-hygiene-after-incident
      description: 'Runbook: Audit silence hygiene after incident.'
      args:
        installation: {type: string, required: true, description: The installation to run against.}
        window: {type: string, required: false, description: 'How far to look back, for example 24h.'}
      steps:
        - id: step-1
          tool: x_alerts_list_routes
          args: {installation: '{{ .input.installation }}'}
          store: true
  - name: audit-silence-hygiene-for-the-week
    labels: {fixture.invalid/tier: gold, fixture.invalid/verb: audit}
    config:
      name: audit-silence-hygiene-for-the-week
      description: 'Runbook: Audit silence hygiene for the week.'
      args:
        installation: {type: string, required: true, description: The installation to run against.}
        window: {type: string, required: false, description: 'How far to look back, for example 24h.'}
      steps:
        - id: step-1
          tool: x_alerts_list_alerts
          args: {installation: '{{ .input.installation }}'}
          store: true
        - id: step-2
          tool: x_handbook_list_recent_changes
          args: {window: audit silence hygiene}
          store: true
  - name: audit-upgrade-readiness-across-organizations
    labels: {fixture.invalid/verb: audit}
    config:
      name: audit-upgrade-readiness-across-organizations
      description: 'Runbook: Audit upgrade readiness across organizations.'
      args:
        installation: {type: string, required: true, description: The installation to run against.}
        window: {type: string, required: false, description: 'How far to look back, for example 24h.'}
      steps:
        - id: step-1
          tool: x_metrics_list_label_values
          args: {installation: '{{ .input.installation }}'}
          store: true
        - id: step-2
          tool: x_metrics_list_label_values
          args: {installation: '{{ .input.installation }}'}
          store: true
        - id: step-3
          tool: x_clusters_list_node_pools
          args: {installation: '{{ .input.installation }}'}
          store: true
        - id: step-4
          tool: x_clusters_list_node_pools
          args: {installation: '{{ .input.installation }}'}
          store: true
  - name: audit-usage-anomalies-before-upgrade
    labels: {fixture.invalid/verb: audit}
    config:
      name: audit-usage-anomalies-before-upgrade
      description: 'Runbook: Audit usage anomalies before upgrade.'
      args:
        installation: {type: string, required: true, description: The installation to run against.}
        window: {type: string, required: false, description: 'How far to look back, for example 24h.'}
      steps:
        - id: step-1
          tool: x_logs_list_streams
          args: {installation: '{{ .input.installation }}'}
          store: true
  - name: audit-usage-anomalies-on-request
    labels: {fixture.invalid/tier: gold, fixture.invalid/verb: audit}
    config:
      name: audit-usage-anomalies-on-request
      description: 'Runbook: Audit usage anomalies on request.'
      args:
        installation: {type: string, required: true, description: The installation to run against.}
        window: {type: string, required: false, description: 'How far to look back, for example 24h.'}
      steps:
        - id: step-1
          tool: x_alerts_list_routes
          args: {installation: '{{ .input.installation }}'}
          store: true
        - id: step-2
          tool: x_alerts_list_alerts
          args: {installation: '{{ .input.installation }}'}
          store: true
        - id: step-3
          tool: x_metrics_list_label_values
          args: {installation: '{{ .input.installation }}'}
          store: true
        - id: step-4
          tool: x_alerts_get_alert
          args: {installation: '{{ .input.installation }}'}
          store: true
        - id: step-5
          tool: x_handbook_read_page
          args: {path: audit usage anomalies}
          store: true
  - name: check-alert-noise-after-incident
    labels: {fixture.invalid/verb: check}
    config:
      name: check-alert-noise-after-incident
      description: 'Runbook: Check alert noise after incident.'
      args:
        installation: {type: string, required: true, description: The installation to run against.}
        window: {type: string, required: false, description: 'How far to look back, for example 24h.'}
      steps:
        - id: step-1
          tool: x_alerts_get_alert
          args: {installation: '{{ .input.installation }}'}
          store: true
        - id: step-2
          tool: x_clusters_list_events
          args: {installation: '{{ .input.installation }}', window: '{{ .input.window }}'}
          store: true
        - id: step-3
          tool: x_runbook-library_list_runbooks
          args: {alert: check alert noise}
          store: true
        - id: step-4
          tool: x_metrics_list_label_values
          args: {installation: '{{ .input.installation }}'}
          store: true
        - id: step-5
          tool: x_runbook-library_list_runbooks
          args: {alert: check alert noise}
          store: true
  - name: check-alert-noise-per-installation
    labels: {fixture.invalid/verb: check}
    config:
      name: check-alert-noise-per-installation
      description: 'Runbook: Check alert noise per installation.'
      args:
        installation: {type: string, required: true, description: The installation to run against.}
        window: {type: string, required: false, description: 'How far to look back, for example 24h.'}
      steps:
        - id: step-1
          tool: x_logs_detect_patterns
          args: {installation: '{{ .input.installation }}', window: '{{ .input.window }}'}
          store: true
  - name: check-certificate-expiry-for-the-week
    labels: {fixture.invalid/tier: gold, fixture.invalid/verb: check}
    config:
      name: check-certificate-expiry-for-the-week
      description: 'Runbook: Check certificate expiry for the week.'
      args:
        installation: {type: string, required: true, description: The installation to run against.}
        window: {type: string, required: false, description: 'How far to look back, for example 24h.'}
      steps:
        - id: step-1
          tool: x_logs_tail_stream
          args: {installation: '{{ .input.installation }}'}
          store: true
        - id: step-2
          tool: x_clusters_list_node_pools
          args: {installation: '{{ .input.installation }}'}
          store: true
        - id: step-3
          tool: x_clusters_list_clusters
          args: {installation: '{{ .input.installation }}'}
          store: true
        - id: step-4
          tool: x_runbook-library_search_runbooks
          args: {query: check certificate expiry}
          store: true
  - name: check-certificate-expiry-on-request
    labels: {fixture.invalid/verb: check}
    config:
      name: check-certificate-expiry-on-request
      description: 'Runbook: Check certificate expiry on request.'
      args:
        installation: {type: string, required: true, description: The installation to run against.}
        window: {type: string, required: false, description: 'How far to look back, for example 24h.'}
      steps:
        - id: step-1
          tool: x_metrics_query_instant
          args: {installation: '{{ .input.installation }}'}
          store: true
  - name: check-ingress-errors-after-incident
    labels: {fixture.invalid/verb: check}
    config:
      name: check-ingress-errors-after-incident
      description: 'Runbook: Check ingress errors after incident.'
      args:
        installation: {type: string, required: true, description: The installation to run against.}
        window: {type: string, required: false, description: 'How far to look back, for example 24h.'}
      steps:
        - id: step-1
          tool: x_clusters_get_release
          args: {installation: '{{ .input.installation }}'}
          store: true
        - id: step-2
          tool: x_clusters_get_release
          args: {installation: '{{ .input.installation }}'}
          store: true
        - id: step-3
          tool: x_ledger_get_statement
          args: {installation: '{{ .input.installation }}'}
          store: true
        - id: step-4
          tool: x_metrics_list_rules
          args: {installation: '{{ .input.installation }}'}
          store: true
  - name: check-ingress-errors-on-request
    labels: {fixture.invalid/tier: gold, fixture.invalid/verb: check}
    config:
      name: check-ingress-errors-on-request
      description: 'Runbook: Check ingress errors on request.'
      args:
        installation: {type: string, required: true, description: The installation to run against.}
        window: {type: string, required: false, description: 'How far to look back, for example 24h.'}
      steps:
        - id: step-1
          tool: x_clusters_list_upgrades
          args: {installation: '{{ .input.installation }}', window: '{{ .input.window }}'}
          store: true
        - id: step-2
          tool: x_logs_list_streams
          args: {installation: '{{ .input.installation }}'}
          store: true
        - id: step-3
          tool: x_runbook-library_read_runbook
          args: {runbook: check ingress errors}
          store: true
        - id: step-4
          tool: x_metrics_query_instant
          args: {installation: '{{ .input.installation }}'}
          store: true
  - name: check-log-volume-across-organizations
    labels: {fixture.invalid/verb: check}
    config:
      name: check-log-volume-across-organizations
      description: 'Runbook: Check log volume across organizations.'
      args:
        installation: {type: string, required: true, description: The installation to run against.}
        window: {type: string, required: false, description: 'How far to look back, for example 24h.'}
      steps:
        - id: step-1
          tool: x_logs_tail_stream
          args: {installation: '{{ .input.installation }}'}
          store: true
  - name: check-node-pool-capacity-per-installation
    labels: {fixture.invalid/verb: check}
    config:
      name: check-node-pool-capacity-per-installation
      description: 'Runbook: Check node pool capacity per installation.'
      args:
        installation: {type: string, required: true, description: The installation to run against.}
        window: {type: string, required: false, description: 'How far to look back, for example 24h.'}
      steps:
        - id: step-1
          tool: x_logs_count_lines
          args: {installation: '{{ .input.installation }}', window: '{{ .input.window }}'}
          store: true
        - id: step-2
          tool: x_metrics_explain_series
          args: {installation: '{{ .input.installation }}'}
          store: true
        - id: step-3
          tool: x_logs_count_lines
          args: {installation: '{{ .input.installation }}', window: '{{ .input.window }}'}
          store: true
        - id: step-4
          tool: x_runbook-library_search_runbooks
          args: {query: check node pool capacity}
          store: true
        - id: step-5
          tool: x_metrics_list_rules
          args: {installation: '{{ .input.installation }}'}
          store: true
  - name: check-quota-headroom-for-the-week
    labels: {fixture.invalid/tier: gold, fixture.invalid/verb: check}
    config:
      name: check-quota-headroom-for-the-week
      description: 'Runbook: Check quota headroom for the week.'
      args:
        installation: {type: string, required: true, description: The installation to run against.}
        window: {type: string, required: false, description: 'How far to look back, for example 24h.'}
      steps:
        - id: step-1
          tool: x_clusters_list_events
          args: {installation: '{{ .input.installation }}', window: '{{ .input.window }}'}
          store: true
  - name: check-quota-headroom-on-request
    labels: {fixture.invalid/verb: check}
    config:
      name: check-quota-headroom-on-request
      description: 'Runbook: Check quota headroom on request.'
      args:
        installation: {type: string, required: true, description: The installation to run against.}
        window: {type: string, required: false, description: 'How far to look back, for example 24h.'}
      steps:
        - id: step-1
          tool: x_alerts_list_alerts
          args: {installation: '{{ .input.installation }}'}
          store: true
        - id: step-2
          tool: x_alerts_list_alerts
          args: {installation: '{{ .input.installation }}'}
          store: true
        - id: step-3
          tool: x_metrics_list_rules
          args: {installation: '{{ .input.installation }}'}
          store: true
  - name: check-release-drift-before-upgrade
    labels: {fixture.invalid/verb: check}
    config:
      name: check-release-drift-before-upgrade
      description: 'Runbook: Check release drift before upgrade.'
      args:
        installation: {type: string, required: true, description: The installation to run against.}
        window: {type: string, required: false, description: 'How far to look back, for example 24h.'}
      steps:
        - id: step-1
          tool: x_metrics_list_series
          args: {installation: '{{ .input.installation }}'}
          store: true
        - id: step-2
          tool: x_tickets_read_ticket
          args: {ticket: check release drift}
          store: true
  - name: check-release-drift-per-installation
    labels: {fixture.invalid/tier: gold, fixture.invalid/verb: check}
    config:
      name: check-release-drift-per-installation
      description: 'Runbook: Check release drift per installation.'
      args:
        installation: {type: string, required: true, description: The installation to run against.}
        window: {type: string, required: false, description: 'How far to look back, for example 24h.'}
      steps:
        - id: step-1
          tool: x_metrics_list_label_values
          args: {installation: '{{ .input.installation }}'}
          store: true
        - id: step-2
          tool: x_metrics_list_label_values
          args: {installation: '{{ .input.installation }}'}
          store: true
  - name: check-retention-gaps-before-upgrade
    labels: {fixture.invalid/verb: check}
    config:
      name: check-retention-gaps-before-upgrade
      description: 'Runbook: Check retention gaps before upgrade.'
      args:
        installation: {type: string, required: true, description: The installation to run against.}
        window: {type: string, required: false, description: 'How far to look back, for example 24h.'}
      steps:
        - id: step-1
          tool: x_clusters_list_node_pools
          args: {installation: '{{ .input.installation }}'}
          store: true
  - name: check-scrape-health-after-incident
    labels: {fixture.invalid/verb: check}
    config:
      name: check-scrape-health-after-incident
      description: 'Runbook: Check scrape health after incident.'
      args:
        installation: {type: string, required: true, description: The installation to run against.}
        window: {type: string, required: false, description: 'How far to look back, for example 24h.'}
      steps:
        - id: step-1
          tool: x_metrics_list_series
          args: {installation: '{{ .input.installation }}'}
          store: true
  - name: check-silence-hygiene-on-request
    labels: {fixture.invalid/tier: gold, fixture.invalid/verb: check}
    config:
      name: check-silence-hygiene-on-request
      description: 'Runbook: Check silence hygiene on request.'
      args:
        installation: {type: string, required: true, description: The installation to run against.}
        window: {type: string, required: false, description: 'How far to look back, for example 24h.'}
      steps:
        - id: step-1
          tool: x_clusters_list_clusters
          args: {installation: '{{ .input.installation }}'}
          store: true
        - id: step-2
          tool: x_clusters_list_clusters
          args: {installation: '{{ .input.installation }}'}
          store: true
        - id: step-3
          tool: x_runbook-library_list_runbooks
          args: {alert: check silence hygiene}
          store: true
  - name: check-silence-hygiene-per-installation
    labels: {fixture.invalid/verb: check}
    config:
      name: check-silence-hygiene-per-installation
      description: 'Runbook: Check silence hygiene per installation.'
      args:
        installation: {type: string, required: true, description: The installation to run against.}
        window: {type: string, required: false, description: 'How far to look back, for example 24h.'}
      steps:
        - id: step-1
          tool: x_logs_count_lines
          args: {installation: '{{ .input.installation }}', window: '{{ .input.window }}'}
          store: true
        - id: step-2
          tool: x_alerts_list_alerts
          args: {installation: '{{ .input.installation }}'}
          store: true
        - id: step-3
          tool: x_logs_tail_stream
          args: {installation: '{{ .input.installation }}'}
          store: true
        - id: step-4
          tool: x_metrics_list_label_values
          args: {installation: '{{ .input.installation }}'}
          store: true
  - name: check-upgrade-readiness-for-the-week
    labels: {fixture.invalid/verb: check}
    config:
      name: check-upgrade-readiness-for-the-week
      description: 'Runbook: Check upgrade readiness for the week.'
      args:
        installation: {type: string, required: true, description: The installation to run against.}
        window: {type: string, required: false, description: 'How far to look back, for example 24h.'}
      steps:
        - id: step-1
          tool: x_metrics_query_instant
          args: {installation: '{{ .input.installation }}'}
          store: true
        - id: step-2
          tool: x_clusters_list_node_pools
          args: {installation: '{{ .input.installation }}'}
          store: true
  - name: check-usage-anomalies-for-the-week
    labels: {fixture.invalid/tier: gold, fixture.invalid/verb: check}
    config:
      name: check-usage-anomalies-for-the-week
      description: 'Runbook: Check usage anomalies for the week.'
      args:
        installation: {type: string, required: true, description: The installation to run against.}
        window: {type: string, required: false, description: 'How far to look back, for example 24h.'}
      steps:
        - id: step-1
          tool: x_logs_get_retention
          args: {installation: '{{ .input.installation }}'}
          store: true
  - name: check-usage-anomalies-per-installation
    labels: {fixture.invalid/verb: check}
    config:
      name: check-usage-anomalies-per-installation
      description: 'Runbook: Check usage anomalies per installation.'
      args:
        installation: {type: string, required: true, description: The installation to run against.}
        window: {type: string, required: false, description: 'How far to look back, for example 24h.'}
      steps:
        - id: step-1
          tool: x_metrics_list_targets
          args: {installation: '{{ .input.installation }}'}
          store: true
        - id: step-2
          tool: x_runbook-library_search_runbooks
          args: {query: check usage anomalies}
          store: true
        - id: step-3
          tool: x_clusters_get_release
          args: {installation: '{{ .input.installation }}'}
          store: true
        - id: step-4
          tool: x_tickets_list_tickets
          args: {queue: check usage anomalies}
          store: true
        - id: step-5
          tool: x_clusters_get_release
          args: {installation: '{{ .input.installation }}'}
          store: true
  - name: compare-alert-noise-after-incident
    labels: {fixture.invalid/verb: compare}
    config:
      name: compare-alert-noise-after-incident
      description: 'Runbook: Compare alert noise after incident.'
      args:
        installation: {type: string, required: true, description: The installation to run against.}
        window: {type: string, required: false, description: 'How far to look back, for example 24h.'}
      steps:
        - id: step-1
          tool: x_alerts_list_alerts
          args: {installation: '{{ .input.installation }}'}
          store: true
        - id: step-2
          tool: x_ledger_export_records
          args: {installation: '{{ .input.installation }}'}
          store: true
        - id: step-3
          tool: x_handbook_search_pages
          args: {query: compare alert noise}
          store: true
  - name: compare-certificate-expiry-across-organizations
    labels: {fixture.invalid/tier: gold, fixture.invalid/verb: compare}
    config:
      name: compare-certificate-expiry-across-organizations
      description: 'Runbook: Compare certificate expiry across organizations.'
      args:
        installation: {type: string, required: true, description: The installation to run against.}
        window: {type: string, required: false, description: 'How far to look back, for example 24h.'}
      steps:
        - id: step-1
          tool: x_metrics_query_range
          args: {installation: '{{ .input.installation }}', window: '{{ .input.window }}'}
          store: true
        - id: step-2
          tool: x_runbook-library_list_runbooks
          args: {alert: compare certificate expiry}
          store: true
        - id: step-3
          tool: x_logs_query_logs
          args: {installation: '{{ .input.installation }}', window: '{{ .input.window }}'}
          store: true
        - id: step-4
          tool: x_tickets_list_tickets
          args: {queue: compare certificate expiry}
          store: true
        - id: step-5
          tool: x_ledger_export_records
          args: {installation: '{{ .input.installation }}'}
          store: true
  - name: compare-certificate-expiry-before-upgrade
    labels: {fixture.invalid/verb: compare}
    config:
      name: compare-certificate-expiry-before-upgrade
      description: 'Runbook: Compare certificate expiry before upgrade.'
      args:
        installation: {type: string, required: true, description: The installation to run against.}
        window: {type: string, required: false, description: 'How far to look back, for example 24h.'}
      steps:
        - id: step-1
          tool: x_logs_query_logs
          args: {installation: '{{ .input.installation }}', window: '{{ .input.window }}'}
          store: true
        - id: step-2
          tool: x_ledger_get_statement
          args: {installation: '{{ .input.installation }}'}
          store: true
        - id: step-3
          tool: x_logs_list_streams
          args: {installation: '{{ .input.installation }}'}
          store: true
        - id: step-4
          tool: x_metrics_list_series
          args: {installation: '{{ .input.installation }}'}
          store: true
  - name: compare-certificate-expiry-for-the-week
    labels: {fixture.invalid/verb: compare}
    config:
      name: compare-certificate-expiry-for-the-week
      description: 'Runbook: Compare certificate expiry for the week.'
      args:
        installation: {type: string, required: true, description: The installation to run against.}
        window: {type: string, required: false, description: 'How far to look back, for example 24h.'}
      steps:
        - id: step-1
          tool: x_metrics_list_targets
          args: {installation: '{{ .input.installation }}'}
          store: true
        - id: step-2
          tool: x_metrics_explain_series
          args: {installation: '{{ .input.installation }}'}
          store: true
        - id: step-3
          tool: x_clusters_get_quota
          args: {installation: '{{ .input.installation }}'}
          store: true
        - id: step-4
          tool: x_ledger_get_statement
          args: {installation: '{{ .input.installation }}'}
          store: true
  - name: compare-ingress-errors-across-organizations
    labels: {fixture.invalid/tier: gold, fixture.invalid/verb: compare}
    config:
      name: compare-ingress-errors-across-organizations
      description: 'Runbook: Compare ingress errors across organizations.'
      args:
        installation: {type: string, required: true, description: The installation to run against.}
        window: {type: string, required: false, description: 'How far to look back, for example 24h.'}
      steps:
        - id: step-1
          tool: x_clusters_list_node_pools
          args: {installation: '{{ .input.installation }}'}
          store: true
        - id: step-2
          tool: x_logs_detect_patterns
          args: {installation: '{{ .input.installation }}', window: '{{ .input.window }}'}
          store: true
        - id: step-3
          tool: x_metrics_list_series
          args: {installation: '{{ .input.installation }}'}
          store: true
        - id: step-4
          tool: x_metrics_list_rules
          args: {installation: '{{ .input.installation }}'}
          store: true
  - name: compare-ingress-errors-after-incident
    labels: {fixture.invalid/verb: compare}
    config:
      name: compare-ingress-errors-after-incident
      description: 'Runbook: Compare ingress errors after incident.'
      args:
        installation: {type: string, required: true, description: The installation to run against.}
        window: {type: string, required: false, description: 'How far to look back, for example 24h.'}
      steps:
        - id: step-1
          tool: x_clusters_list_upgrades
          args: {installation: '{{ .input.installation }}', window: '{{ .input.window }}'}
          store: true
        - id: step-2
          tool: x_tickets_read_ticket
          args: {ticket: compare ingress errors}
          store: true
        - id: step-3
          tool: x_tickets_list_tickets
          args: {queue: compare ingress errors}
          store: true
        - id: step-4
          tool: x_logs_get_retention
          args: {installation: '{{ .input.installation }}'}
          store: true
  - name: compare-ingress-errors-per-installation
    labels: {fixture.invalid/verb: compare}
    config:
      name: compare-ingress-errors-per-installation
      description: 'Runbook: Compare ingress errors per installation.'
      args:
        installation: {type: string, required: true, description: The installation to run against.}
        window: {type: string, required: false, description: 'How far to look back, for example 24h.'}
      steps:
        - id: step-1
          tool: x_clusters_list_upgrades
          args: {installation: '{{ .input.installation }}', window: '{{ .input.window }}'}
          store: true
        - id: step-2
          tool: x_metrics_list_label_values
          args: {installation: '{{ .input.installation }}'}
          store: true
        - id: step-3
          tool: x_metrics_list_label_values
          args: {installation: '{{ .input.installation }}'}
          store: true
  - name: compare-log-volume-before-upgrade
    labels: {fixture.invalid/tier: gold, fixture.invalid/verb: compare}
    config:
      name: compare-log-volume-before-upgrade
      description: 'Runbook: Compare log volume before upgrade.'
      args:
        installation: {type: string, required: true, description: The installation to run against.}
        window: {type: string, required: false, description: 'How far to look back, for example 24h.'}
      steps:
        - id: step-1
          tool: x_clusters_get_cluster
          args: {installation: '{{ .input.installation }}'}
          store: true
  - name: compare-log-volume-for-the-week
    labels: {fixture.invalid/verb: compare}
    config:
      name: compare-log-volume-for-the-week
      description: 'Runbook: Compare log volume for the week.'
      args:
        installation: {type: string, required: true, description: The installation to run against.}
        window: {type: string, required: false, description: 'How far to look back, for example 24h.'}
      steps:
        - id: step-1
          tool: x_logs_list_streams
          args: {installation: '{{ .input.installation }}'}
          store: true
        - id: step-2
          tool: x_alerts_list_routes
          args: {installation: '{{ .input.installation }}'}
          store: true
        - id: step-3
          tool: x_metrics_list_rules
          args: {installation: '{{ .input.installation }}'}
          store: true
        - id: step-4
          tool: x_ledger_get_statement
          args: {installation: '{{ .input.installation }}'}
          store: true
  - name: compare-log-volume-on-request
    labels: {fixture.invalid/verb: compare}
    config:
      name: compare-log-volume-on-request
      description: 'Runbook: Compare log volume on request.'
      args:
        installation: {type: string, required: true, description: The installation to run against.}
        window: {type: string, required: false, description: 'How far to look back, for example 24h.'}
      steps:
        - id: step-1
          tool: x_metrics_list_label_values
          args: {installation: '{{ .input.installation }}'}
          store: true
        - id: step-2
          tool: x_alerts_list_silences
          args: {installation: '{{ .input.installation }}'}
          store: true
  - name: compare-node-pool-capacity-after-incident
    labels: {fixture.invalid/tier: gold, fixture.invalid/verb: compare}
    config:
      name: compare-node-pool-capacity-after-incident
      description: 'Runbook: Compare node pool capacity after incident.'
      args:
        installation: {type: string, required: true, description: The installation to run against.}
        window: {type: string, required: false, description: 'How far to look back, for example 24h.'}
      steps:
        - id: step-1
          tool: x_alerts_get_alert
          args: {installation: '{{ .input.installation }}'}
          store: true
        - id: step-2
          tool: x_ledger_list_usage
          args: {installation: '{{ .input.installation }}', window: '{{ .input.window }}'}
          store: true
        - id: step-3
          tool: x_clusters_list_upgrades
          args: {installation: '{{ .input.installation }}', window: '{{ .input.window }}'}
          store: true
        - id: step-4
          tool: x_handbook_list_recent_changes
          args: {window: compare node pool capacity}
          store: true
        - id: step-5
          tool: x_metrics_explain_series
          args: {installation: '{{ .input.installation }}'}
          store: true
  - name: compare-node-pool-capacity-on-request
    labels: {fixture.invalid/verb: compare}
    config:
      name: compare-node-pool-capacity-on-request
      description: 'Runbook: Compare node pool capacity on request.'
      args:
        installation: {type: string, required: true, description: The installation to run against.}
        window: {type: string, required: false, description: 'How far to look back, for example 24h.'}
      steps:
        - id: step-1
          tool: x_ledger_list_usage
          args: {installation: '{{ .input.installation }}', window: '{{ .input.window }}'}
          store: true
        - id: step-2
          tool: x_alerts_list_routes
          args: {installation: '{{ .input.installation }}'}
          store: true
        - id: step-3
          tool: x_logs_list_streams
          args: {installation: '{{ .input.installation }}'}
          store: true
  - name: compare-quota-headroom-across-organizations
    labels: {fixture.invalid/verb: compare}
    config:
      name: compare-quota-headroom-across-organizations
      description: 'Runbook: Compare quota headroom across organizations.'
      args:
        installation: {type: string, required: true, description: The installation to run against.}
        window: {type: string, required: false, description: 'How far to look back, for example 24h.'}
      steps:
        - id: step-1
          tool: x_clusters_list_clusters
          args: {installation: '{{ .input.installation }}'}
          store: true
        - id: step-2
          tool: x_logs_tail_stream
          args: {installation: '{{ .input.installation }}'}
          store: true
  - name: compare-quota-headroom-after-incident
    labels: {fixture.invalid/tier: gold, fixture.invalid/verb: compare}
    config:
      name: compare-quota-headroom-after-incident
      description: 'Runbook: Compare quota headroom after incident.'
      args:
        installation: {type: string, required: true, description: The installation to run against.}
        window: {type: string, required: false, description: 'How far to look back, for example 24h.'}
      steps:
        - id: step-1
          tool: x_ledger_list_usage
          args: {installation: '{{ .input.installation }}', window: '{{ .input.window }}'}
          store: true
        - id: step-2
          tool: x_runbook-library_list_runbooks
          args: {alert: compare quota headroom}
          store: true
        - id: step-3
          tool: x_clusters_get_quota
          args: {installation: '{{ .input.installation }}'}
          store: true
        - id: step-4
          tool: x_clusters_list_node_pools
          args: {installation: '{{ .input.installation }}'}
          store: true
  - name: compare-quota-headroom-on-request
    labels: {fixture.invalid/verb: compare}
    config:
      name: compare-quota-headroom-on-request
      description: 'Runbook: Compare quota headroom on request.'
      args:
        installation: {type: string, required: true, description: The installation to run against.}
        window: {type: string, required: false, description: 'How far to look back, for example 24h.'}
      steps:
        - id: step-1
          tool: x_logs_count_lines
          args: {installation: '{{ .input.installation }}', window: '{{ .input.window }}'}
          store: true
        - id: step-2
          tool: x_metrics_list_rules
          args: {installation: '{{ .input.installation }}'}
          store: true
        - id: step-3
          tool: x_alerts_list_routes
          args: {installation: '{{ .input.installation }}'}
          store: true
  - name: compare-release-drift-across-organizations
    labels: {fixture.invalid/verb: compare}
    config:
      name: compare-release-drift-across-organizations
      description: 'Runbook: Compare release drift across organizations.'
      args:
        installation: {type: string, required: true, description: The installation to run against.}
        window: {type: string, required: false, description: 'How far to look back, for example 24h.'}
      steps:
        - id: step-1
          tool: x_alerts_list_alerts
          args: {installation: '{{ .input.installation }}'}
          store: true
        - id: step-2
          tool: x_ledger_get_statement
          args: {installation: '{{ .input.installation }}'}
          store: true
        - id: step-3
          tool: x_metrics_query_range
          args: {installation: '{{ .input.installation }}', window: '{{ .input.window }}'}
          store: true
        - id: step-4
          tool: x_alerts_get_alert
          args: {installation: '{{ .input.installation }}'}
          store: true
  - name: compare-release-drift-before-upgrade
    labels: {fixture.invalid/tier: gold, fixture.invalid/verb: compare}
    config:
      name: compare-release-drift-before-upgrade
      description: 'Runbook: Compare release drift before upgrade.'
      args:
        installation: {type: string, required: true, description: The installation to run against.}
        window: {type: string, required: false, description: 'How far to look back, for example 24h.'}
      steps:
        - id: step-1
          tool: x_clusters_list_upgrades
          args: {installation: '{{ .input.installation }}', window: '{{ .input.window }}'}
          store: true
        - id: step-2
          tool: x_clusters_get_cluster
          args: {installation: '{{ .input.installation }}'}
          store: true
  - name: compare-retention-gaps-across-organizations
    labels: {fixture.invalid/verb: compare}
    config:
      name: compare-retention-gaps-across-organizations
      description: 'Runbook: Compare retention gaps across organizations.'
      args:
        installation: {type: string, required: true, description: The installation to run against.}
        window: {type: string, required: false, description: 'How far to look back, for example 24h.'}
      steps:
        - id: step-1
          tool: x_clusters_get_cluster
          args: {installation: '{{ .input.installation }}'}
          store: true
        - id: step-2
          tool: x_handbook_list_sections
          args: {path: compare retention gaps}
          store: true
        - id: step-3
          tool: x_clusters_list_node_pools
          args: {installation: '{{ .input.installation }}'}
          store: true
        - id: step-4
          tool: x_handbook_list_sections
          args: {path: compare retention gaps}
          store: true
  - name: compare-retention-gaps-after-incident
    labels: {fixture.invalid/verb: compare}
    config:
      name: compare-retention-gaps-after-incident
      description: 'Runbook: Compare retention gaps after incident.'
      args:
        installation: {type: string, required: true, description: The installation to run against.}
        window: {type: string, required: false, description: 'How far to look back, for example 24h.'}
      steps:
        - id: step-1
          tool: x_logs_list_streams
          args: {installation: '{{ .input.installation }}'}
          store: true
        - id: step-2
          tool: x_handbook_read_page
          args: {path: compare retention gaps}
          store: true
        - id: step-3
          tool: x_alerts_get_alert
          args: {installation: '{{ .input.installation }}'}
          store: true
  - name: compare-retention-gaps-per-installation
    labels: {fixture.invalid/tier: gold, fixture.invalid/verb: compare}
    config:
      name: compare-retention-gaps-per-installation
      description: 'Runbook: Compare retention gaps per installation.'
      args:
        installation: {type: string, required: true, description: The installation to run against.}
        window: {type: string, required: false, description: 'How far to look back, for example 24h.'}
      steps:
        - id: step-1
          tool: x_metrics_list_targets
          args: {installation: '{{ .input.installation }}'}
          store: true
        - id: step-2
          tool: x_metrics_query_instant
          args: {installation: '{{ .input.installation }}'}
          store: true
        - id: step-3
          tool: x_alerts_list_routes
          args: {installation: '{{ .input.installation }}'}
          store: true
        - id: step-4
          tool: x_clusters_get_quota
          args: {installation: '{{ .input.installation }}'}
          store: true
        - id: step-5
          tool: x_logs_count_lines
          args: {installation: '{{ .input.installation }}', window: '{{ .input.window }}'}
          store: true
  - name: compare-scrape-health-across-organizations
    labels: {fixture.invalid/verb: compare}
    config:
      name: compare-scrape-health-across-organizations
      description: 'Runbook: Compare scrape health across organizations.'
      args:
        installation: {type: string, required: true, description: The installation to run against.}
        window: {type: string, required: false, description: 'How far to look back, for example 24h.'}
      steps:
        - id: step-1
          tool: x_metrics_explain_series
          args: {installation: '{{ .input.installation }}'}
          store: true
        - id: step-2
          tool: x_clusters_list_upgrades
          args: {installation: '{{ .input.installation }}', window: '{{ .input.window }}'}
          store: true
        - id: step-3
          tool: x_runbook-library_search_runbooks
          args: {query: compare scrape health}
          store: true
  - name: compare-scrape-health-per-installation
    labels: {fixture.invalid/verb: compare}
    config:
      name: compare-scrape-health-per-installation
      description: 'Runbook: Compare scrape health per installation.'
      args:
        installation: {type: string, required: true, description: The installation to run against.}
        window: {type: string, required: false, description: 'How far to look back, for example 24h.'}
      steps:
        - id: step-1
          tool: x_clusters_get_quota
          args: {installation: '{{ .input.installation }}'}
          store: true
        - id: step-2
          tool: x_logs_count_lines
          args: {installation: '{{ .input.installation }}', window: '{{ .input.window }}'}
          store: true
  - name: compare-silence-hygiene-before-upgrade
    labels: {fixture.invalid/tier: gold, fixture.invalid/verb: compare}
    config:
      name: compare-silence-hygiene-before-upgrade
      description: 'Runbook: Compare silence hygiene before upgrade.'
      args:
        installation: {type: string, required: true, description: The installation to run against.}
        window: {type: string, required: false, description: 'How far to look back, for example 24h.'}
      steps:
        - id: step-1
          tool: x_metrics_query_range
          args: {installation: '{{ .input.installation }}', window: '{{ .input.window }}'}
          store: true
  - name: compare-silence-hygiene-for-the-week
    labels: {fixture.invalid/verb: compare}
    config:
      name: compare-silence-hygiene-for-the-week
      description: 'Runbook: Compare silence hygiene for the week.'
      args:
        installation: {type: string, required: true, description: The installation to run against.}
        window: {type: string, required: false, description: 'How far to look back, for example 24h.'}
      steps:
        - id: step-1
          tool: x_logs_tail_stream
          args: {installation: '{{ .input.installation }}'}
          store: true
        - id: step-2
          tool: x_clusters_list_events
          args: {installation: '{{ .input.installation }}', window: '{{ .input.window }}'}
          store: true
        - id: step-3
          tool: x_clusters_list_events
          args: {installation: '{{ .input.installation }}', window: '{{ .input.window }}'}
          store: true
        - id: step-4
          tool: x_clusters_get_release
          args: {installation: '{{ .input.installation }}'}
          store: true
  - name: compare-silence-hygiene-per-installation
    labels: {fixture.invalid/verb: compare}
    config:
      name: compare-silence-hygiene-per-installation
      description: 'Runbook: Compare silence hygiene per installation.'
      args:
        installation: {type: string, required: true, description: The installation to run against.}
        window: {type: string, required: false, description: 'How far to look back, for example 24h.'}
      steps:
        - id: step-1
          tool: x_clusters_list_upgrades
          args: {installation: '{{ .input.installation }}', window: '{{ .input.window }}'}
          store: true
        - id: step-2
          tool: x_runbook-library_list_recent_runs
          args: {runbook: compare silence hygiene}
          store: true
        - id: step-3
          tool: x_runbook-library_list_runbooks
          args: {alert: compare silence hygiene}
          store: true
        - id: step-4
          tool: x_metrics_query_range
          args: {installation: '{{ .input.installation }}', window: '{{ .input.window }}'}
          store: true
        - id: step-5
          tool: x_alerts_get_alert
          args: {installation: '{{ .input.installation }}'}
          store: true
  - name: compare-upgrade-readiness-after-incident
    labels: {fixture.invalid/tier: gold, fixture.invalid/verb: compare}
    config:
      name: compare-upgrade-readiness-after-incident
      description: 'Runbook: Compare upgrade readiness after incident.'
      args:
        installation: {type: string, required: true, description: The installation to run against.}
        window: {type: string, required: false, description: 'How far to look back, for example 24h.'}
      steps:
        - id: step-1
          tool: x_clusters_get_quota
          args: {installation: '{{ .input.installation }}'}
          store: true
        - id: step-2
          tool: x_metrics_list_label_values
          args: {installation: '{{ .input.installation }}'}
          store: true
        - id: step-3
          tool: x_clusters_list_node_pools
          args: {installation: '{{ .input.installation }}'}
          store: true
  - name: compare-upgrade-readiness-for-the-week
    labels: {fixture.invalid/verb: compare}
    config:
      name: compare-upgrade-readiness-for-the-week
      description: 'Runbook: Compare upgrade readiness for the week.'
      args:
        installation: {type: string, required: true, description: The installation to run against.}
        window: {type: string, required: false, description: 'How far to look back, for example 24h.'}
      steps:
        - id: step-1
          tool: x_metrics_list_series
          args: {installation: '{{ .input.installation }}'}
          store: true
        - id: step-2
          tool: x_tickets_search_tickets
          args: {query: compare upgrade readiness}
          store: true
  - name: compare-usage-anomalies-across-organizations
    labels: {fixture.invalid/verb: compare}
    config:
      name: compare-usage-anomalies-across-organizations
      description: 'Runbook: Compare usage anomalies across organizations.'
      args:
        installation: {type: string, required: true, description: The installation to run against.}
        window: {type: string, required: false, description: 'How far to look back, for example 24h.'}
      steps:
        - id: step-1
          tool: x_alerts_get_alert
          args: {installation: '{{ .input.installation }}'}
          store: true
        - id: step-2
          tool: x_runbook-library_list_runbooks
          args: {alert: compare usage anomalies}
          store: true
        - id: step-3
          tool: x_handbook_list_sections
          args: {path: compare usage anomalies}
          store: true
  - name: compare-usage-anomalies-before-upgrade
    labels: {fixture.invalid/tier: gold, fixture.invalid/verb: compare}
    config:
      name: compare-usage-anomalies-before-upgrade
      description: 'Runbook: Compare usage anomalies before upgrade.'
      args:
        installation: {type: string, required: true, description: The installation to run against.}
        window: {type: string, required: false, description: 'How far to look back, for example 24h.'}
      steps:
        - id: step-1
          tool: x_logs_get_retention
          args: {installation: '{{ .input.installation }}'}
          store: true
        - id: step-2
          tool: x_runbook-library_read_runbook
          args: {runbook: compare usage anomalies}
          store: true
        - id: step-3
          tool: x_logs_get_retention
          args: {installation: '{{ .input.installation }}'}
          store: true
  - name: drain-alert-noise-for-the-week
    labels: {fixture.invalid/verb: drain}
    config:
      name: drain-alert-noise-for-the-week
      description: 'Runbook: Drain alert noise for the week.'
      args:
        installation: {type: string, required: true, description: The installation to run against.}
        window: {type: string, required: false, description: 'How far to look back, for example 24h.'}
      steps:
        - id: step-1
          tool: x_logs_count_lines
          args: {installation: '{{ .input.installation }}', window: '{{ .input.window }}'}
          store: true
        - id: step-2
          tool: x_alerts_get_alert
          args: {installation: '{{ .input.installation }}'}
          store: true
        - id: step-3
          tool: x_alerts_list_alerts
          args: {installation: '{{ .input.installation }}'}
          store: true
        - id: step-4
          tool: x_logs_count_lines
          args: {installation: '{{ .input.installation }}', window: '{{ .input.window }}'}
          store: true
        - id: step-5
          tool: x_logs_get_retention
          args: {installation: '{{ .input.installation }}'}
          store: true
  - name: drain-certificate-expiry-across-organizations
    labels: {fixture.invalid/verb: drain}
    config:
      name: drain-certificate-expiry-across-organizations
      description: 'Runbook: Drain certificate expiry across organizations.'
      args:
        installation: {type: string, required: true, description: The installation to run against.}
        window: {type: string, required: false, description: 'How far to look back, for example 24h.'}
      steps:
        - id: step-1
          tool: x_metrics_list_targets
          args: {installation: '{{ .input.installation }}'}
          store: true
        - id: step-2
          tool: x_clusters_get_release
          args: {installation: '{{ .input.installation }}'}
          store: true
        - id: step-3
          tool: x_clusters_list_upgrades
          args: {installation: '{{ .input.installation }}', window: '{{ .input.window }}'}
          store: true
  - name: drain-certificate-expiry-after-incident
    labels: {fixture.invalid/tier: gold, fixture.invalid/verb: drain}
    config:
      name: drain-certificate-expiry-after-incident
      description: 'Runbook: Drain certificate expiry after incident.'
      args:
        installation: {type: string, required: true, description: The installation to run against.}
        window: {type: string, required: false, description: 'How far to look back, for example 24h.'}
      steps:
        - id: step-1
          tool: x_metrics_list_label_values
          args: {installation: '{{ .input.installation }}'}
          store: true
        - id: step-2
          tool: x_metrics_list_series
          args: {installation: '{{ .input.installation }}'}
          store: true
        - id: step-3
          tool: x_metrics_list_targets
          args: {installation: '{{ .input.installation }}'}
          store: true
        - id: step-4
          tool: x_clusters_get_release
          args: {installation: '{{ .input.installation }}'}
          store: true
        - id: step-5
          tool: x_alerts_list_silences
          args: {installation: '{{ .input.installation }}'}
          store: true
  - name: drain-certificate-expiry-before-upgrade
    labels: {fixture.invalid/verb: drain}
    config:
      name: drain-certificate-expiry-before-upgrade
      description: 'Runbook: Drain certificate expiry before upgrade.'
      args:
        installation: {type: string, required: true, description: The installation to run against.}
        window: {type: string, required: false, description: 'How far to look back, for example 24h.'}
      steps:
        - id: step-1
          tool: x_logs_query_logs
          args: {installation: '{{ .input.installation }}', window: '{{ .input.window }}'}
          store: true
        - id: step-2
          tool: x_alerts_get_alert
          args: {installation: '{{ .input.installation }}'}
          store: true
  - name: drain-log-volume-on-request
    labels: {fixture.invalid/verb: drain}
    config:
      name: drain-log-volume-on-request
      description: 'Runbook: Drain log volume on request.'
      args:
        installation: {type: string, required: true, description: The installation to run against.}
        window: {type: string, required: false, description: 'How far to look back, for example 24h.'}
      steps:
        - id: step-1
          tool: x_metrics_explain_series
          args: {installation: '{{ .input.installation }}'}
          store: true
  - name: drain-node-pool-capacity-across-organizations
    labels: {fixture.invalid/tier: gold, fixture.invalid/verb: drain}
    config:
      name: drain-node-pool-capacity-across-organizations
      description: 'Runbook: Drain node pool capacity across organizations.'
      args:
        installation: {type: string, required: true, description: The installation to run against.}
        window: {type: string, required: false, description: 'How far to look back, for example 24h.'}
      steps:
        - id: step-1
          tool: x_alerts_get_alert
          args: {installation: '{{ .input.installation }}'}
          store: true
        - id: step-2
          tool: x_runbook-library_list_recent_runs
          args: {runbook: drain node pool capacity}
          store: true
  - name: drain-node-pool-capacity-on-request
    labels: {fixture.invalid/verb: drain}
    config:
      name: drain-node-pool-capacity-on-request
      description: 'Runbook: Drain node pool capacity on request.'
      args:
        installation: {type: string, required: true, description: The installation to run against.}
        window: {type: string, required: false, description: 'How far to look back, for example 24h.'}
      steps:
        - id: step-1
          tool: x_metrics_list_series
          args: {installation: '{{ .input.installation }}'}
          store: true
  - name: drain-quota-headroom-across-organizations
    labels: {fixture.invalid/verb: drain}
    config:
      name: drain-quota-headroom-across-organizations
      description: 'Runbook: Drain quota headroom across organizations.'
      args:
        installation: {type: string, required: true, description: The installation to run against.}
        window: {type: string, required: false, description: 'How far to look back, for example 24h.'}
      steps:
        - id: step-1
          tool: x_logs_list_streams
          args: {installation: '{{ .input.installation }}'}
          store: true
        - id: step-2
          tool: x_metrics_list_rules
          args: {installation: '{{ .input.installation }}'}
          store: true
  - name: drain-release-drift-before-upgrade
    labels: {fixture.invalid/tier: gold, fixture.invalid/verb: drain}
    config:
      name: drain-release-drift-before-upgrade
      description: 'Runbook: Drain release drift before upgrade.'
      args:
        installation: {type: string, required: true, description: The installation to run against.}
        window: {type: string, required: false, description: 'How far to look back, for example 24h.'}
      steps:
        - id: step-1
          tool: x_clusters_get_cluster
          args: {installation: '{{ .input.installation }}'}
          store: true
        - id: step-2
          tool: x_metrics_query_range
          args: {installation: '{{ .input.installation }}', window: '{{ .input.window }}'}
          store: true
        - id: step-3
          tool: x_runbook-library_search_runbooks
          args: {query: drain release drift}
          store: true
        - id: step-4
          tool: x_metrics_explain_series
          args: {installation: '{{ .input.installation }}'}
          store: true
        - id: step-5
          tool: x_clusters_get_cluster
          args: {installation: '{{ .input.installation }}'}
          store: true
  - name: drain-release-drift-on-request
    labels: {fixture.invalid/verb: drain}
    config:
      name: drain-release-drift-on-request
      description: 'Runbook: Drain release drift on request.'
      args:
        installation: {type: string, required: true, description: The installation to run against.}
        window: {type: string, required: false, description: 'How far to look back, for example 24h.'}
      steps:
        - id: step-1
          tool: x_clusters_list_events
          args: {installation: '{{ .input.installation }}', window: '{{ .input.window }}'}
          store: true
        - id: step-2
          tool: x_tickets_search_tickets
          args: {query: drain release drift}
          store: true
        - id: step-3
          tool: x_metrics_list_rules
          args: {installation: '{{ .input.installation }}'}
          store: true
  - name: drain-release-drift-per-installation
    labels: {fixture.invalid/verb: drain}
    config:
      name: drain-release-drift-per-installation
      description: 'Runbook: Drain release drift per installation.'
      args:
        installation: {type: string, required: true, description: The installation to run against.}
        window: {type: string, required: false, description: 'How far to look back, for example 24h.'}
      steps:
        - id: step-1
          tool: x_ledger_list_usage
          args: {installation: '{{ .input.installation }}', window: '{{ .input.window }}'}
          store: true
        - id: step-2
          tool: x_ledger_export_records
          args: {installation: '{{ .input.installation }}'}
          store: true
  - name: drain-retention-gaps-across-organizations
    labels: {fixture.invalid/tier: gold, fixture.invalid/verb: drain}
    config:
      name: drain-retention-gaps-across-organizations
      description: 'Runbook: Drain retention gaps across organizations.'
      args:
        installation: {type: string, required: true, description: The installation to run against.}
        window: {type: string, required: false, description: 'How far to look back, for example 24h.'}
      steps:
        - id: step-1
          tool: x_metrics_list_targets
          args: {installation: '{{ .input.installation }}'}
          store: true
  - name: drain-retention-gaps-per-installation
    labels: {fixture.invalid/verb: drain}
    config:
      name: drain-retention-gaps-per-installation
      description: 'Runbook: Drain retention gaps per installation.'
      args:
        installation: {type: string, required: true, description: The installation to run against.}
        window: {type: string, required: false, description: 'How far to look back, for example 24h.'}
      steps:
        - id: step-1
          tool: x_alerts_list_routes
          args: {installation: '{{ .input.installation }}'}
          store: true
        - id: step-2
          tool: x_handbook_list_sections
          args: {path: drain retention gaps}
          store: true
  - name: drain-scrape-health-across-organizations
    labels: {fixture.invalid/verb: drain}
    config:
      name: drain-scrape-health-across-organizations
      description: 'Runbook: Drain scrape health across organizations.'
      args:
        installation: {type: string, required: true, description: The installation to run against.}
        window: {type: string, required: false, description: 'How far to look back, for example 24h.'}
      steps:
        - id: step-1
          tool: x_alerts_list_routes
          args: {installation: '{{ .input.installation }}'}
          store: true
        - id: step-2
          tool: x_handbook_list_recent_changes
          args: {window: drain scrape health}
          store: true
  - name: drain-scrape-health-for-the-week
    labels: {fixture.invalid/tier: gold, fixture.invalid/verb: drain}
    config:
      name: drain-scrape-health-for-the-week
      description: 'Runbook: Drain scrape health for the week.'
      args:
        installation: {type: string, required: true, description: The installation to run against.}
        window: {type: string, required: false, description: 'How far to look back, for example 24h.'}
      steps:
        - id: step-1
          tool: x_clusters_get_quota
          args: {installation: '{{ .input.installation }}'}
          store: true
  - name: drain-scrape-health-on-request
    labels: {fixture.invalid/verb: drain}
    config:
      name: drain-scrape-health-on-request
      description: 'Runbook: Drain scrape health on request.'
      args:
        installation: {type: string, required: true, description: The installation to run against.}
        window: {type: string, required: false, description: 'How far to look back, for example 24h.'}
      steps:
        - id: step-1
          tool: x_metrics_query_instant
          args: {installation: '{{ .input.installation }}'}
          store: true
        - id: step-2
          tool: x_logs_tail_stream
          args: {installation: '{{ .input.installation }}'}
          store: true
        - id: step-3
          tool: x_clusters_get_release
          args: {installation: '{{ .input.installation }}'}
          store: true
        - id: step-4
          tool: x_metrics_query_instant
          args: {installation: '{{ .input.installation }}'}
          store: true
  - name: drain-scrape-health-per-installation
    labels: {fixture.invalid/verb: drain}
    config:
      name: drain-scrape-health-per-installation
      description: 'Runbook: Drain scrape health per installation.'
      args:
        installation: {type: string, required: true, description: The installation to run against.}
        window: {type: string, required: false, description: 'How far to look back, for example 24h.'}
      steps:
        - id: step-1
          tool: x_alerts_get_alert
          args: {installation: '{{ .input.installation }}'}
          store: true
  - name: drain-silence-hygiene-after-incident
    labels: {fixture.invalid/tier: gold, fixture.invalid/verb: drain}
    config:
      name: drain-silence-hygiene-after-incident
      description: 'Runbook: Drain silence hygiene after incident.'
      args:
        installation: {type: string, required: true, description: The installation to run against.}
        window: {type: string, required: false, description: 'How far to look back, for example 24h.'}
      steps:
        - id: step-1
          tool: x_metrics_list_rules
          args: {installation: '{{ .input.installation }}'}
          store: true
        - id: step-2
          tool: x_clusters_get_quota
          args: {installation: '{{ .input.installation }}'}
          store: true
        - id: step-3
          tool: x_runbook-library_search_runbooks
          args: {query: drain silence hygiene}
          store: true
  - name: drain-silence-hygiene-per-installation
    labels: {fixture.invalid/verb: drain}
    config:
      name: drain-silence-hygiene-per-installation
      description: 'Runbook: Drain silence hygiene per installation.'
      args:
        installation: {type: string, required: true, description: The installation to run against.}
        window: {type: string, required: false, description: 'How far to look back, for example 24h.'}
      steps:
        - id: step-1
          tool: x_ledger_list_adjustments
          args: {installation: '{{ .input.installation }}'}
          store: true
        - id: step-2
          tool: x_clusters_get_quota
          args: {installation: '{{ .input.installation }}'}
          store: true
        - id: step-3
          tool: x_runbook-library_list_recent_runs
          args: {runbook: drain silence hygiene}
          store: true
  - name: drain-upgrade-readiness-for-the-week
    labels: {fixture.invalid/verb: drain}
    config:
      name: drain-upgrade-readiness-for-the-week
      description: 'Runbook: Drain upgrade readiness for the week.'
      args:
        installation: {type: string, required: true, description: The installation to run against.}
        window: {type: string, required: false, description: 'How far to look back, for example 24h.'}
      steps:
        - id: step-1
          tool: x_clusters_get_quota
          args: {installation: '{{ .input.installation }}'}
          store: true
        - id: step-2
          tool: x_clusters_get_cluster
          args: {installation: '{{ .input.installation }}'}
          store: true
        - id: step-3
          tool: x_logs_count_lines
          args: {installation: '{{ .input.installation }}', window: '{{ .input.window }}'}
          store: true
        - id: step-4
          tool: x_handbook_list_recent_changes
          args: {window: drain upgrade readiness}
          store: true
        - id: step-5
          tool: x_clusters_list_node_pools
          args: {installation: '{{ .input.installation }}'}
          store: true
  - name: drain-upgrade-readiness-per-installation
    labels: {fixture.invalid/tier: gold, fixture.invalid/verb: drain}
    config:
      name: drain-upgrade-readiness-per-installation
      description: 'Runbook: Drain upgrade readiness per installation.'
      args:
        installation: {type: string, required: true, description: The installation to run against.}
        window: {type: string, required: false, description: 'How far to look back, for example 24h.'}
      steps:
        - id: step-1
          tool: x_logs_get_retention
          args: {installation: '{{ .input.installation }}'}
          store: true
        - id: step-2
          tool: x_metrics_list_series
          args: {installation: '{{ .input.installation }}'}
          store: true
        - id: step-3
          tool: x_tickets_read_ticket
          args: {ticket: drain upgrade readiness}
          store: true
        - id: step-4
          tool: x_metrics_explain_series
          args: {installation: '{{ .input.installation }}'}
          store: true
        - id: step-5
          tool: x_clusters_get_quota
          args: {installation: '{{ .input.installation }}'}
          store: true
  - name: escalate-alert-noise-after-incident
    labels: {fixture.invalid/verb: escalate}
    config:
      name: escalate-alert-noise-after-incident
      description: 'Runbook: Escalate alert noise after incident.'
      args:
        installation: {type: string, required: true, description: The installation to run against.}
        window: {type: string, required: false, description: 'How far to look back, for example 24h.'}
      steps:
        - id: step-1
          tool: x_metrics_query_instant
          args: {installation: '{{ .input.installation }}'}
          store: true
        - id: step-2
          tool: x_alerts_list_routes
          args: {installation: '{{ .input.installation }}'}
          store: true
  - name: escalate-alert-noise-before-upgrade
    labels: {fixture.invalid/verb: escalate}
    config:
      name: escalate-alert-noise-before-upgrade
      description: 'Runbook: Escalate alert noise before upgrade.'
      args:
        installation: {type: string, required: true, description: The installation to run against.}
        window: {type: string, required: false, description: 'How far to look back, for example 24h.'}
      steps:
        - id: step-1
          tool: x_clusters_get_cluster
          args: {installation: '{{ .input.installation }}'}
          store: true
        - id: step-2
          tool: x_tickets_search_tickets
          args: {query: escalate alert noise}
          store: true
        - id: step-3
          tool: x_clusters_get_release
          args: {installation: '{{ .input.installation }}'}
          store: true
        - id: step-4
          tool: x_runbook-library_read_runbook
          args: {runbook: escalate alert noise}
          store: true
        - id: step-5
          tool: x_runbook-library_list_recent_runs
          args: {runbook: escalate alert noise}
          store: true
  - name: escalate-alert-noise-on-request
    labels: {fixture.invalid/tier: gold, fixture.invalid/verb: escalate}
    config:
      name: escalate-alert-noise-on-request
      description: 'Runbook: Escalate alert noise on request.'
      args:
        installation: {type: string, required: true, description: The installation to run against.}
        window: {type: string, required: false, description: 'How far to look back, for example 24h.'}
      steps:
        - id: step-1
          tool: x_clusters_get_quota
          args: {installation: '{{ .input.installation }}'}
          store: true
  - name: escalate-certificate-expiry-across-organizations
    labels: {fixture.invalid/verb: escalate}
    config:
      name: escalate-certificate-expiry-across-organizations
      description: 'Runbook: Escalate certificate expiry across organizations.'
      args:
        installation: {type: string, required: true, description: The installation to run against.}
        window: {type: string, required: false, description: 'How far to look back, for example 24h.'}
      steps:
        - id: step-1
          tool: x_ledger_list_usage
          args: {installation: '{{ .input.installation }}', window: '{{ .input.window }}'}
          store: true
        - id: step-2
          tool: x_logs_tail_stream
          args: {installation: '{{ .input.installation }}'}
          store: true
        - id: step-3
          tool: x_metrics_list_rules
          args: {installation: '{{ .input.installation }}'}
          store: true
        - id: step-4
          tool: x_clusters_get_release
          args: {installation: '{{ .input.installation }}'}
          store: true
  - name: escalate-certificate-expiry-before-upgrade
    labels: {fixture.invalid/verb: escalate}
    config:
      name: escalate-certificate-expiry-before-upgrade
      description: 'Runbook: Escalate certificate expiry before upgrade.'
      args:
        installation: {type: string, required: true, description: The installation to run against.}
        window: {type: string, required: false, description: 'How far to look back, for example 24h.'}
      steps:
        - id: step-1
          tool: x_alerts_list_routes
          args: {installation: '{{ .input.installation }}'}
          store: true
        - id: step-2
          tool: x_ledger_list_adjustments
          args: {installation: '{{ .input.installation }}'}
          store: true
  - name: escalate-log-volume-after-incident
    labels: {fixture.invalid/tier: gold, fixture.invalid/verb: escalate}
    config:
      name: escalate-log-volume-after-incident
      description: 'Runbook: Escalate log volume after incident.'
      args:
        installation: {type: string, required: true, description: The installation to run against.}
        window: {type: string, required: false, description: 'How far to look back, for example 24h.'}
      steps:
        - id: step-1
          tool: x_metrics_list_targets
          args: {installation: '{{ .input.installation }}'}
          store: true
        - id: step-2
          tool: x_logs_tail_stream
          args: {installation: '{{ .input.installation }}'}
          store: true
        - id: step-3
          tool: x_metrics_list_rules
          args: {installation: '{{ .input.installation }}'}
          store: true
        - id: step-4
          tool: x_handbook_read_page
          args: {path: escalate log volume}
          store: true
        - id: step-5
          tool: x_metrics_explain_series
          args: {installation: '{{ .input.installation }}'}
          store: true
  - name: escalate-node-pool-capacity-before-upgrade
    labels: {fixture.invalid/verb: escalate}
    config:
      name: escalate-node-pool-capacity-before-upgrade
      description: 'Runbook: Escalate node pool capacity before upgrade.'
      args:
        installation: {type: string, required: true, description: The installation to run against.}
        window: {type: string, required: false, description: 'How far to look back, for example 24h.'}
      steps:
        - id: step-1
          tool: x_clusters_list_node_pools
          args: {installation: '{{ .input.installation }}'}
          store: true
        - id: step-2
          tool: x_alerts_get_alert
          args: {installation: '{{ .input.installation }}'}
          store: true
        - id: step-3
          tool: x_runbook-library_list_recent_runs
          args: {runbook: escalate node pool capacity}
          store: true
  - name: escalate-quota-headroom-on-request
    labels: {fixture.invalid/verb: escalate}
    config:
      name: escalate-quota-headroom-on-request
      description: 'Runbook: Escalate quota headroom on request.'
      args:
        installation: {type: string, required: true, description: The installation to run against.}
        window: {type: string, required: false, description: 'How far to look back, for example 24h.'}
      steps:
        - id: step-1
          tool: x_metrics_list_series
          args: {installation: '{{ .input.installation }}'}
          store: true
        - id: step-2
          tool: x_runbook-library_list_runbooks
          args: {alert: escalate quota headroom}
          store: true
        - id: step-3
          tool: x_logs_query_logs
          args: {installation: '{{ .input.installation }}', window: '{{ .input.window }}'}
          store: true
        - id: step-4
          tool: x_runbook-library_list_runbooks
          args: {alert: escalate quota headroom}
          store: true
        - id: step-5
          tool: x_ledger_list_adjustments
          args: {installation: '{{ .input.installation }}'}
          store: true
  - name: escalate-release-drift-after-incident
    labels: {fixture.invalid/tier: gold, fixture.invalid/verb: escalate}
    config:
      name: escalate-release-drift-after-incident
      description: 'Runbook: Escalate release drift after incident.'
      args:
        installation: {type: string, required: true, description: The installation to run against.}
        window: {type: string, required: false, description: 'How far to look back, for example 24h.'}
      steps:
        - id: step-1
          tool: x_clusters_list_upgrades
          args: {installation: '{{ .input.installation }}', window: '{{ .input.window }}'}
          store: true
        - id: step-2
          tool: x_logs_get_retention
          args: {installation: '{{ .input.installation }}'}
          store: true
  - name: escalate-release-drift-on-request
    labels: {fixture.invalid/verb: escalate}
    config:
      name: escalate-release-drift-on-request
      description: 'Runbook: Escalate release drift on request.'
      args:
        installation: {type: string, required: true, description: The installation to run against.}
        window: {type: string, required: false, description: 'How far to look back, for example 24h.'}
      steps:
        - id: step-1
          tool: x_clusters_get_quota
          args: {installation: '{{ .input.installation }}'}
          store: true
        - id: step-2
          tool: x_tickets_search_tickets
          args: {query: escalate release drift}
          store: true
  - name: escalate-retention-gaps-across-organizations
    labels: {fixture.invalid/verb: escalate}
    config:
      name: escalate-retention-gaps-across-organizations
      description: 'Runbook: Escalate retention gaps across organizations.'
      args:
        installation: {type: string, required: true, description: The installation to run against.}
        window: {type: string, required: false, description: 'How far to look back, for example 24h.'}
      steps:
        - id: step-1
          tool: x_logs_list_streams
          args: {installation: '{{ .input.installation }}'}
          store: true
        - id: step-2
          tool: x_clusters_list_node_pools
          args: {installation: '{{ .input.installation }}'}
          store: true
  - name: escalate-retention-gaps-per-installation
    labels: {fixture.invalid/tier: gold, fixture.invalid/verb: escalate}
    config:
      name: escalate-retention-gaps-per-installation
      description: 'Runbook: Escalate retention gaps per installation.'
      args:
        installation: {type: string, required: true, description: The installation to run against.}
        window: {type: string, required: false, description: 'How far to look back, for example 24h.'}
      steps:
        - id: step-1
          tool: x_ledger_export_records
          args: {installation: '{{ .input.installation }}'}
          store: true
        - id: step-2
          tool: x_clusters_get_cluster
          args: {installation: '{{ .input.installation }}'}
          store: true
        - id: step-3
          tool: x_metrics_explain_series
          args: {installation: '{{ .input.installation }}'}
          store: true
  - name: escalate-scrape-health-before-upgrade
    labels: {fixture.invalid/verb: escalate}
    config:
      name: escalate-scrape-health-before-upgrade
      description: 'Runbook: Escalate scrape health before upgrade.'
      args:
        installation: {type: string, required: true, description: The installation to run against.}
        window: {type: string, required: false, description: 'How far to look back, for example 24h.'}
      steps:
        - id: step-1
          tool: x_ledger_list_usage
          args: {installation: '{{ .input.installation }}', window: '{{ .input.window }}'}
          store: true
  - name: escalate-scrape-health-per-installation
    labels: {fixture.invalid/verb: escalate}
    config:
      name: escalate-scrape-health-per-installation
      description: 'Runbook: Escalate scrape health per installation.'
      args:
        installation: {type: string, required: true, description: The installation to run against.}
        window: {type: string, required: false, description: 'How far to look back, for example 24h.'}
      steps:
        - id: step-1
          tool: x_alerts_list_alerts
          args: {installation: '{{ .input.installation }}'}
          store: true
        - id: step-2
          tool: x_logs_detect_patterns
          args: {installation: '{{ .input.installation }}', window: '{{ .input.window }}'}
          store: true
        - id: step-3
          tool: x_alerts_list_routes
          args: {installation: '{{ .input.installation }}'}
          store: true
        - id: step-4
          tool: x_logs_query_logs
          args: {installation: '{{ .input.installation }}', window: '{{ .input.window }}'}
          store: true
        - id: step-5
          tool: x_clusters_list_events
          args: {installation: '{{ .input.installation }}', window: '{{ .input.window }}'}
          store: true
  - name: escalate-silence-hygiene-after-incident
    labels: {fixture.invalid/tier: gold, fixture.invalid/verb: escalate}
    config:
      name: escalate-silence-hygiene-after-incident
      description: 'Runbook: Escalate silence hygiene after incident.'
      args:
        installation: {type: string, required: true, description: The installation to run against.}
        window: {type: string, required: false, description: 'How far to look back, for example 24h.'}
      steps:
        - id: step-1
          tool: x_logs_query_logs
          args: {installation: '{{ .input.installation }}', window: '{{ .input.window }}'}
          store: true
        - id: step-2
          tool: x_alerts_get_alert
          args: {installation: '{{ .input.installation }}'}
          store: true
        - id: step-3
          tool: x_handbook_list_sections
          args: {path: escalate silence hygiene}
          store: true
  - name: escalate-silence-hygiene-before-upgrade
    labels: {fixture.invalid/verb: escalate}
    config:
      name: escalate-silence-hygiene-before-upgrade
      description: 'Runbook: Escalate silence hygiene before upgrade.'
      args:
        installation: {type: string, required: true, description: The installation to run against.}
        window: {type: string, required: false, description: 'How far to look back, for example 24h.'}
      steps:
        - id: step-1
          tool: x_metrics_explain_series
          args: {installation: '{{ .input.installation }}'}
          store: true
        - id: step-2
          tool: x_alerts_list_silences
          args: {installation: '{{ .input.installation }}'}
          store: true
  - name: escalate-upgrade-readiness-after-incident
    labels: {fixture.invalid/verb: escalate}
    config:
      name: escalate-upgrade-readiness-after-incident
      description: 'Runbook: Escalate upgrade readiness after incident.'
      args:
        installation: {type: string, required: true, description: The installation to run against.}
        window: {type: string, required: false, description: 'How far to look back, for example 24h.'}
      steps:
        - id: step-1
          tool: x_ledger_list_usage
          args: {installation: '{{ .input.installation }}', window: '{{ .input.window }}'}
          store: true
        - id: step-2
          tool: x_clusters_list_upgrades
          args: {installation: '{{ .input.installation }}', window: '{{ .input.window }}'}
          store: true
        - id: step-3
          tool: x_clusters_list_clusters
          args: {installation: '{{ .input.installation }}'}
          store: true
        - id: step-4
          tool: x_clusters_list_clusters
          args: {installation: '{{ .input.installation }}'}
          store: true
  - name: escalate-upgrade-readiness-before-upgrade
    labels: {fixture.invalid/tier: gold, fixture.invalid/verb: escalate}
    config:
      name: escalate-upgrade-readiness-before-upgrade
      description: 'Runbook: Escalate upgrade readiness before upgrade.'
      args:
        installation: {type: string, required: true, description: The installation to run against.}
        window: {type: string, required: false, description: 'How far to look back, for example 24h.'}
      steps:
        - id: step-1
          tool: x_logs_query_logs
          args: {installation: '{{ .input.installation }}', window: '{{ .input.window }}'}
          store: true
  - name: escalate-usage-anomalies-across-organizations
    labels: {fixture.invalid/verb: escalate}
    config:
      name: escalate-usage-anomalies-across-organizations
      description: 'Runbook: Escalate usage anomalies across organizations.'
      args:
        installation: {type: string, required: true, description: The installation to run against.}
        window: {type: string, required: false, description: 'How far to look back, for example 24h.'}
      steps:
        - id: step-1
          tool: x_metrics_query_range
          args: {installation: '{{ .input.installation }}', window: '{{ .input.window }}'}
          store: true
        - id: step-2
          tool: x_handbook_read_page
          args: {path: escalate usage anomalies}
          store: true
  - name: escalate-usage-anomalies-per-installation
    labels: {fixture.invalid/verb: escalate}
    config:
      name: escalate-usage-anomalies-per-installation
      description: 'Runbook: Escalate usage anomalies per installation.'
      args:
        installation: {type: string, required: true, description: The installation to run against.}
        window: {type: string, required: false, description: 'How far to look back, for example 24h.'}
      steps:
        - id: step-1
          tool: x_clusters_list_node_pools
          args: {installation: '{{ .input.installation }}'}
          store: true
        - id: step-2
          tool: x_metrics_list_rules
          args: {installation: '{{ .input.installation }}'}
          store: true
        - id: step-3
          tool: x_clusters_get_cluster
          args: {installation: '{{ .input.installation }}'}
          store: true
        - id: step-4
          tool: x_logs_detect_patterns
          args: {installation: '{{ .input.installation }}', window: '{{ .input.window }}'}
          store: true
        - id: step-5
          tool: x_tickets_list_tickets
          args: {queue: escalate usage anomalies}
          store: true
  - name: forecast-alert-noise-across-organizations
    labels: {fixture.invalid/tier: gold, fixture.invalid/verb: forecast}
    config:
      name: forecast-alert-noise-across-organizations
      description: 'Runbook: Forecast alert noise across organizations.'
      args:
        installation: {type: string, required: true, description: The installation to run against.}
        window: {type: string, required: false, description: 'How far to look back, for example 24h.'}
      steps:
        - id: step-1
          tool: x_ledger_list_usage
          args: {installation: '{{ .input.installation }}', window: '{{ .input.window }}'}
          store: true
  - name: forecast-alert-noise-after-incident
    labels: {fixture.invalid/verb: forecast}
    config:
      name: forecast-alert-noise-after-incident
      description: 'Runbook: Forecast alert noise after incident.'
      args:
        installation: {type: string, required: true, description: The installation to run against.}
        window: {type: string, required: false, description: 'How far to look back, for example 24h.'}
      steps:
        - id: step-1
          tool: x_alerts_list_alerts
          args: {installation: '{{ .input.installation }}'}
          store: true
        - id: step-2
          tool: x_alerts_get_alert
          args: {installation: '{{ .input.installation }}'}
          store: true
        - id: step-3
          tool: x_logs_tail_stream
          args: {installation: '{{ .input.installation }}'}
          store: true
  - name: forecast-alert-noise-for-the-week
    labels: {fixture.invalid/verb: forecast}
    config:
      name: forecast-alert-noise-for-the-week
      description: 'Runbook: Forecast alert noise for the week.'
      args:
        installation: {type: string, required: true, description: The installation to run against.}
        window: {type: string, required: false, description: 'How far to look back, for example 24h.'}
      steps:
        - id: step-1
          tool: x_alerts_get_alert
          args: {installation: '{{ .input.installation }}'}
          store: true
  - name: forecast-alert-noise-per-installation
    labels: {fixture.invalid/tier: gold, fixture.invalid/verb: forecast}
    config:
      name: forecast-alert-noise-per-installation
      description: 'Runbook: Forecast alert noise per installation.'
      args:
        installation: {type: string, required: true, description: The installation to run against.}
        window: {type: string, required: false, description: 'How far to look back, for example 24h.'}
      steps:
        - id: step-1
          tool: x_metrics_list_targets
          args: {installation: '{{ .input.installation }}'}
          store: true
  - name: forecast-certificate-expiry-across-organizations
    labels: {fixture.invalid/verb: forecast}
    config:
      name: forecast-certificate-expiry-across-organizations
      description: 'Runbook: Forecast certificate expiry across organizations.'
      args:
        installation: {type: string, required: true, description: The installation to run against.}
        window: {type: string, required: false, description: 'How far to look back, for example 24h.'}
      steps:
        - id: step-1
          tool: x_alerts_get_alert
          args: {installation: '{{ .input.installation }}'}
          store: true
        - id: step-2
          tool: x_handbook_list_sections
          args: {path: forecast certificate expiry}
          store: true
        - id: step-3
          tool: x_logs_count_lines
          args: {installation: '{{ .input.installation }}', window: '{{ .input.window }}'}
          store: true
  - name: forecast-certificate-expiry-before-upgrade
    labels: {fixture.invalid/verb: forecast}
    config:
      name: forecast-certificate-expiry-before-upgrade
      description: 'Runbook: Forecast certificate expiry before upgrade.'
      args:
        installation: {type: string, required: true, description: The installation to run against.}
        window: {type: string, required: false, description: 'How far to look back, for example 24h.'}
      steps:
        - id: step-1
          tool: x_metrics_query_range
          args: {installation: '{{ .input.installation }}', window: '{{ .input.window }}'}
          store: true
        - id: step-2
          tool: x_ledger_list_usage
          args: {installation: '{{ .input.installation }}', window: '{{ .input.window }}'}
          store: true
        - id: step-3
          tool: x_runbook-library_read_runbook
          args: {runbook: forecast certificate expiry}
          store: true
        - id: step-4
          tool: x_metrics_list_targets
          args: {installation: '{{ .input.installation }}'}
          store: true
        - id: step-5
          tool: x_clusters_list_node_pools
          args: {installation: '{{ .input.installation }}'}
          store: true
  - name: forecast-ingress-errors-before-upgrade
    labels: {fixture.invalid/tier: gold, fixture.invalid/verb: forecast}
    config:
      name: forecast-ingress-errors-before-upgrade
      description: 'Runbook: Forecast ingress errors before upgrade.'
      args:
        installation: {type: string, required: true, description: The installation to run against.}
        window: {type: string, required: false, description: 'How far to look back, for example 24h.'}
      steps:
        - id: step-1
          tool: x_logs_get_retention
          args: {installation: '{{ .input.installation }}'}
          store: true
  - name: forecast-ingress-errors-per-installation
    labels: {fixture.invalid/verb: forecast}
    config:
      name: forecast-ingress-errors-per-installation
      description: 'Runbook: Forecast ingress errors per installation.'
      args:
        installation: {type: string, required: true, description: The installation to run against.}
        window: {type: string, required: false, description: 'How far to look back, for example 24h.'}
      steps:
        - id: step-1
          tool: x_clusters_get_quota
          args: {installation: '{{ .input.installation }}'}
          store: true
        - id: step-2
          tool: x_metrics_explain_series
          args: {installation: '{{ .input.installation }}'}
          store: true
        - id: step-3
          tool: x_clusters_get_cluster
          args: {installation: '{{ .input.installation }}'}
          store: true
        - id: step-4
          tool: x_runbook-library_list_recent_runs
          args: {runbook: forecast ingress errors}
          store: true
        - id: step-5
          tool: x_logs_detect_patterns
          args: {installation: '{{ .input.installation }}', window: '{{ .input.window }}'}
          store: true
  - name: forecast-log-volume-across-organizations
    labels: {fixture.invalid/verb: forecast}
    config:
      name: forecast-log-volume-across-organizations
      description: 'Runbook: Forecast log volume across organizations.'
      args:
        installation: {type: string, required: true, description: The installation to run against.}
        window: {type: string, required: false, description: 'How far to look back, for example 24h.'}
      steps:
        - id: step-1
          tool: x_alerts_list_alerts
          args: {installation: '{{ .input.installation }}'}
          store: true
        - id: step-2
          tool: x_ledger_export_records
          args: {installation: '{{ .input.installation }}'}
          store: true
        - id: step-3
          tool: x_logs_detect_patterns
          args: {installation: '{{ .input.installation }}', window: '{{ .input.window }}'}
          store: true
        - id: step-4
          tool: x_clusters_list_upgrades
          args: {installation: '{{ .input.installation }}', window: '{{ .input.window }}'}
          store: true
        - id: step-5
          tool: x_alerts_list_alerts
          args: {installation: '{{ .input.installation }}'}
          store: true
  - name: forecast-log-volume-for-the-week
    labels: {fixture.invalid/tier: gold, fixture.invalid/verb: forecast}
    config:
      name: forecast-log-volume-for-the-week
      description: 'Runbook: Forecast log volume for the week.'
      args:
        installation: {type: string, required: true, description: The installation to run against.}
        window: {type: string, required: false, description: 'How far to look back, for example 24h.'}
      steps:
        - id: step-1
          tool: x_metrics_query_range
          args: {installation: '{{ .input.installation }}', window: '{{ .input.window }}'}
          store: true
        - id: step-2
          tool: x_alerts_list_alerts
          args: {installation: '{{ .input.installation }}'}
          store: true
        - id: step-3
          tool: x_runbook-library_list_runbooks
          args: {alert: forecast log volume}
          store: true
  - name: forecast-log-volume-per-installation
    labels: {fixture.invalid/verb: forecast}
    config:
      name: forecast-log-volume-per-installation
      description: 'Runbook: Forecast log volume per installation.'
      args:
        installation: {type: string, required: true, description: The installation to run against.}
        window: {type: string, required: false, description: 'How far to look back, for example 24h.'}
      steps:
        - id: step-1
          tool: x_clusters_list_events
          args: {installation: '{{ .input.installation }}', window: '{{ .input.window }}'}
          store: true
        - id: step-2
          tool: x_clusters_list_events
          args: {installation: '{{ .input.installation }}', window: '{{ .input.window }}'}
          store: true
        - id: step-3
          tool: x_metrics_query_range
          args: {installation: '{{ .input.installation }}', window: '{{ .input.window }}'}
          store: true
  - name: forecast-node-pool-capacity-across-organizations
    labels: {fixture.invalid/verb: forecast}
    config:
      name: forecast-node-pool-capacity-across-organizations
      description: 'Runbook: Forecast node pool capacity across organizations.'
      args:
        installation: {type: string, required: true, description: The installation to run against.}
        window: {type: string, required: false, description: 'How far to look back, for example 24h.'}
      steps:
        - id: step-1
          tool: x_ledger_export_records
          args: {installation: '{{ .input.installation }}'}
          store: true
        - id: step-2
          tool: x_tickets_search_tickets
          args: {query: forecast node pool capacity}
          store: true
        - id: step-3
          tool: x_clusters_get_release
          args: {installation: '{{ .input.installation }}'}
          store: true
        - id: step-4
          tool: x_alerts_list_routes
          args: {installation: '{{ .input.installation }}'}
          store: true
        - id: step-5
          tool: x_runbook-library_list_recent_runs
          args: {runbook: forecast node pool capacity}
          store: true
  - name: forecast-node-pool-capacity-after-incident
    labels: {fixture.invalid/tier: gold, fixture.invalid/verb: forecast}
    config:
      name: forecast-node-pool-capacity-after-incident
      description: 'Runbook: Forecast node pool capacity after incident.'
      args:
        installation: {type: string, required: true, description: The installation to run against.}
        window: {type: string, required: false, description: 'How far to look back, for example 24h.'}
      steps:
        - id: step-1
          tool: x_alerts_list_routes
          args: {installation: '{{ .input.installation }}'}
          store: true
        - id: step-2
          tool: x_runbook-library_list_runbooks
          args: {alert: forecast node pool capacity}
          store: true
        - id: step-3
          tool: x_logs_count_lines
          args: {installation: '{{ .input.installation }}', window: '{{ .input.window }}'}
          store: true
  - name: forecast-node-pool-capacity-before-upgrade
    labels: {fixture.invalid/verb: forecast}
    config:
      name: forecast-node-pool-capacity-before-upgrade
      description: 'Runbook: Forecast node pool capacity before upgrade.'
      args:
        installation: {type: string, required: true, description: The installation to run against.}
        window: {type: string, required: false, description: 'How far to look back, for example 24h.'}
      steps:
        - id: step-1
          tool: x_clusters_get_cluster
          args: {installation: '{{ .input.installation }}'}
          store: true
        - id: step-2
          tool: x_metrics_query_instant
          args: {installation: '{{ .input.installation }}'}
          store: true
        - id: step-3
          tool: x_alerts_get_alert
          args: {installation: '{{ .input.installation }}'}
          store: true
        - id: step-4
          tool: x_ledger_get_statement
          args: {installation: '{{ .input.installation }}'}
          store: true
  - name: forecast-node-pool-capacity-for-the-week
    labels: {fixture.invalid/verb: forecast}
    config:
      name: forecast-node-pool-capacity-for-the-week
      description: 'Runbook: Forecast node pool capacity for the week.'
      args:
        installation: {type: string, required: true, description: The installation to run against.}
        window: {type: string, required: false, description: 'How far to look back, for example 24h.'}
      steps:
        - id: step-1
          tool: x_logs_get_retention
          args: {installation: '{{ .input.installation }}'}
          store: true
        - id: step-2
          tool: x_tickets_read_ticket
          args: {ticket: forecast node pool capacity}
          store: true
        - id: step-3
          tool: x_ledger_list_adjustments
          args: {installation: '{{ .input.installation }}'}
          store: true
  - name: forecast-quota-headroom-per-installation
    labels: {fixture.invalid/tier: gold, fixture.invalid/verb: forecast}
    config:
      name: forecast-quota-headroom-per-installation
      description: 'Runbook: Forecast quota headroom per installation.'
      args:
        installation: {type: string, required: true, description: The installation to run against.}
        window: {type: string, required: false, description: 'How far to look back, for example 24h.'}
      steps:
        - id: step-1
          tool: x_logs_get_retention
          args: {installation: '{{ .input.installation }}'}
          store: true
  - name: forecast-release-drift-per-installation
    labels: {fixture.invalid/verb: forecast}
    config:
      name: forecast-release-drift-per-installation
      description: 'Runbook: Forecast release drift per installation.'
      args:
        installation: {type: string, required: true, description: The installation to run against.}
        window: {type: string, required: false, description: 'How far to look back, for example 24h.'}
      steps:
        - id: step-1
          tool: x_logs_tail_stream
          args: {installation: '{{ .input.installation }}'}
          store: true
  - name: forecast-scrape-health-across-organizations
    labels: {fixture.invalid/verb: forecast}
    config:
      name: forecast-scrape-health-across-organizations
      description: 'Runbook: Forecast scrape health across organizations.'
      args:
        installation: {type: string, required: true, description: The installation to run against.}
        window: {type: string, required: false, description: 'How far to look back, for example 24h.'}
      steps:
        - id: step-1
          tool: x_metrics_list_series
          args: {installation: '{{ .input.installation }}'}
          store: true
        - id: step-2
          tool: x_ledger_export_records
          args: {installation: '{{ .input.installation }}'}
          store: true
        - id: step-3
          tool: x_clusters_get_release
          args: {installation: '{{ .input.installation }}'}
          store: true
        - id: step-4
          tool: x_tickets_list_tickets
          args: {queue: forecast scrape health}
          store: true
  - name: forecast-scrape-health-for-the-week
    labels: {fixture.invalid/tier: gold, fixture.invalid/verb: forecast}
    config:
      name: forecast-scrape-health-for-the-week
      description: 'Runbook: Forecast scrape health for the week.'
      args:
        installation: {type: string, required: true, description: The installation to run against.}
        window: {type: string, required: false, description: 'How far to look back, for example 24h.'}
      steps:
        - id: step-1
          tool: x_metrics_list_rules
          args: {installation: '{{ .input.installation }}'}
          store: true
        - id: step-2
          tool: x_alerts_list_alerts
          args: {installation: '{{ .input.installation }}'}
          store: true
        - id: step-3
          tool: x_tickets_list_tickets
          args: {queue: forecast scrape health}
          store: true
        - id: step-4
          tool: x_metrics_list_targets
          args: {installation: '{{ .input.installation }}'}
          store: true
  - name: forecast-scrape-health-per-installation
    labels: {fixture.invalid/verb: forecast}
    config:
      name: forecast-scrape-health-per-installation
      description: 'Runbook: Forecast scrape health per installation.'
      args:
        installation: {type: string, required: true, description: The installation to run against.}
        window: {type: string, required: false, description: 'How far to look back, for example 24h.'}
      steps:
        - id: step-1
          tool: x_alerts_list_silences
          args: {installation: '{{ .input.installation }}'}
          store: true
  - name: forecast-silence-hygiene-after-incident
    labels: {fixture.invalid/verb: forecast}
    config:
      name: forecast-silence-hygiene-after-incident
      description: 'Runbook: Forecast silence hygiene after incident.'
      args:
        installation: {type: string, required: true, description: The installation to run against.}
        window: {type: string, required: false, description: 'How far to look back, for example 24h.'}
      steps:
        - id: step-1
          tool: x_metrics_list_label_values
          args: {installation: '{{ .input.installation }}'}
          store: true
        - id: step-2
          tool: x_metrics_list_rules
          args: {installation: '{{ .input.installation }}'}
          store: true
  - name: forecast-silence-hygiene-for-the-week
    labels: {fixture.invalid/tier: gold, fixture.invalid/verb: forecast}
    config:
      name: forecast-silence-hygiene-for-the-week
      description: 'Runbook: Forecast silence hygiene for the week.'
      args:
        installation: {type: string, required: true, description: The installation to run against.}
        window: {type: string, required: false, description: 'How far to look back, for example 24h.'}
      steps:
        - id: step-1
          tool: x_logs_list_streams
          args: {installation: '{{ .input.installation }}'}
          store: true
        - id: step-2
          tool: x_runbook-library_list_recent_runs
          args: {runbook: forecast silence hygiene}
          store: true
  - name: forecast-upgrade-readiness-before-upgrade
    labels: {fixture.invalid/verb: forecast}
    config:
      name: forecast-upgrade-readiness-before-upgrade
      description: 'Runbook: Forecast upgrade readiness before upgrade.'
      args:
        installation: {type: string, required: true, description: The installation to run against.}
        window: {type: string, required: false, description: 'How far to look back, for example 24h.'}
      steps:
        - id: step-1
          tool: x_clusters_list_clusters
          args: {installation: '{{ .input.installation }}'}
          store: true
        - id: step-2
          tool: x_runbook-library_list_recent_runs
          args: {runbook: forecast upgrade readiness}
          store: true
  - name: forecast-upgrade-readiness-for-the-week
    labels: {fixture.invalid/verb: forecast}
    config:
      name: forecast-upgrade-readiness-for-the-week
      description: 'Runbook: Forecast upgrade readiness for the week.'
      args:
        installation: {type: string, required: true, description: The installation to run against.}
        window: {type: string, required: false, description: 'How far to look back, for example 24h.'}
      steps:
        - id: step-1
          tool: x_logs_detect_patterns
          args: {installation: '{{ .input.installation }}', window: '{{ .input.window }}'}
          store: true
        - id: step-2
          tool: x_runbook-library_read_runbook
          args: {runbook: forecast upgrade readiness}
          store: true
        - id: step-3
          tool: x_metrics_list_label_values
          args: {installation: '{{ .input.installation }}'}
          store: true
        - id: step-4
          tool: x_alerts_get_alert
          args: {installation: '{{ .input.installation }}'}
          store: true
  - name: forecast-usage-anomalies-across-organizations
    labels: {fixture.invalid/tier: gold, fixture.invalid/verb: forecast}
    config:
      name: forecast-usage-anomalies-across-organizations
      description: 'Runbook: Forecast usage anomalies across organizations.'
      args:
        installation: {type: string, required: true, description: The installation to run against.}
        window: {type: string, required: false, description: 'How far to look back, for example 24h.'}
      steps:
        - id: step-1
          tool: x_clusters_get_cluster
          args: {installation: '{{ .input.installation }}'}
          store: true
        - id: step-2
          tool: x_clusters_get_cluster
          args: {installation: '{{ .input.installation }}'}
          store: true
  - name: forecast-usage-anomalies-on-request
    labels: {fixture.invalid/verb: forecast}
    config:
      name: forecast-usage-anomalies-on-request
      description: 'Runbook: Forecast usage anomalies on request.'
      args:
        installation: {type: string, required: true, description: The installation to run against.}
        window: {type: string, required: false, description: 'How far to look back, for example 24h.'}
      steps:
        - id: step-1
          tool: x_alerts_list_silences
          args: {installation: '{{ .input.installation }}'}
          store: true
        - id: step-2
          tool: x_handbook_read_page
          args: {path: forecast usage anomalies}
          store: true
  - name: forecast-usage-anomalies-per-installation
    labels: {fixture.invalid/verb: forecast}
    config:
      name: forecast-usage-anomalies-per-installation
      description: 'Runbook: Forecast usage anomalies per installation.'
      args:
        installation: {type: string, required: true, description: The installation to run against.}
        window: {type: string, required: false, description: 'How far to look back, for example 24h.'}
      steps:
        - id: step-1
          tool: x_metrics_list_rules
          args: {installation: '{{ .input.installation }}'}
          store: true
        - id: step-2
          tool: x_ledger_export_records
          args: {installation: '{{ .input.installation }}'}
          store: true
        - id: step-3
          tool: x_clusters_list_events
          args: {installation: '{{ .input.installation }}', window: '{{ .input.window }}'}
          store: true
  - name: inspect-certificate-expiry-across-organizations
    labels: {fixture.invalid/tier: gold, fixture.invalid/verb: inspect}
    config:
      name: inspect-certificate-expiry-across-organizations
      description: 'Runbook: Inspect certificate expiry across organizations.'
      args:
        installation: {type: string, required: true, description: The installation to run against.}
        window: {type: string, required: false, description: 'How far to look back, for example 24h.'}
      steps:
        - id: step-1
          tool: x_clusters_list_events
          args: {installation: '{{ .input.installation }}', window: '{{ .input.window }}'}
          store: true
  - name: inspect-certificate-expiry-per-installation
    labels: {fixture.invalid/verb: inspect}
    config:
      name: inspect-certificate-expiry-per-installation
      description: 'Runbook: Inspect certificate expiry per installation.'
      args:
        installation: {type: string, required: true, description: The installation to run against.}
        window: {type: string, required: false, description: 'How far to look back, for example 24h.'}
      steps:
        - id: step-1
          tool: x_metrics_explain_series
          args: {installation: '{{ .input.installation }}'}
          store: true
        - id: step-2
          tool: x_logs_list_streams
          args: {installation: '{{ .input.installation }}'}
          store: true
        - id: step-3
          tool: x_tickets_search_tickets
          args: {query: inspect certificate expiry}
          store: true
        - id: step-4
          tool: x_metrics_query_range
          args: {installation: '{{ .input.installation }}', window: '{{ .input.window }}'}
          store: true
  - name: inspect-ingress-errors-after-incident
    labels: {fixture.invalid/verb: inspect}
    config:
      name: inspect-ingress-errors-after-incident
      description: 'Runbook: Inspect ingress errors after incident.'
      args:
        installation: {type: string, required: true, description: The installation to run against.}
        window: {type: string, required: false, description: 'How far to look back, for example 24h.'}
      steps:
        - id: step-1
          tool: x_clusters_list_events
          args: {installation: '{{ .input.installation }}', window: '{{ .input.window }}'}
          store: true
        - id: step-2
          tool: x_metrics_query_instant
          args: {installation: '{{ .input.installation }}'}
          store: true
        - id: step-3
          tool: x_tickets_search_tickets
          args: {query: inspect ingress errors}
          store: true
  - name: inspect-log-volume-after-incident
    labels: {fixture.invalid/tier: gold, fixture.invalid/verb: inspect}
    config:
      name: inspect-log-volume-after-incident
      description: 'Runbook: Inspect log volume after incident.'
      args:
        installation: {type: string, required: true, description: The installation to run against.}
        window: {type: string, required: false, description: 'How far to look back, for example 24h.'}
      steps:
        - id: step-1
          tool: x_metrics_query_range
          args: {installation: '{{ .input.installation }}', window: '{{ .input.window }}'}
          store: true
        - id: step-2
          tool: x_logs_list_streams
          args: {installation: '{{ .input.installation }}'}
          store: true
        - id: step-3
          tool: x_runbook-library_search_runbooks
          args: {query: inspect log volume}
          store: true
        - id: step-4
          tool: x_ledger_get_statement
          args: {installation: '{{ .input.installation }}'}
          store: true
        - id: step-5
          tool: x_clusters_list_upgrades
          args: {installation: '{{ .input.installation }}', window: '{{ .input.window }}'}
          store: true
  - name: inspect-node-pool-capacity-across-organizations
    labels: {fixture.invalid/verb: inspect}
    config:
      name: inspect-node-pool-capacity-across-organizations
      description: 'Runbook: Inspect node pool capacity across organizations.'
      args:
        installation: {type: string, required: true, description: The installation to run against.}
        window: {type: string, required: false, description: 'How far to look back, for example 24h.'}
      steps:
        - id: step-1
          tool: x_clusters_list_upgrades
          args: {installation: '{{ .input.installation }}', window: '{{ .input.window }}'}
          store: true
        - id: step-2
          tool: x_handbook_list_sections
          args: {path: inspect node pool capacity}
          store: true
        - id: step-3
          tool: x_logs_count_lines
          args: {installation: '{{ .input.installation }}', window: '{{ .input.window }}'}
          store: true
        - id: step-4
          tool: x_runbook-library_search_runbooks
          args: {query: inspect node pool capacity}
          store: true
  - name: inspect-node-pool-capacity-after-incident
    labels: {fixture.invalid/verb: inspect}
    config:
      name: inspect-node-pool-capacity-after-incident
      description: 'Runbook: Inspect node pool capacity after incident.'
      args:
        installation: {type: string, required: true, description: The installation to run against.}
        window: {type: string, required: false, description: 'How far to look back, for example 24h.'}
      steps:
        - id: step-1
          tool: x_logs_detect_patterns
          args: {installation: '{{ .input.installation }}', window: '{{ .input.window }}'}
          store: true
        - id: step-2
          tool: x_logs_get_retention
          args: {installation: '{{ .input.installation }}'}
          store: true
        - id: step-3
          tool: x_tickets_read_ticket
          args: {ticket: inspect node pool capacity}
          store: true
        - id: step-4
          tool: x_metrics_list_label_values
          args: {installation: '{{ .input.installation }}'}
          store: true
        - id: step-5
          tool: x_metrics_list_rules
          args: {installation: '{{ .input.installation }}'}
          store: true
  - name: inspect-node-pool-capacity-before-upgrade
    labels: {fixture.invalid/tier: gold, fixture.invalid/verb: inspect}
    config:
      name: inspect-node-pool-capacity-before-upgrade
      description: 'Runbook: Inspect node pool capacity before upgrade.'
      args:
        installation: {type: string, required: true, description: The installation to run against.}
        window: {type: string, required: false, description: 'How far to look back, for example 24h.'}
      steps:
        - id: step-1
          tool: x_alerts_list_silences
          args: {installation: '{{ .input.installation }}'}
          store: true
        - id: step-2
          tool: x_logs_tail_stream
          args: {installation: '{{ .input.installation }}'}
          store: true
  - name: inspect-node-pool-capacity-for-the-week
    labels: {fixture.invalid/verb: inspect}
    config:
      name: inspect-node-pool-capacity-for-the-week
      description: 'Runbook: Inspect node pool capacity for the week.'
      args:
        installation: {type: string, required: true, description: The installation to run against.}
        window: {type: string, required: false, description: 'How far to look back, for example 24h.'}
      steps:
        - id: step-1
          tool: x_clusters_get_release
          args: {installation: '{{ .input.installation }}'}
          store: true
        - id: step-2
          tool: x_alerts_list_routes
          args: {installation: '{{ .input.installation }}'}
          store: true
        - id: step-3
          tool: x_clusters_get_release
          args: {installation: '{{ .input.installation }}'}
          store: true
  - name: inspect-node-pool-capacity-on-request
    labels: {fixture.invalid/verb: inspect}
    config:
      name: inspect-node-pool-capacity-on-request
      description: 'Runbook: Inspect node pool capacity on request.'
      args:
        installation: {type: string, required: true, description: The installation to run against.}
        window: {type: string, required: false, description: 'How far to look back, for example 24h.'}
      steps:
        - id: step-1
          tool: x_ledger_list_adjustments
          args: {installation: '{{ .input.installation }}'}
          store: true
        - id: step-2
          tool: x_clusters_list_clusters
          args: {installation: '{{ .input.installation }}'}
          store: true
        - id: step-3
          tool: x_metrics_list_label_values
          args: {installation: '{{ .input.installation }}'}
          store: true
        - id: step-4
          tool: x_tickets_search_tickets
          args: {query: inspect node pool capacity}
          store: true
        - id: step-5
          tool: x_logs_list_streams
          args: {installation: '{{ .input.installation }}'}
          store: true
  - name: inspect-quota-headroom-across-organizations
    labels: {fixture.invalid/tier: gold, fixture.invalid/verb: inspect}
    config:
      name: inspect-quota-headroom-across-organizations
      description: 'Runbook: Inspect quota headroom across organizations.'
      args:
        installation: {type: string, required: true, description: The installation to run against.}
        window: {type: string, required: false, description: 'How far to look back, for example 24h.'}
      steps:
        - id: step-1
          tool: x_alerts_get_alert
          args: {installation: '{{ .input.installation }}'}
          store: true
  - name: inspect-release-drift-after-incident
    labels: {fixture.invalid/verb: inspect}
    config:
      name: inspect-release-drift-after-incident
      description: 'Runbook: Inspect release drift after incident.'
      args:
        installation: {type: string, required: true, description: The installation to run against.}
        window: {type: string, required: false, description: 'How far to look back, for example 24h.'}
      steps:
        - id: step-1
          tool: x_metrics_query_instant
          args: {installation: '{{ .input.installation }}'}
          store: true
        - id: step-2
          tool: x_alerts_list_routes
          args: {installation: '{{ .input.installation }}'}
          store: true
        - id: step-3
          tool: x_logs_tail_stream
          args: {installation: '{{ .input.installation }}'}
          store: true
        - id: step-4
          tool: x_metrics_explain_series
          args: {installation: '{{ .input.installation }}'}
          store: true
        - id: step-5
          tool: x_clusters_get_quota
          args: {installation: '{{ .input.installation }}'}
          store: true
  - name: inspect-release-drift-before-upgrade
    labels: {fixture.invalid/verb: inspect}
    config:
      name: inspect-release-drift-before-upgrade
      description: 'Runbook: Inspect release drift before upgrade.'
      args:
        installation: {type: string, required: true, description: The installation to run against.}
        window: {type: string, required: false, description: 'How far to look back, for example 24h.'}
      steps:
        - id: step-1
          tool: x_logs_count_lines
          args: {installation: '{{ .input.installation }}', window: '{{ .input.window }}'}
          store: true
  - name: inspect-retention-gaps-across-organizations
    labels: {fixture.invalid/tier: gold, fixture.invalid/verb: inspect}
    config:
      name: inspect-retention-gaps-across-organizations
      description: 'Runbook: Inspect retention gaps across organizations.'
      args:
        installation: {type: string, required: true, description: The installation to run against.}
        window: {type: string, required: false, description: 'How far to look back, for example 24h.'}
      steps:
        - id: step-1
          tool: x_metrics_query_range
          args: {installation: '{{ .input.installation }}', window: '{{ .input.window }}'}
          store: true
        - id: step-2
          tool: x_ledger_list_adjustments
          args: {installation: '{{ .input.installation }}'}
          store: true
        - id: step-3
          tool: x_runbook-library_search_runbooks
          args: {query: inspect retention gaps}
          store: true
  - name: inspect-retention-gaps-before-upgrade
    labels: {fixture.invalid/verb: inspect}
    config:
      name: inspect-retention-gaps-before-upgrade
      description: 'Runbook: Inspect retention gaps before upgrade.'
      args:
        installation: {type: string, required: true, description: The installation to run against.}
        window: {type: string, required: false, description: 'How far to look back, for example 24h.'}
      steps:
        - id: step-1
          tool: x_metrics_list_targets
          args: {installation: '{{ .input.installation }}'}
          store: true
        - id: step-2
          tool: x_runbook-library_list_runbooks
          args: {alert: inspect retention gaps}
          store: true
        - id: step-3
          tool: x_clusters_list_upgrades
          args: {installation: '{{ .input.installation }}', window: '{{ .input.window }}'}
          store: true
        - id: step-4
          tool: x_clusters_list_upgrades
          args: {installation: '{{ .input.installation }}', window: '{{ .input.window }}'}
          store: true
  - name: inspect-retention-gaps-for-the-week
    labels: {fixture.invalid/verb: inspect}
    config:
      name: inspect-retention-gaps-for-the-week
      description: 'Runbook: Inspect retention gaps for the week.'
      args:
        installation: {type: string, required: true, description: The installation to run against.}
        window: {type: string, required: false, description: 'How far to look back, for example 24h.'}
      steps:
        - id: step-1
          tool: x_metrics_list_targets
          args: {installation: '{{ .input.installation }}'}
          store: true
        - id: step-2
          tool: x_runbook-library_list_recent_runs
          args: {runbook: inspect retention gaps}
          store: true
        - id: step-3
          tool: x_logs_query_logs
          args: {installation: '{{ .input.installation }}', window: '{{ .input.window }}'}
          store: true
        - id: step-4
          tool: x_ledger_list_adjustments
          args: {installation: '{{ .input.installation }}'}
          store: true
        - id: step-5
          tool: x_logs_list_streams
          args: {installation: '{{ .input.installation }}'}
          store: true
  - name: inspect-silence-hygiene-across-organizations
    labels: {fixture.invalid/tier: gold, fixture.invalid/verb: inspect}
    config:
      name: inspect-silence-hygiene-across-organizations
      description: 'Runbook: Inspect silence hygiene across organizations.'
      args:
        installation: {type: string, required: true, description: The installation to run against.}
        window: {type: string, required: false, description: 'How far to look back, for example 24h.'}
      steps:
        - id: step-1
          tool: x_logs_count_lines
          args: {installation: '{{ .input.installation }}', window: '{{ .input.window }}'}
          store: true
        - id: step-2
          tool: x_metrics_explain_series
          args: {installation: '{{ .input.installation }}'}
          store: true
        - id: step-3
          tool: x_clusters_list_node_pools
          args: {installation: '{{ .input.installation }}'}
          store: true
        - id: step-4
          tool: x_handbook_list_sections
          args: {path: inspect silence hygiene}
          store: true
        - id: step-5
          tool: x_metrics_list_targets
          args: {installation: '{{ .input.installation }}'}
          store: true
  - name: inspect-silence-hygiene-before-upgrade
    labels: {fixture.invalid/verb: inspect}
    config:
      name: inspect-silence-hygiene-before-upgrade
      description: 'Runbook: Inspect silence hygiene before upgrade.'
      args:
        installation: {type: string, required: true, description: The installation to run against.}
        window: {type: string, required: false, description: 'How far to look back, for example 24h.'}
      steps:
        - id: step-1
          tool: x_logs_get_retention
          args: {installation: '{{ .input.installation }}'}
          store: true
        - id: step-2
          tool: x_runbook-library_list_recent_runs
          args: {runbook: inspect silence hygiene}
          store: true
        - id: step-3
          tool: x_alerts_list_routes
          args: {installation: '{{ .input.installation }}'}
          store: true
        - id: step-4
          tool: x_metrics_explain_series
          args: {installation: '{{ .input.installation }}'}
          store: true
        - id: step-5
          tool: x_logs_query_logs
          args: {installation: '{{ .input.installation }}', window: '{{ .input.window }}'}
          store: true
  - name: inspect-silence-hygiene-on-request
    labels: {fixture.invalid/verb: inspect}
    config:
      name: inspect-silence-hygiene-on-request
      description: 'Runbook: Inspect silence hygiene on request.'
      args:
        installation: {type: string, required: true, description: The installation to run against.}
        window: {type: string, required: false, description: 'How far to look back, for example 24h.'}
      steps:
        - id: step-1
          tool: x_metrics_explain_series
          args: {installation: '{{ .input.installation }}'}
          store: true
  - name: inspect-upgrade-readiness-across-organizations
    labels: {fixture.invalid/tier: gold, fixture.invalid/verb: inspect}
    config:
      name: inspect-upgrade-readiness-across-organizations
      description: 'Runbook: Inspect upgrade readiness across organizations.'
      args:
        installation: {type: string, required: true, description: The installation to run against.}
        window: {type: string, required: false, description: 'How far to look back, for example 24h.'}
      steps:
        - id: step-1
          tool: x_logs_query_logs
          args: {installation: '{{ .input.installation }}', window: '{{ .input.window }}'}
          store: true
        - id: step-2
          tool: x_alerts_list_alerts
          args: {installation: '{{ .input.installation }}'}
          store: true
        - id: step-3
          tool: x_runbook-library_search_runbooks
          args: {query: inspect upgrade readiness}
          store: true
  - name: inspect-usage-anomalies-before-upgrade
    labels: {fixture.invalid/verb: inspect}
    config:
      name: inspect-usage-anomalies-before-upgrade
      description: 'Runbook: Inspect usage anomalies before upgrade.'
      args:
        installation: {type: string, required: true, description: The installation to run against.}
        window: {type: string, required: false, description: 'How far to look back, for example 24h.'}
      steps:
        - id: step-1
          tool: x_logs_count_lines
          args: {installation: '{{ .input.installation }}', window: '{{ .input.window }}'}
          store: true
        - id: step-2
          tool: x_metrics_query_range
          args: {installation: '{{ .input.installation }}', window: '{{ .input.window }}'}
          store: true
  - name: reconcile-alert-noise-across-organizations
    labels: {fixture.invalid/verb: reconcile}
    config:
      name: reconcile-alert-noise-across-organizations
      description: 'Runbook: Reconcile alert noise across organizations.'
      args:
        installation: {type: string, required: true, description: The installation to run against.}
        window: {type: string, required: false, description: 'How far to look back, for example 24h.'}
      steps:
        - id: step-1
          tool: x_logs_get_retention
          args: {installation: '{{ .input.installation }}'}
          store: true
        - id: step-2
          tool: x_ledger_get_statement
          args: {installation: '{{ .input.installation }}'}
          store: true
        - id: step-3
          tool: x_handbook_list_sections
          args: {path: reconcile alert noise}
          store: true
        - id: step-4
          tool: x_metrics_list_rules
          args: {installation: '{{ .input.installation }}'}
          store: true
  - name: reconcile-alert-noise-before-upgrade
    labels: {fixture.invalid/tier: gold, fixture.invalid/verb: reconcile}
    config:
      name: reconcile-alert-noise-before-upgrade
      description: 'Runbook: Reconcile alert noise before upgrade.'
      args:
        installation: {type: string, required: true, description: The installation to run against.}
        window: {type: string, required: false, description: 'How far to look back, for example 24h.'}
      steps:
        - id: step-1
          tool: x_metrics_list_series
          args: {installation: '{{ .input.installation }}'}
          store: true
        - id: step-2
          tool: x_ledger_export_records
          args: {installation: '{{ .input.installation }}'}
          store: true
        - id: step-3
          tool: x_metrics_list_label_values
          args: {installation: '{{ .input.installation }}'}
          store: true
        - id: step-4
          tool: x_ledger_list_usage
          args: {installation: '{{ .input.installation }}', window: '{{ .input.window }}'}
          store: true
  - name: reconcile-alert-noise-on-request
    labels: {fixture.invalid/verb: reconcile}
    config:
      name: reconcile-alert-noise-on-request
      description: 'Runbook: Reconcile alert noise on request.'
      args:
        installation: {type: string, required: true, description: The installation to run against.}
        window: {type: string, required: false, description: 'How far to look back, for example 24h.'}
      steps:
        - id: step-1
          tool: x_clusters_get_release
          args: {installation: '{{ .input.installation }}'}
          store: true
        - id: step-2
          tool: x_ledger_export_records
          args: {installation: '{{ .input.installation }}'}
          store: true
        - id: step-3
          tool: x_clusters_get_release
          args: {installation: '{{ .input.installation }}'}
          store: true
        - id: step-4
          tool: x_logs_tail_stream
          args: {installation: '{{ .input.installation }}'}
          store: true
        - id: step-5
          tool: x_handbook_list_sections
          args: {path: reconcile alert noise}
          store: true
  - name: reconcile-certificate-expiry-across-organizations
    labels: {fixture.invalid/verb: reconcile}
    config:
      name: reconcile-certificate-expiry-across-organizations
      description: 'Runbook: Reconcile certificate expiry across organizations.'
      args:
        installation: {type: string, required: true, description: The installation to run against.}
        window: {type: string, required: false, description: 'How far to look back, for example 24h.'}
      steps:
        - id: step-1
          tool: x_clusters_get_cluster
          args: {installation: '{{ .input.installation }}'}
          store: true
  - name: reconcile-certificate-expiry-before-upgrade
    labels: {fixture.invalid/tier: gold, fixture.invalid/verb: reconcile}
    config:
      name: reconcile-certificate-expiry-before-upgrade
      description: 'Runbook: Reconcile certificate expiry before upgrade.'
      args:
        installation: {type: string, required: true, description: The installation to run against.}
        window: {type: string, required: false, description: 'How far to look back, for example 24h.'}
      steps:
        - id: step-1
          tool: x_clusters_list_clusters
          args: {installation: '{{ .input.installation }}'}
          store: true
        - id: step-2
          tool: x_tickets_list_tickets
          args: {queue: reconcile certificate expiry}
          store: true
  - name: reconcile-certificate-expiry-for-the-week
    labels: {fixture.invalid/verb: reconcile}
    config:
      name: reconcile-certificate-expiry-for-the-week
      description: 'Runbook: Reconcile certificate expiry for the week.'
      args:
        installation: {type: string, required: true, description: The installation to run against.}
        window: {type: string, required: false, description: 'How far to look back, for example 24h.'}
      steps:
        - id: step-1
          tool: x_alerts_list_silences
          args: {installation: '{{ .input.installation }}'}
          store: true
        - id: step-2
          tool: x_runbook-library_list_recent_runs
          args: {runbook: reconcile certificate expiry}
          store: true
        - id: step-3
          tool: x_handbook_read_page
          args: {path: reconcile certificate expiry}
          store: true
        - id: step-4
          tool: x_clusters_list_events
          args: {installation: '{{ .input.installation }}', window: '{{ .input.window }}'}
          store: true
        - id: step-5
          tool: x_logs_list_streams
          args: {installation: '{{ .input.installation }}'}
          store: true
  - name: reconcile-certificate-expiry-per-installation
    labels: {fixture.invalid/verb: reconcile}
    config:
      name: reconcile-certificate-expiry-per-installation
      description: 'Runbook: Reconcile certificate expiry per installation.'
      args:
        installation: {type: string, required: true, description: The installation to run against.}
        window: {type: string, required: false, description: 'How far to look back, for example 24h.'}
      steps:
        - id: step-1
          tool: x_logs_query_logs
          args: {installation: '{{ .input.installation }}', window: '{{ .input.window }}'}
          store: true
        - id: step-2
          tool: x_runbook-library_search_runbooks
          args: {query: reconcile certificate expiry}
          store: true
        - id: step-3
          tool: x_metrics_list_label_values
          args: {installation: '{{ .input.installation }}'}
          store: true
        - id: step-4
          tool: x_metrics_list_label_values
          args: {installation: '{{ .input.installation }}'}
          store: true
  - name: reconcile-ingress-errors-on-request
    labels: {fixture.invalid/tier: gold, fixture.invalid/verb: reconcile}
    config:
      name: reconcile-ingress-errors-on-request
      description: 'Runbook: Reconcile ingress errors on request.'
      args:
        installation: {type: string, required: true, description: The installation to run against.}
        window: {type: string, required: false, description: 'How far to look back, for example 24h.'}
      steps:
        - id: step-1
          tool: x_alerts_list_alerts
          args: {installation: '{{ .input.installation }}'}
          store: true
        - id: step-2
          tool: x_handbook_list_recent_changes
          args: {window: reconcile ingress errors}
          store: true
        - id: step-3
          tool: x_ledger_list_adjustments
          args: {installation: '{{ .input.installation }}'}
          store: true
        - id: step-4
          tool: x_clusters_get_quota
          args: {installation: '{{ .input.installation }}'}
          store: true
        - id: step-5
          tool: x_tickets_read_ticket
          args: {ticket: reconcile ingress errors}
          store: true
  - name: reconcile-log-volume-after-incident
    labels: {fixture.invalid/verb: reconcile}
    config:
      name: reconcile-log-volume-after-incident
      description: 'Runbook: Reconcile log volume after incident.'
      args:
        installation: {type: string, required: true, description: The installation to run against.}
        window: {type: string, required: false, description: 'How far to look back, for example 24h.'}
      steps:
        - id: step-1
          tool: x_clusters_list_node_pools
          args: {installation: '{{ .input.installation }}'}
          store: true
        - id: step-2
          tool: x_tickets_list_tickets
          args: {queue: reconcile log volume}
          store: true
  - name: reconcile-log-volume-for-the-week
    labels: {fixture.invalid/verb: reconcile}
    config:
      name: reconcile-log-volume-for-the-week
      description: 'Runbook: Reconcile log volume for the week.'
      args:
        installation: {type: string, required: true, description: The installation to run against.}
        window: {type: string, required: false, description: 'How far to look back, for example 24h.'}
      steps:
        - id: step-1
          tool: x_logs_get_retention
          args: {installation: '{{ .input.installation }}'}
          store: true
  - name: reconcile-log-volume-on-request
    labels: {fixture.invalid/tier: gold, fixture.invalid/verb: reconcile}
    config:
      name: reconcile-log-volume-on-request
      description: 'Runbook: Reconcile log volume on request.'
      args:
        installation: {type: string, required: true, description: The installation to run against.}
        window: {type: string, required: false, description: 'How far to look back, for example 24h.'}
      steps:
        - id: step-1
          tool: x_logs_detect_patterns
          args: {installation: '{{ .input.installation }}', window: '{{ .input.window }}'}
          store: true
        - id: step-2
          tool: x_clusters_list_clusters
          args: {installation: '{{ .input.installation }}'}
          store: true
        - id: step-3
          tool: x_metrics_list_series
          args: {installation: '{{ .input.installation }}'}
          store: true
        - id: step-4
          tool: x_metrics_list_series
          args: {installation: '{{ .input.installation }}'}
          store: true
  - name: reconcile-node-pool-capacity-across-organizations
    labels: {fixture.invalid/verb: reconcile}
    config:
      name: reconcile-node-pool-capacity-across-organizations
      description: 'Runbook: Reconcile node pool capacity across organizations.'
      args:
        installation: {type: string, required: true, description: The installation to run against.}
        window: {type: string, required: false, description: 'How far to look back, for example 24h.'}
      steps:
        - id: step-1
          tool: x_logs_count_lines
          args: {installation: '{{ .input.installation }}', window: '{{ .input.window }}'}
          store: true
        - id: step-2
          tool: x_logs_query_logs
          args: {installation: '{{ .input.installation }}', window: '{{ .input.window }}'}
          store: true
        - id: step-3
          tool: x_ledger_list_usage
          args: {installation: '{{ .input.installation }}', window: '{{ .input.window }}'}
          store: true
        - id: step-4
          tool: x_handbook_list_recent_changes
          args: {window: reconcile node pool capacity}
          store: true
  - name: reconcile-node-pool-capacity-after-incident
    labels: {fixture.invalid/verb: reconcile}
    config:
      name: reconcile-node-pool-capacity-after-incident
      description: 'Runbook: Reconcile node pool capacity after incident.'
      args:
        installation: {type: string, required: true, description: The installation to run against.}
        window: {type: string, required: false, description: 'How far to look back, for example 24h.'}
      steps:
        - id: step-1
          tool: x_clusters_get_release
          args: {installation: '{{ .input.installation }}'}
          store: true
        - id: step-2
          tool: x_clusters_list_clusters
          args: {installation: '{{ .input.installation }}'}
          store: true
  - name: reconcile-node-pool-capacity-per-installation
    labels: {fixture.invalid/tier: gold, fixture.invalid/verb: reconcile}
    config:
      name: reconcile-node-pool-capacity-per-installation
      description: 'Runbook: Reconcile node pool capacity per installation.'
      args:
        installation: {type: string, required: true, description: The installation to run against.}
        window: {type: string, required: false, description: 'How far to look back, for example 24h.'}
      steps:
        - id: step-1
          tool: x_metrics_list_series
          args: {installation: '{{ .input.installation }}'}
          store: true
        - id: step-2
          tool: x_metrics_query_instant
          args: {installation: '{{ .input.installation }}'}
          store: true
        - id: step-3
          tool: x_runbook-library_read_runbook
          args: {runbook: reconcile node pool capacity}
          store: true
        - id: step-4
          tool: x_clusters_get_quota
          args: {installation: '{{ .input.installation }}'}
          store: true
        - id: step-5
          tool: x_ledger_get_statement
          args: {installation: '{{ .input.installation }}'}
          store: true
  - name: reconcile-quota-headroom-across-organizations
    labels: {fixture.invalid/verb: reconcile}
    config:
      name: reconcile-quota-headroom-across-organizations
      description: 'Runbook: Reconcile quota headroom across organizations.'
      args:
        installation: {type: string, required: true, description: The installation to run against.}
        window: {type: string, required: false, description: 'How far to look back, for example 24h.'}
      steps:
        - id: step-1
          tool: x_logs_get_retention
          args: {installation: '{{ .input.installation }}'}
          store: true
  - name: reconcile-quota-headroom-after-incident
    labels: {fixture.invalid/verb: reconcile}
    config:
      name: reconcile-quota-headroom-after-incident
      description: 'Runbook: Reconcile quota headroom after incident.'
      args:
        installation: {type: string, required: true, description: The installation to run against.}
        window: {type: string, required: false, description: 'How far to look back, for example 24h.'}
      steps:
        - id: step-1
          tool: x_logs_detect_patterns
          args: {installation: '{{ .input.installation }}', window: '{{ .input.window }}'}
          store: true
        - id: step-2
          tool: x_tickets_read_ticket
          args: {ticket: reconcile quota headroom}
          store: true
        - id: step-3
          tool: x_metrics_query_instant
          args: {installation: '{{ .input.installation }}'}
          store: true
  - name: reconcile-quota-headroom-before-upgrade
    labels: {fixture.invalid/tier: gold, fixture.invalid/verb: reconcile}
    config:
      name: reconcile-quota-headroom-before-upgrade
      description: 'Runbook: Reconcile quota headroom before upgrade.'
      args:
        installation: {type: string, required: true, description: The installation to run against.}
        window: {type: string, required: false, description: 'How far to look back, for example 24h.'}
      steps:
        - id: step-1
          tool: x_alerts_list_silences
          args: {installation: '{{ .input.installation }}'}
          store: true
        - id: step-2
          tool: x_ledger_get_statement
          args: {installation: '{{ .input.installation }}'}
          store: true
  - name: reconcile-quota-headroom-on-request
    labels: {fixture.invalid/verb: reconcile}
    config:
      name: reconcile-quota-headroom-on-request
      description: 'Runbook: Reconcile quota headroom on request.'
      args:
        installation: {type: string, required: true, description: The installation to run against.}
        window: {type: string, required: false, description: 'How far to look back, for example 24h.'}
      steps:
        - id: step-1
          tool: x_ledger_list_adjustments
          args: {installation: '{{ .input.installation }}'}
          store: true
        - id: step-2
          tool: x_alerts_list_alerts
          args: {installation: '{{ .input.installation }}'}
          store: true
        - id: step-3
          tool: x_tickets_search_tickets
          args: {query: reconcile quota headroom}
          store: true
  - name: reconcile-quota-headroom-per-installation
    labels: {fixture.invalid/verb: reconcile}
    config:
      name: reconcile-quota-headroom-per-installation
      description: 'Runbook: Reconcile quota headroom per installation.'
      args:
        installation: {type: string, required: true, description: The installation to run against.}
        window: {type: string, required: false, description: 'How far to look back, for example 24h.'}
      steps:
        - id: step-1
          tool: x_metrics_query_instant
          args: {installation: '{{ .input.installation }}'}
          store: true
        - id: step-2
          tool: x_metrics_list_series
          args: {installation: '{{ .input.installation }}'}
          store: true
  - name: reconcile-release-drift-before-upgrade
    labels: {fixture.invalid/tier: gold, fixture.invalid/verb: reconcile}
    config:
      name: reconcile-release-drift-before-upgrade
      description: 'Runbook: Reconcile release drift before upgrade.'
      args:
        installation: {type: string, required: true, description: The installation to run against.}
        window: {type: string, required: false, description: 'How far to look back, for example 24h.'}
      steps:
        - id: step-1
          tool: x_logs_count_lines
          args: {installation: '{{ .input.installation }}', window: '{{ .input.window }}'}
          store: true
        - id: step-2
          tool: x_metrics_query_range
          args: {installation: '{{ .input.installation }}', window: '{{ .input.window }}'}
          store: true
        - id: step-3
          tool: x_logs_list_streams
          args: {installation: '{{ .input.installation }}'}
          store: true
        - id: step-4
          tool: x_alerts_list_alerts
          args: {installation: '{{ .input.installation }}'}
          store: true
  - name: reconcile-release-drift-on-request
    labels: {fixture.invalid/verb: reconcile}
    config:
      name: reconcile-release-drift-on-request
      description: 'Runbook: Reconcile release drift on request.'
      args:
        installation: {type: string, required: true, description: The installation to run against.}
        window: {type: string, required: false, description: 'How far to look back, for example 24h.'}
      steps:
        - id: step-1
          tool: x_clusters_list_node_pools
          args: {installation: '{{ .input.installation }}'}
          store: true
        - id: step-2
          tool: x_clusters_list_node_pools
          args: {installation: '{{ .input.installation }}'}
          store: true
  - name: reconcile-release-drift-per-installation
    labels: {fixture.invalid/verb: reconcile}
    config:
      name: reconcile-release-drift-per-installation
      description: 'Runbook: Reconcile release drift per installation.'
      args:
        installation: {type: string, required: true, description: The installation to run against.}
        window: {type: string, required: false, description: 'How far to look back, for example 24h.'}
      steps:
        - id: step-1
          tool: x_metrics_explain_series
          args: {installation: '{{ .input.installation }}'}
          store: true
  - name: reconcile-retention-gaps-across-organizations
    labels: {fixture.invalid/tier: gold, fixture.invalid/verb: reconcile}
    config:
      name: reconcile-retention-gaps-across-organizations
      description: 'Runbook: Reconcile retention gaps across organizations.'
      args:
        installation: {type: string, required: true, description: The installation to run against.}
        window: {type: string, required: false, description: 'How far to look back, for example 24h.'}
      steps:
        - id: step-1
          tool: x_alerts_list_silences
          args: {installation: '{{ .input.installation }}'}
          store: true
        - id: step-2
          tool: x_metrics_explain_series
          args: {installation: '{{ .input.installation }}'}
          store: true
        - id: step-3
          tool: x_alerts_list_silences
          args: {installation: '{{ .input.installation }}'}
          store: true
  - name: reconcile-retention-gaps-for-the-week
    labels: {fixture.invalid/verb: reconcile}
    config:
      name: reconcile-retention-gaps-for-the-week
      description: 'Runbook: Reconcile retention gaps for the week.'
      args:
        installation: {type: string, required: true, description: The installation to run against.}
        window: {type: string, required: false, description: 'How far to look back, for example 24h.'}
      steps:
        - id: step-1
          tool: x_clusters_list_clusters
          args: {installation: '{{ .input.installation }}'}
          store: true
        - id: step-2
          tool: x_metrics_list_rules
          args: {installation: '{{ .input.installation }}'}
          store: true
        - id: step-3
          tool: x_clusters_list_node_pools
          args: {installation: '{{ .input.installation }}'}
          store: true
  - name: reconcile-retention-gaps-per-installation
    labels: {fixture.invalid/verb: reconcile}
    config:
      name: reconcile-retention-gaps-per-installation
      description: 'Runbook: Reconcile retention gaps per installation.'
      args:
        installation: {type: string, required: true, description: The installation to run against.}
        window: {type: string, required: false, description: 'How far to look back, for example 24h.'}
      steps:
        - id: step-1
          tool: x_metrics_query_range
          args: {installation: '{{ .input.installation }}', window: '{{ .input.window }}'}
          store: true
  - name: reconcile-scrape-health-before-upgrade
    labels: {fixture.invalid/tier: gold, fixture.invalid/verb: reconcile}
    config:
      name: reconcile-scrape-health-before-upgrade
      description: 'Runbook: Reconcile scrape health before upgrade.'
      args:
        installation: {type: string, required: true, description: The installation to run against.}
        window: {type: string, required: false, description: 'How far to look back, for example 24h.'}
      steps:
        - id: step-1
          tool: x_logs_count_lines
          args: {installation: '{{ .input.installation }}', window: '{{ .input.window }}'}
          store: true
        - id: step-2
          tool: x_ledger_get_statement
          args: {installation: '{{ .input.installation }}'}
          store: true
        - id: step-3
          tool: x_alerts_list_silences
          args: {installation: '{{ .input.installation }}'}
          store: true
        - id: step-4
          tool: x_metrics_list_label_values
          args: {installation: '{{ .input.installation }}'}
          store: true
        - id: step-5
          tool: x_clusters_get_cluster
          args: {installation: '{{ .input.installation }}'}
          store: true
  - name: reconcile-silence-hygiene-across-organizations
    labels: {fixture.invalid/verb: reconcile}
    config:
      name: reconcile-silence-hygiene-across-organizations
      description: 'Runbook: Reconcile silence hygiene across organizations.'
      args:
        installation: {type: string, required: true, description: The installation to run against.}
        window: {type: string, required: false, description: 'How far to look back, for example 24h.'}
      steps:
        - id: step-1
          tool: x_metrics_query_range
          args: {installation: '{{ .input.installation }}', window: '{{ .input.window }}'}
          store: true
        - id: step-2
          tool: x_tickets_search_tickets
          args: {query: reconcile silence hygiene}
          store: true
        - id: step-3
          tool: x_logs_get_retention
          args: {installation: '{{ .input.installation }}'}
          store: true
        - id: step-4
          tool: x_runbook-library_list_recent_runs
          args: {runbook: reconcile silence hygiene}
          store: true
  - name: reconcile-silence-hygiene-for-the-week
    labels: {fixture.invalid/verb: reconcile}
    config:
      name: reconcile-silence-hygiene-for-the-week
      description: 'Runbook: Reconcile silence hygiene for the week.'
      args:
        installation: {type: string, required: true, description: The installation to run against.}
        window: {type: string, required: false, description: 'How far to look back, for example 24h.'}
      steps:
        - id: step-1
          tool: x_clusters_get_quota
          args: {installation: '{{ .input.installation }}'}
          store: true
        - id: step-2
          tool: x_metrics_list_series
          args: {installation: '{{ .input.installation }}'}
          store: true
  - name: reconcile-upgrade-readiness-before-upgrade
    labels: {fixture.invalid/tier: gold, fixture.invalid/verb: reconcile}
    config:
      name: reconcile-upgrade-readiness-before-upgrade
      description: 'Runbook: Reconcile upgrade readiness before upgrade.'
      args:
        installation: {type: string, required: true, description: The installation to run against.}
        window: {type: string, required: false, description: 'How far to look back, for example 24h.'}
      steps:
        - id: step-1
          tool: x_ledger_list_adjustments
          args: {installation: '{{ .input.installation }}'}
          store: true
        - id: step-2
          tool: x_logs_tail_stream
          args: {installation: '{{ .input.installation }}'}
          store: true
  - name: reconcile-upgrade-readiness-for-the-week
    labels: {fixture.invalid/verb: reconcile}
    config:
      name: reconcile-upgrade-readiness-for-the-week
      description: 'Runbook: Reconcile upgrade readiness for the week.'
      args:
        installation: {type: string, required: true, description: The installation to run against.}
        window: {type: string, required: false, description: 'How far to look back, for example 24h.'}
      steps:
        - id: step-1
          tool: x_clusters_list_upgrades
          args: {installation: '{{ .input.installation }}', window: '{{ .input.window }}'}
          store: true
  - name: rotate-alert-noise-on-request
    labels: {fixture.invalid/verb: rotate}
    config:
      name: rotate-alert-noise-on-request
      description: 'Runbook: Rotate alert noise on request.'
      args:
        installation: {type: string, required: true, description: The installation to run against.}
        window: {type: string, required: false, description: 'How far to look back, for example 24h.'}
      steps:
        - id: step-1
          tool: x_ledger_get_statement
          args: {installation: '{{ .input.installation }}'}
          store: true
        - id: step-2
          tool: x_metrics_list_rules
          args: {installation: '{{ .input.installation }}'}
          store: true
  - name: rotate-certificate-expiry-across-organizations
    labels: {fixture.invalid/tier: gold, fixture.invalid/verb: rotate}
    config:
      name: rotate-certificate-expiry-across-organizations
      description: 'Runbook: Rotate certificate expiry across organizations.'
      args:
        installation: {type: string, required: true, description: The installation to run against.}
        window: {type: string, required: false, description: 'How far to look back, for example 24h.'}
      steps:
        - id: step-1
          tool: x_logs_list_streams
          args: {installation: '{{ .input.installation }}'}
          store: true
        - id: step-2
          tool: x_handbook_list_sections
          args: {path: rotate certificate expiry}
          store: true
        - id: step-3
          tool: x_metrics_list_rules
          args: {installation: '{{ .input.installation }}'}
          store: true
  - name: rotate-certificate-expiry-on-request
    labels: {fixture.invalid/verb: rotate}
    config:
      name: rotate-certificate-expiry-on-request
      description: 'Runbook: Rotate certificate expiry on request.'
      args:
        installation: {type: string, required: true, description: The installation to run against.}
        window: {type: string, required: false, description: 'How far to look back, for example 24h.'}
      steps:
        - id: step-1
          tool: x_clusters_list_upgrades
          args: {installation: '{{ .input.installation }}', window: '{{ .input.window }}'}
          store: true
        - id: step-2
          tool: x_ledger_list_usage
          args: {installation: '{{ .input.installation }}', window: '{{ .input.window }}'}
          store: true
        - id: step-3
          tool: x_handbook_read_page
          args: {path: rotate certificate expiry}
          store: true
        - id: step-4
          tool: x_ledger_list_usage
          args: {installation: '{{ .input.installation }}', window: '{{ .input.window }}'}
          store: true
        - id: step-5
          tool: x_clusters_get_quota
          args: {installation: '{{ .input.installation }}'}
          store: true
  - name: rotate-certificate-expiry-per-installation
    labels: {fixture.invalid/verb: rotate}
    config:
      name: rotate-certificate-expiry-per-installation
      description: 'Runbook: Rotate certificate expiry per installation.'
      args:
        installation: {type: string, required: true, description: The installation to run against.}
        window: {type: string, required: false, description: 'How far to look back, for example 24h.'}
      steps:
        - id: step-1
          tool: x_metrics_list_series
          args: {installation: '{{ .input.installation }}'}
          store: true
  - name: rotate-ingress-errors-across-organizations
    labels: {fixture.invalid/tier: gold, fixture.invalid/verb: rotate}
    config:
      name: rotate-ingress-errors-across-organizations
      description: 'Runbook: Rotate ingress errors across organizations.'
      args:
        installation: {type: string, required: true, description: The installation to run against.}
        window: {type: string, required: false, description: 'How far to look back, for example 24h.'}
      steps:
        - id: step-1
          tool: x_alerts_list_routes
          args: {installation: '{{ .input.installation }}'}
          store: true
        - id: step-2
          tool: x_clusters_get_release
          args: {installation: '{{ .input.installation }}'}
          store: true
        - id: step-3
          tool: x_ledger_list_usage
          args: {installation: '{{ .input.installation }}', window: '{{ .input.window }}'}
          store: true
        - id: step-4
          tool: x_tickets_list_tickets
          args: {queue: rotate ingress errors}
          store: true
        - id: step-5
          tool: x_metrics_list_targets
          args: {installation: '{{ .input.installation }}'}
          store: true
  - name: rotate-ingress-errors-after-incident
    labels: {fixture.invalid/verb: rotate}
    config:
      name: rotate-ingress-errors-after-incident
      description: 'Runbook: Rotate ingress errors after incident.'
      args:
        installation: {type: string, required: true, description: The installation to run against.}
        window: {type: string, required: false, description: 'How far to look back, for example 24h.'}
      steps:
        - id: step-1
          tool: x_alerts_get_alert
          args: {installation: '{{ .input.installation }}'}
          store: true
        - id: step-2
          tool: x_tickets_search_tickets
          args: {query: rotate ingress errors}
          store: true
        - id: step-3
          tool: x_metrics_list_series
          args: {installation: '{{ .input.installation }}'}
          store: true
        - id: step-4
          tool: x_metrics_list_series
          args: {installation: '{{ .input.installation }}'}
          store: true
        - id: step-5
          tool: x_logs_query_logs
          args: {installation: '{{ .input.installation }}', window: '{{ .input.window }}'}
          store: true
  - name: rotate-ingress-errors-for-the-week
    labels: {fixture.invalid/verb: rotate}
    config:
      name: rotate-ingress-errors-for-the-week
      description: 'Runbook: Rotate ingress errors for the week.'
      args:
        installation: {type: string, required: true, description: The installation to run against.}
        window: {type: string, required: false, description: 'How far to look back, for example 24h.'}
      steps:
        - id: step-1
          tool: x_ledger_list_adjustments
          args: {installation: '{{ .input.installation }}'}
          store: true
        - id: step-2
          tool: x_clusters_get_quota
          args: {installation: '{{ .input.installation }}'}
          store: true
  - name: rotate-ingress-errors-on-request
    labels: {fixture.invalid/tier: gold, fixture.invalid/verb: rotate}
    config:
      name: rotate-ingress-errors-on-request
      description: 'Runbook: Rotate ingress errors on request.'
      args:
        installation: {type: string, required: true, description: The installation to run against.}
        window: {type: string, required: false, description: 'How far to look back, for example 24h.'}
      steps:
        - id: step-1
          tool: x_ledger_get_statement
          args: {installation: '{{ .input.installation }}'}
          store: true
        - id: step-2
          tool: x_clusters_list_events
          args: {installation: '{{ .input.installation }}', window: '{{ .input.window }}'}
          store: true
        - id: step-3
          tool: x_handbook_list_recent_changes
          args: {window: rotate ingress errors}
          store: true
        - id: step-4
          tool: x_ledger_get_statement
          args: {installation: '{{ .input.installation }}'}
          store: true
        - id: step-5
          tool: x_tickets_list_tickets
          args: {queue: rotate ingress errors}
          store: true
  - name: rotate-ingress-errors-per-installation
    labels: {fixture.invalid/verb: rotate}
    config:
      name: rotate-ingress-errors-per-installation
      description: 'Runbook: Rotate ingress errors per installation.'
      args:
        installation: {type: string, required: true, description: The installation to run against.}
        window: {type: string, required: false, description: 'How far to look back, for example 24h.'}
      steps:
        - id: step-1
          tool: x_alerts_list_silences
          args: {installation: '{{ .input.installation }}'}
          store: true
        - id: step-2
          tool: x_metrics_query_range
          args: {installation: '{{ .input.installation }}', window: '{{ .input.window }}'}
          store: true
        - id: step-3
          tool: x_metrics_list_targets
          args: {installation: '{{ .input.installation }}'}
          store: true
        - id: step-4
          tool: x_clusters_list_events
          args: {installation: '{{ .input.installation }}', window: '{{ .input.window }}'}
          store: true
        - id: step-5
          tool: x_alerts_get_alert
          args: {installation: '{{ .input.installation }}'}
          store: true
  - name: rotate-log-volume-after-incident
    labels: {fixture.invalid/verb: rotate}
    config:
      name: rotate-log-volume-after-incident
      description: 'Runbook: Rotate log volume after incident.'
      args:
        installation: {type: string, required: true, description: The installation to run against.}
        window: {type: string, required: false, description: 'How far to look back, for example 24h.'}
      steps:
        - id: step-1
          tool: x_clusters_list_node_pools
          args: {installation: '{{ .input.installation }}'}
          store: true
  - name: rotate-log-volume-before-upgrade
    labels: {fixture.invalid/tier: gold, fixture.invalid/verb: rotate}
    config:
      name: rotate-log-volume-before-upgrade
      description: 'Runbook: Rotate log volume before upgrade.'
      args:
        installation: {type: string, required: true, description: The installation to run against.}
        window: {type: string, required: false, description: 'How far to look back, for example 24h.'}
      steps:
        - id: step-1
          tool: x_clusters_get_release
          args: {installation: '{{ .input.installation }}'}
          store: true
        - id: step-2
          tool: x_clusters_list_clusters
          args: {installation: '{{ .input.installation }}'}
          store: true
  - name: rotate-log-volume-for-the-week
    labels: {fixture.invalid/verb: rotate}
    config:
      name: rotate-log-volume-for-the-week
      description: 'Runbook: Rotate log volume for the week.'
      args:
        installation: {type: string, required: true, description: The installation to run against.}
        window: {type: string, required: false, description: 'How far to look back, for example 24h.'}
      steps:
        - id: step-1
          tool: x_metrics_list_label_values
          args: {installation: '{{ .input.installation }}'}
          store: true
  - name: rotate-log-volume-on-request
    labels: {fixture.invalid/verb: rotate}
    config:
      name: rotate-log-volume-on-request
      description: 'Runbook: Rotate log volume on request.'
      args:
        installation: {type: string, required: true, description: The installation to run against.}
        window: {type: string, required: false, description: 'How far to look back, for example 24h.'}
      steps:
        - id: step-1
          tool: x_clusters_list_upgrades
          args: {installation: '{{ .input.installation }}', window: '{{ .input.window }}'}
          store: true
        - id: step-2
          tool: x_metrics_query_range
          args: {installation: '{{ .input.installation }}', window: '{{ .input.window }}'}
          store: true
        - id: step-3
          tool: x_clusters_get_release
          args: {installation: '{{ .input.installation }}'}
          store: true
        - id: step-4
          tool: x_clusters_list_clusters
          args: {installation: '{{ .input.installation }}'}
          store: true
        - id: step-5
          tool: x_logs_get_retention
          args: {installation: '{{ .input.installation }}'}
          store: true
  - name: rotate-node-pool-capacity-across-organizations
    labels: {fixture.invalid/tier: gold, fixture.invalid/verb: rotate}
    config:
      name: rotate-node-pool-capacity-across-organizations
      description: 'Runbook: Rotate node pool capacity across organizations.'
      args:
        installation: {type: string, required: true, description: The installation to run against.}
        window: {type: string, required: false, description: 'How far to look back, for example 24h.'}
      steps:
        - id: step-1
          tool: x_clusters_get_cluster
          args: {installation: '{{ .input.installation }}'}
          store: true
        - id: step-2
          tool: x_tickets_read_ticket
          args: {ticket: rotate node pool capacity}
          store: true
        - id: step-3
          tool: x_alerts_list_silences
          args: {installation: '{{ .input.installation }}'}
          store: true
  - name: rotate-node-pool-capacity-after-incident
    labels: {fixture.invalid/verb: rotate}
    config:
      name: rotate-node-pool-capacity-after-incident
      description: 'Runbook: Rotate node pool capacity after incident.'
      args:
        installation: {type: string, required: true, description: The installation to run against.}
        window: {type: string, required: false, description: 'How far to look back, for example 24h.'}
      steps:
        - id: step-1
          tool: x_logs_query_logs
          args: {installation: '{{ .input.installation }}', window: '{{ .input.window }}'}
          store: true
        - id: step-2
          tool: x_ledger_export_records
          args: {installation: '{{ .input.installation }}'}
          store: true
  - name: rotate-node-pool-capacity-before-upgrade
    labels: {fixture.invalid/verb: rotate}
    config:
      name: rotate-node-pool-capacity-before-upgrade
      description: 'Runbook: Rotate node pool capacity before upgrade.'
      args:
        installation: {type: string, required: true, description: The installation to run against.}
        window: {type: string, required: false, description: 'How far to look back, for example 24h.'}
      steps:
        - id: step-1
          tool: x_logs_query_logs
          args: {installation: '{{ .input.installation }}', window: '{{ .input.window }}'}
          store: true
        - id: step-2
          tool: x_metrics_list_series
          args: {installation: '{{ .input.installation }}'}
          store: true
  - name: rotate-node-pool-capacity-per-installation
    labels: {fixture.invalid/tier: gold, fixture.invalid/verb: rotate}
    config:
      name: rotate-node-pool-capacity-per-installation
      description: 'Runbook: Rotate node pool capacity per installation.'
      args:
        installation: {type: string, required: true, description: The installation to run against.}
        window: {type: string, required: false, description: 'How far to look back, for example 24h.'}
      steps:
        - id: step-1
          tool: x_logs_tail_stream
          args: {installation: '{{ .input.installation }}'}
          store: true
        - id: step-2
          tool: x_ledger_get_statement
          args: {installation: '{{ .input.installation }}'}
          store: true
        - id: step-3
          tool: x_handbook_list_recent_changes
          args: {window: rotate node pool capacity}
          store: true
  - name: rotate-release-drift-across-organizations
    labels: {fixture.invalid/verb: rotate}
    config:
      name: rotate-release-drift-across-organizations
      description: 'Runbook: Rotate release drift across organizations.'
      args:
        installation: {type: string, required: true, description: The installation to run against.}
        window: {type: string, required: false, description: 'How far to look back, for example 24h.'}
      steps:
        - id: step-1
          tool: x_clusters_list_node_pools
          args: {installation: '{{ .input.installation }}'}
          store: true
        - id: step-2
          tool: x_clusters_get_cluster
          args: {installation: '{{ .input.installation }}'}
          store: true
        - id: step-3
          tool: x_runbook-library_list_runbooks
          args: {alert: rotate release drift}
          store: true
  - name: rotate-retention-gaps-after-incident
    labels: {fixture.invalid/verb: rotate}
    config:
      name: rotate-retention-gaps-after-incident
      description: 'Runbook: Rotate retention gaps after incident.'
      args:
        installation: {type: string, required: true, description: The installation to run against.}
        window: {type: string, required: false, description: 'How far to look back, for example 24h.'}
      steps:
        - id: step-1
          tool: x_clusters_get_release
          args: {installation: '{{ .input.installation }}'}
          store: true
  - name: rotate-retention-gaps-before-upgrade
    labels: {fixture.invalid/tier: gold, fixture.invalid/verb: rotate}
    config:
      name: rotate-retention-gaps-before-upgrade
      description: 'Runbook: Rotate retention gaps before upgrade.'
      args:
        installation: {type: string, required: true, description: The installation to run against.}
        window: {type: string, required: false, description: 'How far to look back, for example 24h.'}
      steps:
        - id: step-1
          tool: x_metrics_query_range
          args: {installation: '{{ .input.installation }}', window: '{{ .input.window }}'}
          store: true
        - id: step-2
          tool: x_metrics_list_targets
          args: {installation: '{{ .input.installation }}'}
          store: true
        - id: step-3
          tool: x_clusters_get_cluster
          args: {installation: '{{ .input.installation }}'}
          store: true
        - id: step-4
          tool: x_alerts_list_alerts
          args: {installation: '{{ .input.installation }}'}
          store: true
        - id: step-5
          tool: x_ledger_export_records
          args: {installation: '{{ .input.installation }}'}
          store: true
  - name: rotate-scrape-health-before-upgrade
    labels: {fixture.invalid/verb: rotate}
    config:
      name: rotate-scrape-health-before-upgrade
      description: 'Runbook: Rotate scrape health before upgrade.'
      args:
        installation: {type: string, required: true, description: The installation to run against.}
        window: {type: string, required: false, description: 'How far to look back, for example 24h.'}
      steps:
        - id: step-1
          tool: x_metrics_query_range
          args: {installation: '{{ .input.installation }}', window: '{{ .input.window }}'}
          store: true
        - id: step-2
          tool: x_tickets_list_tickets
          args: {queue: rotate scrape health}
          store: true
        - id: step-3
          tool: x_runbook-library_list_recent_runs
          args: {runbook: rotate scrape health}
          store: true
        - id: step-4
          tool: x_ledger_list_adjustments
          args: {installation: '{{ .input.installation }}'}
          store: true
  - name: rotate-scrape-health-on-request
    labels: {fixture.invalid/verb: rotate}
    config:
      name: rotate-scrape-health-on-request
      description: 'Runbook: Rotate scrape health on request.'
      args:
        installation: {type: string, required: true, description: The installation to run against.}
        window: {type: string, required: false, description: 'How far to look back, for example 24h.'}
      steps:
        - id: step-1
          tool: x_clusters_list_events
          args: {installation: '{{ .input.installation }}', window: '{{ .input.window }}'}
          store: true
  - name: rotate-silence-hygiene-before-upgrade
    labels: {fixture.invalid/tier: gold, fixture.invalid/verb: rotate}
    config:
      name: rotate-silence-hygiene-before-upgrade
      description: 'Runbook: Rotate silence hygiene before upgrade.'
      args:
        installation: {type: string, required: true, description: The installation to run against.}
        window: {type: string, required: false, description: 'How far to look back, for example 24h.'}
      steps:
        - id: step-1
          tool: x_metrics_list_label_values
          args: {installation: '{{ .input.installation }}'}
          store: true
  - name: rotate-silence-hygiene-on-request
    labels: {fixture.invalid/verb: rotate}
    config:
      name: rotate-silence-hygiene-on-request
      description: 'Runbook: Rotate silence hygiene on request.'
      args:
        installation: {type: string, required: true, description: The installation to run against.}
        window: {type: string, required: false, description: 'How far to look back, for example 24h.'}
      steps:
        - id: step-1
          tool: x_clusters_get_quota
          args: {installation: '{{ .input.installation }}'}
          store: true
  - name: rotate-silence-hygiene-per-installation
    labels: {fixture.invalid/verb: rotate}
    config:
      name: rotate-silence-hygiene-per-installation
      description: 'Runbook: Rotate silence hygiene per installation.'
      args:
        installation: {type: string, required: true, description: The installation to run against.}
        window: {type: string, required: false, description: 'How far to look back, for example 24h.'}
      steps:
        - id: step-1
          tool: x_logs_count_lines
          args: {installation: '{{ .input.installation }}', window: '{{ .input.window }}'}
          store: true
  - name: rotate-usage-anomalies-across-organizations
    labels: {fixture.invalid/tier: gold, fixture.invalid/verb: rotate}
    config:
      name: rotate-usage-anomalies-across-organizations
      description: 'Runbook: Rotate usage anomalies across organizations.'
      args:
        installation: {type: string, required: true, description: The installation to run against.}
        window: {type: string, required: false, description: 'How far to look back, for example 24h.'}
      steps:
        - id: step-1
          tool: x_clusters_list_upgrades
          args: {installation: '{{ .input.installation }}', window: '{{ .input.window }}'}
          store: true
        - id: step-2
          tool: x_alerts_get_alert
          args: {installation: '{{ .input.installation }}'}
          store: true
  - name: rotate-usage-anomalies-for-the-week
    labels: {fixture.invalid/verb: rotate}
    config:
      name: rotate-usage-anomalies-for-the-week
      description: 'Runbook: Rotate usage anomalies for the week.'
      args:
        installation: {type: string, required: true, description: The installation to run against.}
        window: {type: string, required: false, description: 'How far to look back, for example 24h.'}
      steps:
        - id: step-1
          tool: x_logs_tail_stream
          args: {installation: '{{ .input.installation }}'}
          store: true
  - name: snapshot-alert-noise-after-incident
    labels: {fixture.invalid/verb: snapshot}
    config:
      name: snapshot-alert-noise-after-incident
      description: 'Runbook: Snapshot alert noise after incident.'
      args:
        installation: {type: string, required: true, description: The installation to run against.}
        window: {type: string, required: false, description: 'How far to look back, for example 24h.'}
      steps:
        - id: step-1
          tool: x_alerts_list_routes
          args: {installation: '{{ .input.installation }}'}
          store: true
        - id: step-2
          tool: x_tickets_list_tickets
          args: {queue: snapshot alert noise}
          store: true
        - id: step-3
          tool: x_clusters_list_upgrades
          args: {installation: '{{ .input.installation }}', window: '{{ .input.window }}'}
          store: true
        - id: step-4
          tool: x_metrics_list_targets
          args: {installation: '{{ .input.installation }}'}
          store: true
        - id: step-5
          tool: x_logs_detect_patterns
          args: {installation: '{{ .input.installation }}', window: '{{ .input.window }}'}
          store: true
  - name: snapshot-alert-noise-for-the-week
    labels: {fixture.invalid/tier: gold, fixture.invalid/verb: snapshot}
    config:
      name: snapshot-alert-noise-for-the-week
      description: 'Runbook: Snapshot alert noise for the week.'
      args:
        installation: {type: string, required: true, description: The installation to run against.}
        window: {type: string, required: false, description: 'How far to look back, for example 24h.'}
      steps:
        - id: step-1
          tool: x_logs_get_retention
          args: {installation: '{{ .input.installation }}'}
          store: true
        - id: step-2
          tool: x_tickets_read_ticket
          args: {ticket: snapshot alert noise}
          store: true
  - name: snapshot-certificate-expiry-across-organizations
    labels: {fixture.invalid/verb: snapshot}
    config:
      name: snapshot-certificate-expiry-across-organizations
      description: 'Runbook: Snapshot certificate expiry across organizations.'
      args:
        installation: {type: string, required: true, description: The installation to run against.}
        window: {type: string, required: false, description: 'How far to look back, for example 24h.'}
      steps:
        - id: step-1
          tool: x_clusters_list_events
          args: {installation: '{{ .input.installation }}', window: '{{ .input.window }}'}
          store: true
        - id: step-2
          tool: x_tickets_read_ticket
          args: {ticket: snapshot certificate expiry}
          store: true
        - id: step-3
          tool: x_logs_query_logs
          args: {installation: '{{ .input.installation }}', window: '{{ .input.window }}'}
          store: true
        - id: step-4
          tool: x_clusters_list_node_pools
          args: {installation: '{{ .input.installation }}'}
          store: true
        - id: step-5
          tool: x_metrics_list_targets
          args: {installation: '{{ .input.installation }}'}
          store: true
  - name: snapshot-certificate-expiry-before-upgrade
    labels: {fixture.invalid/verb: snapshot}
    config:
      name: snapshot-certificate-expiry-before-upgrade
      description: 'Runbook: Snapshot certificate expiry before upgrade.'
      args:
        installation: {type: string, required: true, description: The installation to run against.}
        window: {type: string, required: false, description: 'How far to look back, for example 24h.'}
      steps:
        - id: step-1
          tool: x_metrics_query_instant
          args: {installation: '{{ .input.installation }}'}
          store: true
        - id: step-2
          tool: x_logs_get_retention
          args: {installation: '{{ .input.installation }}'}
          store: true
        - id: step-3
          tool: x_logs_get_retention
          args: {installation: '{{ .input.installation }}'}
          store: true
  - name: snapshot-certificate-expiry-on-request
    labels: {fixture.invalid/tier: gold, fixture.invalid/verb: snapshot}
    config:
      name: snapshot-certificate-expiry-on-request
      description: 'Runbook: Snapshot certificate expiry on request.'
      args:
        installation: {type: string, required: true, description: The installation to run against.}
        window: {type: string, required: false, description: 'How far to look back, for example 24h.'}
      steps:
        - id: step-1
          tool: x_clusters_list_clusters
          args: {installation: '{{ .input.installation }}'}
          store: true
        - id: step-2
          tool: x_clusters_list_events
          args: {installation: '{{ .input.installation }}', window: '{{ .input.window }}'}
          store: true
        - id: step-3
          tool: x_metrics_explain_series
          args: {installation: '{{ .input.installation }}'}
          store: true
        - id: step-4
          tool: x_metrics_query_range
          args: {installation: '{{ .input.installation }}', window: '{{ .input.window }}'}
          store: true
        - id: step-5
          tool: x_logs_detect_patterns
          args: {installation: '{{ .input.installation }}', window: '{{ .input.window }}'}
          store: true
  - name: snapshot-ingress-errors-on-request
    labels: {fixture.invalid/verb: snapshot}
    config:
      name: snapshot-ingress-errors-on-request
      description: 'Runbook: Snapshot ingress errors on request.'
      args:
        installation: {type: string, required: true, description: The installation to run against.}
        window: {type: string, required: false, description: 'How far to look back, for example 24h.'}
      steps:
        - id: step-1
          tool: x_clusters_get_quota
          args: {installation: '{{ .input.installation }}'}
          store: true
        - id: step-2
          tool: x_metrics_list_label_values
          args: {installation: '{{ .input.installation }}'}
          store: true
        - id: step-3
          tool: x_clusters_list_upgrades
          args: {installation: '{{ .input.installation }}', window: '{{ .input.window }}'}
          store: true
  - name: snapshot-log-volume-after-incident
    labels: {fixture.invalid/verb: snapshot}
    config:
      name: snapshot-log-volume-after-incident
      description: 'Runbook: Snapshot log volume after incident.'
      args:
        installation: {type: string, required: true, description: The installation to run against.}
        window: {type: string, required: false, description: 'How far to look back, for example 24h.'}
      steps:
        - id: step-1
          tool: x_metrics_list_series
          args: {installation: '{{ .input.installation }}'}
          store: true
        - id: step-2
          tool: x_metrics_query_instant
          args: {installation: '{{ .input.installation }}'}
          store: true
        - id: step-3
          tool: x_metrics_list_series
          args: {installation: '{{ .input.installation }}'}
          store: true
  - name: snapshot-log-volume-before-upgrade
    labels: {fixture.invalid/tier: gold, fixture.invalid/verb: snapshot}
    config:
      name: snapshot-log-volume-before-upgrade
      description: 'Runbook: Snapshot log volume before upgrade.'
      args:
        installation: {type: string, required: true, description: The installation to run against.}
        window: {type: string, required: false, description: 'How far to look back, for example 24h.'}
      steps:
        - id: step-1
          tool: x_alerts_list_routes
          args: {installation: '{{ .input.installation }}'}
          store: true
  - name: snapshot-log-volume-for-the-week
    labels: {fixture.invalid/verb: snapshot}
    config:
      name: snapshot-log-volume-for-the-week
      description: 'Runbook: Snapshot log volume for the week.'
      args:
        installation: {type: string, required: true, description: The installation to run against.}
        window: {type: string, required: false, description: 'How far to look back, for example 24h.'}
      steps:
        - id: step-1
          tool: x_clusters_get_quota
          args: {installation: '{{ .input.installation }}'}
          store: true
        - id: step-2
          tool: x_metrics_list_label_values
          args: {installation: '{{ .input.installation }}'}
          store: true
  - name: snapshot-node-pool-capacity-for-the-week
    labels: {fixture.invalid/verb: snapshot}
    config:
      name: snapshot-node-pool-capacity-for-the-week
      description: 'Runbook: Snapshot node pool capacity for the week.'
      args:
        installation: {type: string, required: true, description: The installation to run against.}
        window: {type: string, required: false, description: 'How far to look back, for example 24h.'}
      steps:
        - id: step-1
          tool: x_metrics_query_range
          args: {installation: '{{ .input.installation }}', window: '{{ .input.window }}'}
          store: true
        - id: step-2
          tool: x_alerts_list_routes
          args: {installation: '{{ .input.installation }}'}
          store: true
        - id: step-3
          tool: x_metrics_list_label_values
          args: {installation: '{{ .input.installation }}'}
          store: true
        - id: step-4
          tool: x_alerts_list_silences
          args: {installation: '{{ .input.installation }}'}
          store: true
        - id: step-5
          tool: x_runbook-library_list_runbooks
          args: {alert: snapshot node pool capacity}
          store: true
  - name: snapshot-node-pool-capacity-per-installation
    labels: {fixture.invalid/tier: gold, fixture.invalid/verb: snapshot}
    config:
      name: snapshot-node-pool-capacity-per-installation
      description: 'Runbook: Snapshot node pool capacity per installation.'
      args:
        installation: {type: string, required: true, description: The installation to run against.}
        window: {type: string, required: false, description: 'How far to look back, for example 24h.'}
      steps:
        - id: step-1
          tool: x_clusters_list_node_pools
          args: {installation: '{{ .input.installation }}'}
          store: true
  - name: snapshot-quota-headroom-per-installation
    labels: {fixture.invalid/verb: snapshot}
    config:
      name: snapshot-quota-headroom-per-installation
      description: 'Runbook: Snapshot quota headroom per installation.'
      args:
        installation: {type: string, required: true, description: The installation to run against.}
        window: {type: string, required: false, description: 'How far to look back, for example 24h.'}
      steps:
        - id: step-1
          tool: x_logs_query_logs
          args: {installation: '{{ .input.installation }}', window: '{{ .input.window }}'}
          store: true
        - id: step-2
          tool: x_metrics_list_rules
          args: {installation: '{{ .input.installation }}'}
          store: true
        - id: step-3
          tool: x_clusters_get_release
          args: {installation: '{{ .input.installation }}'}
          store: true
        - id: step-4
          tool: x_alerts_list_routes
          args: {installation: '{{ .input.installation }}'}
          store: true
  - name: snapshot-release-drift-after-incident
    labels: {fixture.invalid/verb: snapshot}
    config:
      name: snapshot-release-drift-after-incident
      description: 'Runbook: Snapshot release drift after incident.'
      args:
        installation: {type: string, required: true, description: The installation to run against.}
        window: {type: string, required: false, description: 'How far to look back, for example 24h.'}
      steps:
        - id: step-1
          tool: x_ledger_export_records
          args: {installation: '{{ .input.installation }}'}
          store: true
        - id: step-2
          tool: x_alerts_list_alerts
          args: {installation: '{{ .input.installation }}'}
          store: true
        - id: step-3
          tool: x_logs_detect_patterns
          args: {installation: '{{ .input.installation }}', window: '{{ .input.window }}'}
          store: true
        - id: step-4
          tool: x_ledger_list_adjustments
          args: {installation: '{{ .input.installation }}'}
          store: true
        - id: step-5
          tool: x_alerts_list_routes
          args: {installation: '{{ .input.installation }}'}
          store: true
  - name: snapshot-release-drift-before-upgrade
    labels: {fixture.invalid/tier: gold, fixture.invalid/verb: snapshot}
    config:
      name: snapshot-release-drift-before-upgrade
      description: 'Runbook: Snapshot release drift before upgrade.'
      args:
        installation: {type: string, required: true, description: The installation to run against.}
        window: {type: string, required: false, description: 'How far to look back, for example 24h.'}
      steps:
        - id: step-1
          tool: x_clusters_get_release
          args: {installation: '{{ .input.installation }}'}
          store: true
        - id: step-2
          tool: x_runbook-library_read_runbook
          args: {runbook: snapshot release drift}
          store: true
        - id: step-3
          tool: x_clusters_get_cluster
          args: {installation: '{{ .input.installation }}'}
          store: true
        - id: step-4
          tool: x_logs_count_lines
          args: {installation: '{{ .input.installation }}', window: '{{ .input.window }}'}
          store: true
  - name: snapshot-release-drift-for-the-week
    labels: {fixture.invalid/verb: snapshot}
    config:
      name: snapshot-release-drift-for-the-week
      description: 'Runbook: Snapshot release drift for the week.'
      args:
        installation: {type: string, required: true, description: The installation to run against.}
        window: {type: string, required: false, description: 'How far to look back, for example 24h.'}
      steps:
        - id: step-1
          tool: x_alerts_list_routes
          args: {installation: '{{ .input.installation }}'}
          store: true
        - id: step-2
          tool: x_alerts_list_silences
          args: {installation: '{{ .input.installation }}'}
          store: true
        - id: step-3
          tool: x_tickets_search_tickets
          args: {query: snapshot release drift}
          store: true
        - id: step-4
          tool: x_metrics_list_targets
          args: {installation: '{{ .input.installation }}'}
          store: true
        - id: step-5
          tool: x_handbook_list_recent_changes
          args: {window: snapshot release drift}
          store: true
  - name: snapshot-release-drift-on-request
    labels: {fixture.invalid/verb: snapshot}
    config:
      name: snapshot-release-drift-on-request
      description: 'Runbook: Snapshot release drift on request.'
      args:
        installation: {type: string, required: true, description: The installation to run against.}
        window: {type: string, required: false, description: 'How far to look back, for example 24h.'}
      steps:
        - id: step-1
          tool: x_ledger_get_statement
          args: {installation: '{{ .input.installation }}'}
          store: true
        - id: step-2
          tool: x_tickets_read_ticket
          args: {ticket: snapshot release drift}
          store: true
        - id: step-3
          tool: x_logs_count_lines
          args: {installation: '{{ .input.installation }}', window: '{{ .input.window }}'}
          store: true
        - id: step-4
          tool: x_metrics_list_series
          args: {installation: '{{ .input.installation }}'}
          store: true
  - name: snapshot-release-drift-per-installation
    labels: {fixture.invalid/tier: gold, fixture.invalid/verb: snapshot}
    config:
      name: snapshot-release-drift-per-installation
      description: 'Runbook: Snapshot release drift per installation.'
      args:
        installation: {type: string, required: true, description: The installation to run against.}
        window: {type: string, required: false, description: 'How far to look back, for example 24h.'}
      steps:
        - id: step-1
          tool: x_clusters_get_release
          args: {installation: '{{ .input.installation }}'}
          store: true
        - id: step-2
          tool: x_clusters_list_node_pools
          args: {installation: '{{ .input.installation }}'}
          store: true
  - name: snapshot-scrape-health-across-organizations
    labels: {fixture.invalid/verb: snapshot}
    config:
      name: snapshot-scrape-health-across-organizations
      description: 'Runbook: Snapshot scrape health across organizations.'
      args:
        installation: {type: string, required: true, description: The installation to run against.}
        window: {type: string, required: false, description: 'How far to look back, for example 24h.'}
      steps:
        - id: step-1
          tool: x_logs_count_lines
          args: {installation: '{{ .input.installation }}', window: '{{ .input.window }}'}
          store: true
  - name: snapshot-silence-hygiene-for-the-week
    labels: {fixture.invalid/verb: snapshot}
    config:
      name: snapshot-silence-hygiene-for-the-week
      description: 'Runbook: Snapshot silence hygiene for the week.'
      args:
        installation: {type: string, required: true, description: The installation to run against.}
        window: {type: string, required: false, description: 'How far to look back, for example 24h.'}
      steps:
        - id: step-1
          tool: x_metrics_list_rules
          args: {installation: '{{ .input.installation }}'}
          store: true
        - id: step-2
          tool: x_handbook_list_recent_changes
          args: {window: snapshot silence hygiene}
          store: true
        - id: step-3
          tool: x_tickets_list_tickets
          args: {queue: snapshot silence hygiene}
          store: true
        - id: step-4
          tool: x_clusters_list_events
          args: {installation: '{{ .input.installation }}', window: '{{ .input.window }}'}
          store: true
        - id: step-5
          tool: x_handbook_read_page
          args: {path: snapshot silence hygiene}
          store: true
  - name: snapshot-upgrade-readiness-across-organizations
    labels: {fixture.invalid/tier: gold, fixture.invalid/verb: snapshot}
    config:
      name: snapshot-upgrade-readiness-across-organizations
      description: 'Runbook: Snapshot upgrade readiness across organizations.'
      args:
        installation: {type: string, required: true, description: The installation to run against.}
        window: {type: string, required: false, description: 'How far to look back, for example 24h.'}
      steps:
        - id: step-1
          tool: x_clusters_get_cluster
          args: {installation: '{{ .input.installation }}'}
          store: true
        - id: step-2
          tool: x_logs_detect_patterns
          args: {installation: '{{ .input.installation }}', window: '{{ .input.window }}'}
          store: true
        - id: step-3
          tool: x_logs_count_lines
          args: {installation: '{{ .input.installation }}', window: '{{ .input.window }}'}
          store: true
  - name: snapshot-usage-anomalies-before-upgrade
    labels: {fixture.invalid/verb: snapshot}
    config:
      name: snapshot-usage-anomalies-before-upgrade
      description: 'Runbook: Snapshot usage anomalies before upgrade.'
      args:
        installation: {type: string, required: true, description: The installation to run against.}
        window: {type: string, required: false, description: 'How far to look back, for example 24h.'}
      steps:
        - id: step-1
          tool: x_clusters_get_release
          args: {installation: '{{ .input.installation }}'}
          store: true
        - id: step-2
          tool: x_metrics_list_rules
          args: {installation: '{{ .input.installation }}'}
          store: true
        - id: step-3
          tool: x_metrics_explain_series
          args: {installation: '{{ .input.installation }}'}
          store: true
  - name: summarize-alert-noise-across-organizations
    labels: {fixture.invalid/verb: summarize}
    config:
      name: summarize-alert-noise-across-organizations
      description: 'Runbook: Summarize alert noise across organizations.'
      args:
        installation: {type: string, required: true, description: The installation to run against.}
        window: {type: string, required: false, description: 'How far to look back, for example 24h.'}
      steps:
        - id: step-1
          tool: x_ledger_list_adjustments
          args: {installation: '{{ .input.installation }}'}
          store: true
        - id: step-2
          tool: x_metrics_list_label_values
          args: {installation: '{{ .input.installation }}'}
          store: true
  - name: summarize-alert-noise-after-incident
    labels: {fixture.invalid/tier: gold, fixture.invalid/verb: summarize}
    config:
      name: summarize-alert-noise-after-incident
      description: 'Runbook: Summarize alert noise after incident.'
      args:
        installation: {type: string, required: true, description: The installation to run against.}
        window: {type: string, required: false, description: 'How far to look back, for example 24h.'}
      steps:
        - id: step-1
          tool: x_ledger_export_records
          args: {installation: '{{ .input.installation }}'}
          store: true
        - id: step-2
          tool: x_logs_get_retention
          args: {installation: '{{ .input.installation }}'}
          store: true
        - id: step-3
          tool: x_metrics_list_label_values
          args: {installation: '{{ .input.installation }}'}
          store: true
        - id: step-4
          tool: x_alerts_list_silences
          args: {installation: '{{ .input.installation }}'}
          store: true
  - name: summarize-alert-noise-for-the-week
    labels: {fixture.invalid/verb: summarize}
    config:
      name: summarize-alert-noise-for-the-week
      description: 'Runbook: Summarize alert noise for the week.'
      args:
        installation: {type: string, required: true, description: The installation to run against.}
        window: {type: string, required: false, description: 'How far to look back, for example 24h.'}
      steps:
        - id: step-1
          tool: x_alerts_list_silences
          args: {installation: '{{ .input.installation }}'}
          store: true
  - name: summarize-alert-noise-on-request
    labels: {fixture.invalid/verb: summarize}
    config:
      name: summarize-alert-noise-on-request
      description: 'Runbook: Summarize alert noise on request.'
      args:
        installation: {type: string, required: true, description: The installation to run against.}
        window: {type: string, required: false, description: 'How far to look back, for example 24h.'}
      steps:
        - id: step-1
          tool: x_metrics_list_targets
          args: {installation: '{{ .input.installation }}'}
          store: true
  - name: summarize-certificate-expiry-after-incident
    labels: {fixture.invalid/tier: gold, fixture.invalid/verb: summarize}
    config:
      name: summarize-certificate-expiry-after-incident
      description: 'Runbook: Summarize certificate expiry after incident.'
      args:
        installation: {type: string, required: true, description: The installation to run against.}
        window: {type: string, required: false, description: 'How far to look back, for example 24h.'}
      steps:
        - id: step-1
          tool: x_metrics_query_range
          args: {installation: '{{ .input.installation }}', window: '{{ .input.window }}'}
          store: true
        - id: step-2
          tool: x_tickets_read_ticket
          args: {ticket: summarize certificate expiry}
          store: true
        - id: step-3
          tool: x_runbook-library_list_runbooks
          args: {alert: summarize certificate expiry}
          store: true
        - id: step-4
          tool: x_ledger_export_records
          args: {installation: '{{ .input.installation }}'}
          store: true
  - name: summarize-certificate-expiry-for-the-week
    labels: {fixture.invalid/verb: summarize}
    config:
      name: summarize-certificate-expiry-for-the-week
      description: 'Runbook: Summarize certificate expiry for the week.'
      args:
        installation: {type: string, required: true, description: The installation to run against.}
        window: {type: string, required: false, description: 'How far to look back, for example 24h.'}
      steps:
        - id: step-1
          tool: x_clusters_list_clusters
          args: {installation: '{{ .input.installation }}'}
          store: true
        - id: step-2
          tool: x_runbook-library_search_runbooks
          args: {query: summarize certificate expiry}
          store: true
        - id: step-3
          tool: x_ledger_get_statement
          args: {installation: '{{ .input.installation }}'}
          store: true
        - id: step-4
          tool: x_ledger_get_statement
          args: {installation: '{{ .input.installation }}'}
          store: true
  - name: summarize-ingress-errors-across-organizations
    labels: {fixture.invalid/verb: summarize}
    config:
      name: summarize-ingress-errors-across-organizations
      description: 'Runbook: Summarize ingress errors across organizations.'
      args:
        installation: {type: string, required: true, description: The installation to run against.}
        window: {type: string, required: false, description: 'How far to look back, for example 24h.'}
      steps:
        - id: step-1
          tool: x_logs_count_lines
          args: {installation: '{{ .input.installation }}', window: '{{ .input.window }}'}
          store: true
        - id: step-2
          tool: x_ledger_list_usage
          args: {installation: '{{ .input.installation }}', window: '{{ .input.window }}'}
          store: true
        - id: step-3
          tool: x_ledger_list_usage
          args: {installation: '{{ .input.installation }}', window: '{{ .input.window }}'}
          store: true
  - name: summarize-ingress-errors-for-the-week
    labels: {fixture.invalid/tier: gold, fixture.invalid/verb: summarize}
    config:
      name: summarize-ingress-errors-for-the-week
      description: 'Runbook: Summarize ingress errors for the week.'
      args:
        installation: {type: string, required: true, description: The installation to run against.}
        window: {type: string, required: false, description: 'How far to look back, for example 24h.'}
      steps:
        - id: step-1
          tool: x_clusters_list_clusters
          args: {installation: '{{ .input.installation }}'}
          store: true
  - name: summarize-log-volume-before-upgrade
    labels: {fixture.invalid/verb: summarize}
    config:
      name: summarize-log-volume-before-upgrade
      description: 'Runbook: Summarize log volume before upgrade.'
      args:
        installation: {type: string, required: true, description: The installation to run against.}
        window: {type: string, required: false, description: 'How far to look back, for example 24h.'}
      steps:
        - id: step-1
          tool: x_clusters_list_clusters
          args: {installation: '{{ .input.installation }}'}
          store: true
        - id: step-2
          tool: x_tickets_search_tickets
          args: {query: summarize log volume}
          store: true
        - id: step-3
          tool: x_clusters_get_release
          args: {installation: '{{ .input.installation }}'}
          store: true
  - name: summarize-log-volume-on-request
    labels: {fixture.invalid/verb: summarize}
    config:
      name: summarize-log-volume-on-request
      description: 'Runbook: Summarize log volume on request.'
      args:
        installation: {type: string, required: true, description: The installation to run against.}
        window: {type: string, required: false, description: 'How far to look back, for example 24h.'}
      steps:
        - id: step-1
          tool: x_clusters_list_clusters
          args: {installation: '{{ .input.installation }}'}
          store: true
  - name: summarize-node-pool-capacity-on-request
    labels: {fixture.invalid/tier: gold, fixture.invalid/verb: summarize}
    config:
      name: summarize-node-pool-capacity-on-request
      description: 'Runbook: Summarize node pool capacity on request.'
      args:
        installation: {type: string, required: true, description: The installation to run against.}
        window: {type: string, required: false, description: 'How far to look back, for example 24h.'}
      steps:
        - id: step-1
          tool: x_logs_get_retention
          args: {installation: '{{ .input.installation }}'}
          store: true
        - id: step-2
          tool: x_runbook-library_list_runbooks
          args: {alert: summarize node pool capacity}
          store: true
  - name: summarize-quota-headroom-before-upgrade
    labels: {fixture.invalid/verb: summarize}
    config:
      name: summarize-quota-headroom-before-upgrade
      description: 'Runbook: Summarize quota headroom before upgrade.'
      args:
        installation: {type: string, required: true, description: The installation to run against.}
        window: {type: string, required: false, description: 'How far to look back, for example 24h.'}
      steps:
        - id: step-1
          tool: x_alerts_list_alerts
          args: {installation: '{{ .input.installation }}'}
          store: true
        - id: step-2
          tool: x_runbook-library_list_runbooks
          args: {alert: summarize quota headroom}
          store: true
        - id: step-3
          tool: x_clusters_get_release
          args: {installation: '{{ .input.installation }}'}
          store: true
  - name: summarize-quota-headroom-per-installation
    labels: {fixture.invalid/verb: summarize}
    config:
      name: summarize-quota-headroom-per-installation
      description: 'Runbook: Summarize quota headroom per installation.'
      args:
        installation: {type: string, required: true, description: The installation to run against.}
        window: {type: string, required: false, description: 'How far to look back, for example 24h.'}
      steps:
        - id: step-1
          tool: x_logs_list_streams
          args: {installation: '{{ .input.installation }}'}
          store: true
        - id: step-2
          tool: x_alerts_list_silences
          args: {installation: '{{ .input.installation }}'}
          store: true
        - id: step-3
          tool: x_runbook-library_list_recent_runs
          args: {runbook: summarize quota headroom}
          store: true
        - id: step-4
          tool: x_handbook_search_pages
          args: {query: summarize quota headroom}
          store: true
        - id: step-5
          tool: x_metrics_list_label_values
          args: {installation: '{{ .input.installation }}'}
          store: true
  - name: summarize-retention-gaps-across-organizations
    labels: {fixture.invalid/tier: gold, fixture.invalid/verb: summarize}
    config:
      name: summarize-retention-gaps-across-organizations
      description: 'Runbook: Summarize retention gaps across organizations.'
      args:
        installation: {type: string, required: true, description: The installation to run against.}
        window: {type: string, required: false, description: 'How far to look back, for example 24h.'}
      steps:
        - id: step-1
          tool: x_logs_get_retention
          args: {installation: '{{ .input.installation }}'}
          store: true
        - id: step-2
          tool: x_metrics_query_range
          args: {installation: '{{ .input.installation }}', window: '{{ .input.window }}'}
          store: true
        - id: step-3
          tool: x_metrics_query_range
          args: {installation: '{{ .input.installation }}', window: '{{ .input.window }}'}
          store: true
        - id: step-4
          tool: x_handbook_list_recent_changes
          args: {window: summarize retention gaps}
          store: true
  - name: summarize-retention-gaps-on-request
    labels: {fixture.invalid/verb: summarize}
    config:
      name: summarize-retention-gaps-on-request
      description: 'Runbook: Summarize retention gaps on request.'
      args:
        installation: {type: string, required: true, description: The installation to run against.}
        window: {type: string, required: false, description: 'How far to look back, for example 24h.'}
      steps:
        - id: step-1
          tool: x_alerts_list_silences
          args: {installation: '{{ .input.installation }}'}
          store: true
        - id: step-2
          tool: x_alerts_get_alert
          args: {installation: '{{ .input.installation }}'}
          store: true
        - id: step-3
          tool: x_tickets_search_tickets
          args: {query: summarize retention gaps}
          store: true
        - id: step-4
          tool: x_clusters_get_cluster
          args: {installation: '{{ .input.installation }}'}
          store: true
        - id: step-5
          tool: x_clusters_list_upgrades
          args: {installation: '{{ .input.installation }}', window: '{{ .input.window }}'}
          store: true
  - name: summarize-retention-gaps-per-installation
    labels: {fixture.invalid/verb: summarize}
    config:
      name: summarize-retention-gaps-per-installation
      description: 'Runbook: Summarize retention gaps per installation.'
      args:
        installation: {type: string, required: true, description: The installation to run against.}
        window: {type: string, required: false, description: 'How far to look back, for example 24h.'}
      steps:
        - id: step-1
          tool: x_clusters_list_clusters
          args: {installation: '{{ .input.installation }}'}
          store: true
        - id: step-2
          tool: x_logs_query_logs
          args: {installation: '{{ .input.installation }}', window: '{{ .input.window }}'}
          store: true
        - id: step-3
          tool: x_clusters_get_cluster
          args: {installation: '{{ .input.installation }}'}
          store: true
        - id: step-4
          tool: x_metrics_list_series
          args: {installation: '{{ .input.installation }}'}
          store: true
  - name: summarize-scrape-health-across-organizations
    labels: {fixture.invalid/tier: gold, fixture.invalid/verb: summarize}
    config:
      name: summarize-scrape-health-across-organizations
      description: 'Runbook: Summarize scrape health across organizations.'
      args:
        installation: {type: string, required: true, description: The installation to run against.}
        window: {type: string, required: false, description: 'How far to look back, for example 24h.'}
      steps:
        - id: step-1
          tool: x_ledger_list_usage
          args: {installation: '{{ .input.installation }}', window: '{{ .input.window }}'}
          store: true
        - id: step-2
          tool: x_runbook-library_search_runbooks
          args: {query: summarize scrape health}
          store: true
        - id: step-3
          tool: x_logs_count_lines
          args: {installation: '{{ .input.installation }}', window: '{{ .input.window }}'}
          store: true
        - id: step-4
          tool: x_logs_list_streams
          args: {installation: '{{ .input.installation }}'}
          store: true
  - name: summarize-silence-hygiene-after-incident
    labels: {fixture.invalid/verb: summarize}
    config:
      name: summarize-silence-hygiene-after-incident
      description: 'Runbook: Summarize silence hygiene after incident.'
      args:
        installation: {type: string, required: true, description: The installation to run against.}
        window: {type: string, required: false, description: 'How far to look back, for example 24h.'}
      steps:
        - id: step-1
          tool: x_metrics_explain_series
          args: {installation: '{{ .input.installation }}'}
          store: true
        - id: step-2
          tool: x_runbook-library_list_recent_runs
          args: {runbook: summarize silence hygiene}
          store: true
        - id: step-3
          tool: x_metrics_list_label_values
          args: {installation: '{{ .input.installation }}'}
          store: true
  - name: summarize-silence-hygiene-on-request
    labels: {fixture.invalid/verb: summarize}
    config:
      name: summarize-silence-hygiene-on-request
      description: 'Runbook: Summarize silence hygiene on request.'
      args:
        installation: {type: string, required: true, description: The installation to run against.}
        window: {type: string, required: false, description: 'How far to look back, for example 24h.'}
      steps:
        - id: step-1
          tool: x_metrics_list_series
          args: {installation: '{{ .input.installation }}'}
          store: true
        - id: step-2
          tool: x_alerts_get_alert
          args: {installation: '{{ .input.installation }}'}
          store: true
  - name: triage-alert-noise-after-incident
    labels: {fixture.invalid/tier: gold, fixture.invalid/verb: triage}
    config:
      name: triage-alert-noise-after-incident
      description: 'Runbook: Triage alert noise after incident.'
      args:
        installation: {type: string, required: true, description: The installation to run against.}
        window: {type: string, required: false, description: 'How far to look back, for example 24h.'}
      steps:
        - id: step-1
          tool: x_metrics_query_instant
          args: {installation: '{{ .input.installation }}'}
          store: true
  - name: triage-alert-noise-for-the-week
    labels: {fixture.invalid/verb: triage}
    config:
      name: triage-alert-noise-for-the-week
      description: 'Runbook: Triage alert noise for the week.'
      args:
        installation: {type: string, required: true, description: The installation to run against.}
        window: {type: string, required: false, description: 'How far to look back, for example 24h.'}
      steps:
        - id: step-1
          tool: x_clusters_list_node_pools
          args: {installation: '{{ .input.installation }}'}
          store: true
        - id: step-2
          tool: x_handbook_list_recent_changes
          args: {window: triage alert noise}
          store: true
        - id: step-3
          tool: x_tickets_read_ticket
          args: {ticket: triage alert noise}
          store: true
        - id: step-4
          tool: x_tickets_read_ticket
          args: {ticket: triage alert noise}
          store: true
        - id: step-5
          tool: x_logs_list_streams
          args: {installation: '{{ .input.installation }}'}
          store: true
  - name: triage-alert-noise-on-request
    labels: {fixture.invalid/verb: triage}
    config:
      name: triage-alert-noise-on-request
      description: 'Runbook: Triage alert noise on request.'
      args:
        installation: {type: string, required: true, description: The installation to run against.}
        window: {type: string, required: false, description: 'How far to look back, for example 24h.'}
      steps:
        - id: step-1
          tool: x_clusters_get_release
          args: {installation: '{{ .input.installation }}'}
          store: true
        - id: step-2
          tool: x_ledger_list_usage
          args: {installation: '{{ .input.installation }}', window: '{{ .input.window }}'}
          store: true
        - id: step-3
          tool: x_tickets_read_ticket
          args: {ticket: triage alert noise}
          store: true
  - name: triage-certificate-expiry-after-incident
    labels: {fixture.invalid/tier: gold, fixture.invalid/verb: triage}
    config:
      name: triage-certificate-expiry-after-incident
      description: 'Runbook: Triage certificate expiry after incident.'
      args:
        installation: {type: string, required: true, description: The installation to run against.}
        window: {type: string, required: false, description: 'How far to look back, for example 24h.'}
      steps:
        - id: step-1
          tool: x_clusters_get_quota
          args: {installation: '{{ .input.installation }}'}
          store: true
        - id: step-2
          tool: x_metrics_query_range
          args: {installation: '{{ .input.installation }}', window: '{{ .input.window }}'}
          store: true
        - id: step-3
          tool: x_runbook-library_list_runbooks
          args: {alert: triage certificate expiry}
          store: true
  - name: triage-certificate-expiry-for-the-week
    labels: {fixture.invalid/verb: triage}
    config:
      name: triage-certificate-expiry-for-the-week
      description: 'Runbook: Triage certificate expiry for the week.'
      args:
        installation: {type: string, required: true, description: The installation to run against.}
        window: {type: string, required: false, description: 'How far to look back, for example 24h.'}
      steps:
        - id: step-1
          tool: x_clusters_list_events
          args: {installation: '{{ .input.installation }}', window: '{{ .input.window }}'}
          store: true
  - name: triage-ingress-errors-before-upgrade
    labels: {fixture.invalid/verb: triage}
    config:
      name: triage-ingress-errors-before-upgrade
      description: 'Runbook: Triage ingress errors before upgrade.'
      args:
        installation: {type: string, required: true, description: The installation to run against.}
        window: {type: string, required: false, description: 'How far to look back, for example 24h.'}
      steps:
        - id: step-1
          tool: x_alerts_get_alert
          args: {installation: '{{ .input.installation }}'}
          store: true
        - id: step-2
          tool: x_metrics_list_targets
          args: {installation: '{{ .input.installation }}'}
          store: true
        - id: step-3
          tool: x_runbook-library_list_runbooks
          args: {alert: triage ingress errors}
          store: true
        - id: step-4
          tool: x_handbook_list_sections
          args: {path: triage ingress errors}
          store: true
        - id: step-5
          tool: x_metrics_list_series
          args: {installation: '{{ .input.installation }}'}
          store: true
  - name: triage-ingress-errors-for-the-week
    labels: {fixture.invalid/tier: gold, fixture.invalid/verb: triage}
    config:
      name: triage-ingress-errors-for-the-week
      description: 'Runbook: Triage ingress errors for the week.'
      args:
        installation: {type: string, required: true, description: The installation to run against.}
        window: {type: string, required: false, description: 'How far to look back, for example 24h.'}
      steps:
        - id: step-1
          tool: x_metrics_list_targets
          args: {installation: '{{ .input.installation }}'}
          store: true
        - id: step-2
          tool: x_clusters_list_clusters
          args: {installation: '{{ .input.installation }}'}
          store: true
        - id: step-3
          tool: x_logs_query_logs
          args: {installation: '{{ .input.installation }}', window: '{{ .input.window }}'}
          store: true
        - id: step-4
          tool: x_tickets_read_ticket
          args: {ticket: triage ingress errors}
          store: true
  - name: triage-ingress-errors-on-request
    labels: {fixture.invalid/verb: triage}
    config:
      name: triage-ingress-errors-on-request
      description: 'Runbook: Triage ingress errors on request.'
      args:
        installation: {type: string, required: true, description: The installation to run against.}
        window: {type: string, required: false, description: 'How far to look back, for example 24h.'}
      steps:
        - id: step-1
          tool: x_ledger_list_usage
          args: {installation: '{{ .input.installation }}', window: '{{ .input.window }}'}
          store: true
        - id: step-2
          tool: x_logs_list_streams
          args: {installation: '{{ .input.installation }}'}
          store: true
        - id: step-3
          tool: x_metrics_list_targets
          args: {installation: '{{ .input.installation }}'}
          store: true
  - name: triage-ingress-errors-per-installation
    labels: {fixture.invalid/verb: triage}
    config:
      name: triage-ingress-errors-per-installation
      description: 'Runbook: Triage ingress errors per installation.'
      args:
        installation: {type: string, required: true, description: The installation to run against.}
        window: {type: string, required: false, description: 'How far to look back, for example 24h.'}
      steps:
        - id: step-1
          tool: x_logs_list_streams
          args: {installation: '{{ .input.installation }}'}
          store: true
        - id: step-2
          tool: x_logs_count_lines
          args: {installation: '{{ .input.installation }}', window: '{{ .input.window }}'}
          store: true
  - name: triage-log-volume-across-organizations
    labels: {fixture.invalid/tier: gold, fixture.invalid/verb: triage}
    config:
      name: triage-log-volume-across-organizations
      description: 'Runbook: Triage log volume across organizations.'
      args:
        installation: {type: string, required: true, description: The installation to run against.}
        window: {type: string, required: false, description: 'How far to look back, for example 24h.'}
      steps:
        - id: step-1
          tool: x_alerts_get_alert
          args: {installation: '{{ .input.installation }}'}
          store: true
        - id: step-2
          tool: x_logs_count_lines
          args: {installation: '{{ .input.installation }}', window: '{{ .input.window }}'}
          store: true
        - id: step-3
          tool: x_ledger_export_records
          args: {installation: '{{ .input.installation }}'}
          store: true
        - id: step-4
          tool: x_alerts_list_alerts
          args: {installation: '{{ .input.installation }}'}
          store: true
        - id: step-5
          tool: x_ledger_export_records
          args: {installation: '{{ .input.installation }}'}
          store: true
  - name: triage-log-volume-after-incident
    labels: {fixture.invalid/verb: triage}
    config:
      name: triage-log-volume-after-incident
      description: 'Runbook: Triage log volume after incident.'
      args:
        installation: {type: string, required: true, description: The installation to run against.}
        window: {type: string, required: false, description: 'How far to look back, for example 24h.'}
      steps:
        - id: step-1
          tool: x_alerts_list_routes
          args: {installation: '{{ .input.installation }}'}
          store: true
        - id: step-2
          tool: x_logs_detect_patterns
          args: {installation: '{{ .input.installation }}', window: '{{ .input.window }}'}
          store: true
        - id: step-3
          tool: x_runbook-library_list_runbooks
          args: {alert: triage log volume}
          store: true
  - name: triage-log-volume-on-request
    labels: {fixture.invalid/verb: triage}
    config:
      name: triage-log-volume-on-request
      description: 'Runbook: Triage log volume on request.'
      args:
        installation: {type: string, required: true, description: The installation to run against.}
        window: {type: string, required: false, description: 'How far to look back, for example 24h.'}
      steps:
        - id: step-1
          tool: x_metrics_explain_series
          args: {installation: '{{ .input.installation }}'}
          store: true
        - id: step-2
          tool: x_handbook_read_page
          args: {path: triage log volume}
          store: true
        - id: step-3
          tool: x_ledger_list_usage
          args: {installation: '{{ .input.installation }}', window: '{{ .input.window }}'}
          store: true
        - id: step-4
          tool: x_clusters_get_cluster
          args: {installation: '{{ .input.installation }}'}
          store: true
  - name: triage-node-pool-capacity-before-upgrade
    labels: {fixture.invalid/tier: gold, fixture.invalid/verb: triage}
    config:
      name: triage-node-pool-capacity-before-upgrade
      description: 'Runbook: Triage node pool capacity before upgrade.'
      args:
        installation: {type: string, required: true, description: The installation to run against.}
        window: {type: string, required: false, description: 'How far to look back, for example 24h.'}
      steps:
        - id: step-1
          tool: x_logs_detect_patterns
          args: {installation: '{{ .input.installation }}', window: '{{ .input.window }}'}
          store: true
        - id: step-2
          tool: x_alerts_get_alert
          args: {installation: '{{ .input.installation }}'}
          store: true
        - id: step-3
          tool: x_logs_get_retention
          args: {installation: '{{ .input.installation }}'}
          store: true
        - id: step-4
          tool: x_runbook-library_read_runbook
          args: {runbook: triage node pool capacity}
          store: true
  - name: triage-quota-headroom-across-organizations
    labels: {fixture.invalid/verb: triage}
    config:
      name: triage-quota-headroom-across-organizations
      description: 'Runbook: Triage quota headroom across organizations.'
      args:
        installation: {type: string, required: true, description: The installation to run against.}
        window: {type: string, required: false, description: 'How far to look back, for example 24h.'}
      steps:
        - id: step-1
          tool: x_metrics_explain_series
          args: {installation: '{{ .input.installation }}'}
          store: true
  - name: triage-quota-headroom-after-incident
    labels: {fixture.invalid/verb: triage}
    config:
      name: triage-quota-headroom-after-incident
      description: 'Runbook: Triage quota headroom after incident.'
      args:
        installation: {type: string, required: true, description: The installation to run against.}
        window: {type: string, required: false, description: 'How far to look back, for example 24h.'}
      steps:
        - id: step-1
          tool: x_logs_query_logs
          args: {installation: '{{ .input.installation }}', window: '{{ .input.window }}'}
          store: true
        - id: step-2
          tool: x_logs_list_streams
          args: {installation: '{{ .input.installation }}'}
          store: true
        - id: step-3
          tool: x_clusters_get_release
          args: {installation: '{{ .input.installation }}'}
          store: true
  - name: triage-quota-headroom-before-upgrade
    labels: {fixture.invalid/tier: gold, fixture.invalid/verb: triage}
    config:
      name: triage-quota-headroom-before-upgrade
      description: 'Runbook: Triage quota headroom before upgrade.'
      args:
        installation: {type: string, required: true, description: The installation to run against.}
        window: {type: string, required: false, description: 'How far to look back, for example 24h.'}
      steps:
        - id: step-1
          tool: x_ledger_list_adjustments
          args: {installation: '{{ .input.installation }}'}
          store: true
        - id: step-2
          tool: x_tickets_search_tickets
          args: {query: triage quota headroom}
          store: true
        - id: step-3
          tool: x_ledger_list_adjustments
          args: {installation: '{{ .input.installation }}'}
          store: true
        - id: step-4
          tool: x_clusters_list_upgrades
          args: {installation: '{{ .input.installation }}', window: '{{ .input.window }}'}
          store: true
        - id: step-5
          tool: x_clusters_get_cluster
          args: {installation: '{{ .input.installation }}'}
          store: true
  - name: triage-quota-headroom-for-the-week
    labels: {fixture.invalid/verb: triage}
    config:
      name: triage-quota-headroom-for-the-week
      description: 'Runbook: Triage quota headroom for the week.'
      args:
        installation: {type: string, required: true, description: The installation to run against.}
        window: {type: string, required: false, description: 'How far to look back, for example 24h.'}
      steps:
        - id: step-1
          tool: x_ledger_get_statement
          args: {installation: '{{ .input.installation }}'}
          store: true
        - id: step-2
          tool: x_tickets_list_tickets
          args: {queue: triage quota headroom}
          store: true
        - id: step-3
          tool: x_clusters_list_clusters
          args: {installation: '{{ .input.installation }}'}
          store: true
        - id: step-4
          tool: x_metrics_list_series
          args: {installation: '{{ .input.installation }}'}
          store: true
  - name: triage-release-drift-after-incident
    labels: {fixture.invalid/verb: triage}
    config:
      name: triage-release-drift-after-incident
      description: 'Runbook: Triage release drift after incident.'
      args:
        installation: {type: string, required: true, description: The installation to run against.}
        window: {type: string, required: false, description: 'How far to look back, for example 24h.'}
      steps:
        - id: step-1
          tool: x_clusters_get_quota
          args: {installation: '{{ .input.installation }}'}
          store: true
        - id: step-2
          tool: x_clusters_list_events
          args: {installation: '{{ .input.installation }}', window: '{{ .input.window }}'}
          store: true
  - name: triage-retention-gaps-across-organizations
    labels: {fixture.invalid/tier: gold, fixture.invalid/verb: triage}
    config:
      name: triage-retention-gaps-across-organizations
      description: 'Runbook: Triage retention gaps across organizations.'
      args:
        installation: {type: string, required: true, description: The installation to run against.}
        window: {type: string, required: false, description: 'How far to look back, for example 24h.'}
      steps:
        - id: step-1
          tool: x_clusters_list_events
          args: {installation: '{{ .input.installation }}', window: '{{ .input.window }}'}
          store: true
        - id: step-2
          tool: x_metrics_list_series
          args: {installation: '{{ .input.installation }}'}
          store: true
        - id: step-3
          tool: x_metrics_list_series
          args: {installation: '{{ .input.installation }}'}
          store: true
        - id: step-4
          tool: x_metrics_query_range
          args: {installation: '{{ .input.installation }}', window: '{{ .input.window }}'}
          store: true
  - name: triage-retention-gaps-before-upgrade
    labels: {fixture.invalid/verb: triage}
    config:
      name: triage-retention-gaps-before-upgrade
      description: 'Runbook: Triage retention gaps before upgrade.'
      args:
        installation: {type: string, required: true, description: The installation to run against.}
        window: {type: string, required: false, description: 'How far to look back, for example 24h.'}
      steps:
        - id: step-1
          tool: x_ledger_list_adjustments
          args: {installation: '{{ .input.installation }}'}
          store: true
        - id: step-2
          tool: x_runbook-library_search_runbooks
          args: {query: triage retention gaps}
          store: true
        - id: step-3
          tool: x_logs_query_logs
          args: {installation: '{{ .input.installation }}', window: '{{ .input.window }}'}
          store: true
        - id: step-4
          tool: x_metrics_query_instant
          args: {installation: '{{ .input.installation }}'}
          store: true
  - name: triage-scrape-health-before-upgrade
    labels: {fixture.invalid/verb: triage}
    config:
      name: triage-scrape-health-before-upgrade
      description: 'Runbook: Triage scrape health before upgrade.'
      args:
        installation: {type: string, required: true, description: The installation to run against.}
        window: {type: string, required: false, description: 'How far to look back, for example 24h.'}
      steps:
        - id: step-1
          tool: x_logs_get_retention
          args: {installation: '{{ .input.installation }}'}
          store: true
  - name: triage-scrape-health-for-the-week
    labels: {fixture.invalid/tier: gold, fixture.invalid/verb: triage}
    config:
      name: triage-scrape-health-for-the-week
      description: 'Runbook: Triage scrape health for the week.'
      args:
        installation: {type: string, required: true, description: The installation to run against.}
        window: {type: string, required: false, description: 'How far to look back, for example 24h.'}
      steps:
        - id: step-1
          tool: x_metrics_query_instant
          args: {installation: '{{ .input.installation }}'}
          store: true
        - id: step-2
          tool: x_handbook_list_recent_changes
          args: {window: triage scrape health}
          store: true
        - id: step-3
          tool: x_alerts_list_routes
          args: {installation: '{{ .input.installation }}'}
          store: true
  - name: triage-scrape-health-per-installation
    labels: {fixture.invalid/verb: triage}
    config:
      name: triage-scrape-health-per-installation
      description: 'Runbook: Triage scrape health per installation.'
      args:
        installation: {type: string, required: true, description: The installation to run against.}
        window: {type: string, required: false, description: 'How far to look back, for example 24h.'}
      steps:
        - id: step-1
          tool: x_logs_query_logs
          args: {installation: '{{ .input.installation }}', window: '{{ .input.window }}'}
          store: true
        - id: step-2
          tool: x_handbook_read_page
          args: {path: triage scrape health}
          store: true
        - id: step-3
          tool: x_alerts_list_routes
          args: {installation: '{{ .input.installation }}'}
          store: true
        - id: step-4
          tool: x_ledger_export_records
          args: {installation: '{{ .input.installation }}'}
          store: true
        - id: step-5
          tool: x_metrics_list_targets
          args: {installation: '{{ .input.installation }}'}
          store: true
  - name: triage-silence-hygiene-across-organizations
    labels: {fixture.invalid/verb: triage}
    config:
      name: triage-silence-hygiene-across-organizations
      description: 'Runbook: Triage silence hygiene across organizations.'
      args:
        installation: {type: string, required: true, description: The installation to run against.}
        window: {type: string, required: false, description: 'How far to look back, for example 24h.'}
      steps:
        - id: step-1
          tool: x_clusters_get_cluster
          args: {installation: '{{ .input.installation }}'}
          store: true
        - id: step-2
          tool: x_ledger_list_adjustments
          args: {installation: '{{ .input.installation }}'}
          store: true
        - id: step-3
          tool: x_clusters_get_cluster
          args: {installation: '{{ .input.installation }}'}
          store: true
        - id: step-4
          tool: x_logs_query_logs
          args: {installation: '{{ .input.installation }}', window: '{{ .input.window }}'}
          store: true
  - name: triage-silence-hygiene-on-request
    labels: {fixture.invalid/tier: gold, fixture.invalid/verb: triage}
    config:
      name: triage-silence-hygiene-on-request
      description: 'Runbook: Triage silence hygiene on request.'
      args:
        installation: {type: string, required: true, description: The installation to run against.}
        window: {type: string, required: false, description: 'How far to look back, for example 24h.'}
      steps:
        - id: step-1
          tool: x_logs_list_streams
          args: {installation: '{{ .input.installation }}'}
          store: true
        - id: step-2
          tool: x_alerts_get_alert
          args: {installation: '{{ .input.installation }}'}
          store: true
        - id: step-3
          tool: x_clusters_get_release
          args: {installation: '{{ .input.installation }}'}
          store: true
        - id: step-4
          tool: x_runbook-library_read_runbook
          args: {runbook: triage silence hygiene}
          store: true
  - name: triage-upgrade-readiness-on-request
    labels: {fixture.invalid/verb: triage}
    config:
      name: triage-upgrade-readiness-on-request
      description: 'Runbook: Triage upgrade readiness on request.'
      args:
        installation: {type: string, required: true, description: The installation to run against.}
        window: {type: string, required: false, description: 'How far to look back, for example 24h.'}
      steps:
        - id: step-1
          tool: x_clusters_get_quota
          args: {installation: '{{ .input.installation }}'}
          store: true
        - id: step-2
          tool: x_runbook-library_list_runbooks
          args: {alert: triage upgrade readiness}
          store: true
        - id: step-3
          tool: x_ledger_list_usage
          args: {installation: '{{ .input.installation }}', window: '{{ .input.window }}'}
          store: true
  - name: triage-usage-anomalies-per-installation
    labels: {fixture.invalid/verb: triage}
    config:
      name: triage-usage-anomalies-per-installation
      description: 'Runbook: Triage usage anomalies per installation.'
      args:
        installation: {type: string, required: true, description: The installation to run against.}
        window: {type: string, required: false, description: 'How far to look back, for example 24h.'}
      steps:
        - id: step-1
          tool: x_metrics_list_rules
          args: {installation: '{{ .input.installation }}'}
          store: true
        - id: step-2
          tool: x_logs_tail_stream
          args: {installation: '{{ .input.installation }}'}
          store: true
        - id: step-3
          tool: x_clusters_list_events
          args: {installation: '{{ .input.installation }}', window: '{{ .input.window }}'}
          store: true
        - id: step-4
          tool: x_logs_query_logs
          args: {installation: '{{ .input.installation }}', window: '{{ .input.window }}'}
          store: true
        - id: step-5
          tool: x_logs_count_lines
          args: {installation: '{{ .input.installation }}', window: '{{ .input.window }}'}
          store: true
