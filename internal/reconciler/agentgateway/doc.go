// Package agentgateway builds a Config from an MCPServer spec: an
// agentgateway configuration independent of the eventual serialization
// (Kubernetes CRDs or agw native YAML). NewConfig is pure.
package agentgateway
