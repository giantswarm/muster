// Package agentgateway compiles an MCPServer spec into a Config: an
// agentgateway configuration independent of the eventual serialization
// (Kubernetes CRDs or agw native YAML). Compile is pure.
package agentgateway
